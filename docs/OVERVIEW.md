# Forge — Project Overview

Forge is a batch-first LLM task orchestrator: it decomposes a request into independent work units, runs each as a single document-completion step in a Temporal workflow, validates, and reconciles. This file is the **status-of-record** — what is built, what remains, and what is known-broken. It points to the live tracker and roadmap rather than restating them.

- **Live task list** (what's in flight, completed vs. not): [../development-plans/TASKS.md](../development-plans/TASKS.md)
- **Phase roadmap** (the 14-phase build): [PHASES.md](PHASES.md)
- **How it works** (architecture): [ARCHITECTURE.md](ARCHITECTURE.md) · **Why** (decisions): [DECISIONS.md](DECISIONS.md)
- **Behavioral spec** (Gherkin): [requirements/](requirements/)

Completion below was established by verifying against the code on 2026-06-04, not by prior documentation. The OCR extraction was verified against the code on 2026-06-09 (merge `2556bfe`); the packages it created were later absorbed back into this repo as workspace members (D98, 2026-07-16) — the platform/consumer split survives as a package boundary, not a repo boundary.

## Status at a glance

- **Release 1 (core orchestrator + batch) is shipped.** Phases 1–12 and 14 are implemented and wired into the worker; Phase 13 (tree-sitter) is deferred to Release 2. See [PHASES.md](PHASES.md).
- **This repo is the monorepo** (D98, T2.1 complete 2026-07-16): one uv workspace, one lock, one venv — `apps/pbook`, `apps/ocr`, and `libs/sax-platform` (added by Phase 3, T3.1). A bare clone is self-contained; Python is pinned to **3.14** (standard GIL). The five predecessor repos are archived; `libs/sax-platform` is new to Phase 3, not a predecessor. `libs/forge-contracts` was absorbed into `libs/sax-platform` and retired as a package identity at T3.4, and `libs/sax-llm` was deleted at T3.5 — the workspace is **four packages today** (forge, pbook, ocr, sax-platform).
- **Deployment is local-first** (D99, T0.7 complete 2026-07-16; amended by D102, T0.9 2026-07-22, D103, 2026-07-25, and **D104, 2026-07-31**): forge owns no infrastructure. Temporal comes from the shared `sax-temporal` stacks (prod `:7243`, dev `:7236`) and Postgres from the shared `sax-datastores` stacks (prod `:5442`, dev `:5432`); forge's own podman stack, its boot agent, and its nightly backup agent are deleted. The store of record is `forge_prod` (forge + ocr) and `pbook_prod`, one role per project owning its same-named database with PUBLIC connect revoked, so a wrong-environment or wrong-project URL fails at authentication; S3 holds blobs, and offsite durability for every database — Temporal's included — is the operator's nightly dump plus its scheduled restore check. Supabase is retired/frozen. The workers run under launchd on an always-on desktop, **out of a commit-pinned checkout** (`~/repos-sax/forge-prod`, D103): production deploys only via `make prod-deploy REF=<ref>`, a worker refuses to start on a dirty or unverifiable prod checkout (exit 78), and every worker's Temporal identity carries its launch-time git version (`prod-forge-worker-1@<sha>`), so `temporal task-queue describe` answers "which code is running?" authoritatively. Every process must declare `FORGE_ENV` (no default; prod requires an explicit ack), and dev is isolated in the `forge-dev` Temporal namespace, running the live working tree by design. EC2, Terraform, and the mTLS remote-access gateway are deleted — remote access is out of scope until revisited.
- **Migration status:** Phases 1–2 done (root CI, 85% gates across the workspace, import-linter DAG contracts, mypy strict everywhere); Phase 3 done — T3.1–T3.3 landed the `sax-platform` package (its LLM client both lanes, the tiers registry, and the Mistral OCR capability); T3.4 landed the platform plumbing modules (`sax_platform.temporal`/`db`/`embeddings`/`config`/`logging`) and the `sax_platform.contracts` sandbox-light layer, retiring `forge-contracts` and migrating forge/ocr/pbook onto the platform; T3.5 retired forced tool use platform-wide (forge and pbook now complete structured outputs through `sax_platform.llm`, frozen `OUTPUT_TYPES` replace the string registries, `libs/sax-llm` deleted); T3.6 landed composition roots and closed Phase 3. **Phase 4 is done** — T4.1 (forge timer-loop transport, D88/R1), T4.2 (ocr owns its batches; SPI deleted), T4.3 (decision sweep, D100), T4.4 (ocr status-hint tracker, D101: a stateless 120s tracker sweeps Mistral's list endpoint and broadcasts hints to signal-wait store children). **T0.9 closed Phase 0** (D102: application store rehomed to local Postgres, `FORGE_ENV` guard). The **staging lane** followed (2026-07-23): per-env Temporal namespaces with an enforced coherence check, `--env` profile loading on every CLI, `ocr migrate`, `make dev-worker`. **Phase 5 is open: T5.1 (pure step logic, D95) landed 2026-07-24** — the `evaluate_transition` activity deleted with transition evaluation inlined from the new zero-`temporalio` `forge/step_logic.py`, result slimming (contents travel at most once; embedded results carry sha256 digests), typed `failure_kind`, and `LLMRunTotals` on TaskResult; same day, worker restarts became lane-scoped (`make workers-restart` is production-only; `make dev-worker-restart` for the dev lane) and the pbook worker was installed in prod (`pbook-task-queue` has pollers for the first time). **T5.2 (single step block) landed 2026-07-25** — the step pipeline exists once in `blocks/step.py` (`run_step_attempts` + the three-row `MODE_POLICIES` table), mid-step exceptions leak neither worktree nor branch, and the git seams are idempotent under activity retry; the committed replay histories pass unregenerated. Same day: worker identities gained launch-time version stamps and lane-based bases (`prod-`/`dev-`), and D103 pinned production to a dedicated checkout with a dirty-refusal guard (see the deployment bullet above). **T5.3 (single gather + dispatch) landed 2026-07-26** — the typed dispatch layer and the fan-out gather each exist once (`blocks/dispatch.py` + `blocks/host.py`; `blocks/gather.py`): all five dispatch arms persist interaction records with token counts (exploration for the first time — its counts previously died in a trace span), a raising fan-out child becomes a failed `SubTaskResult` (`child_crashed`, the tenth `failure_kind`) while siblings finish and the parent still writes its run record, `ParentClosePolicy` is explicitly TERMINATE, the nested gather's exception worktree leak is closed, and every activity timeout/retry preset moved to the single `forge/presets.py`; the committed replay histories passed unregenerated through the whole task. **T5.4 (split the monolith) landed 2026-07-27** — re-baselined same day by owner adjudication (its premise was largely consumed by T5.1–T5.3), then shipped as pure reorganization: the `forge/workflows/` package (one driver per module; no importer changed), `workflow_blocks.py` dissolved into `blocks/{transport,worktree}.py`, the exploration loop as `blocks/exploration.py` free functions, one frozen `RunSettings`; the replay histories passed unregenerated after every sub-task and the 2,367-line monolith surface reached zero. The same day, a **premise-level plan review** of the 19 remaining tasks plus a reverse pass over completed work ([../forge-plan-review-2026-07-27.md](../forge-plan-review-2026-07-27.md)) re-baselined the queue: ordering fixes (T6.7 before T7.1/T7.3/T7.4/T7.5), one blocked task (T6.1 vs Architecture Principle 8, owner adjudication pending), and unowned operational debt surfaced (nightly-backup verification is the blocking-soon item). **T5.5 (harness rebuild + replay tests) landed 2026-07-27 (`66b5ec2`).** The 6,179-line `tests/test_workflows.py` is gone: eleven per-mode files under `tests/workflows/` run over `tests/support/workflow_harness.py`, where each test owns a `ScenarioState` and one canonical `build_activities` supplies all 26 named activity mocks as closures over it. Scenario conservation was 100 → 100 class for class plus a new crash-recovery scenario; module-level mutable scenario bindings went 55 → 0, and scripting is keyed by identity rather than call-arrival order — which fixed the known ~1/8 recursive fan-out flake at its mechanism (20/20 recorded) and removed two further latent arrival-order defects. Replay histories went 6 → 13 (all four hard ACs plus all three stretch items, as new files), with the pre-existing six byte-identical and passing unregenerated throughout — the proof the rebuild touched no workflow code; the change-set is test-side only. `pytest-xdist` landed as a dev dependency with the parallel run demonstrated (14 workers, 101 tests). **T5.6 (plan preflight gate) landed 2026-07-27 (`36e1609`)** — planner output is now checked for structural defects before any step runs: the nine eval checks moved into the pure `forge/plan_checks.py` as recursive *finders* (nested sub-task violations are caught for the first time; `eval/deterministic.py` keeps its public API and wording as their presentation), and the five structural finders run as a live gate at the single `blocks/dispatch.py::dispatch_planner` seam, covering both lanes and emitting zero commands on a clean plan. A rejected plan is re-planned with its specific violations appended to the planner's prompt — three attempts total, no backoff, `effort="max"` on the last — then halts cleanly with `failure_kind="plan_preflight"`. The REVISE splice now catches instead of counting (an over-cap splice raised a pydantic error inside workflow code, which Temporal retries forever — a hung run), enforcing `MAX_PLAN_REVISIONS=5` and re-running the gate (`failure_kind="plan_revision"`), and `LLMResponse` rejects the do-nothing response so it is re-sampled rather than parsing into a silent no-op success. Four eval checks are deliberately not gates: two need the repo file set, so cyclic/forward references stay eval-only — the recorded **Partial**, adopted as T5.7. Replay: 12 of 13 histories unregenerated, `conflict_resolution_sync` deliberately regenerated because its recorded planner payload is a plan the gate now rejects. **The T5.6 follow-up landed the same day (`887bbdc`, owner-ruled)**: a preflight halt now reports every attempt rather than only its last (it had understated its own cost by two thirds), with the spend boundary recorded — a halt counts every attempt because none survived, while a run that recovers on retry keeps ordinary D97 surviving-call semantics; replay histories went 13 → 16, pinning the retry arm and both plan-halt terminals for the first time while the existing 13 stayed byte-identical; and batch-lane preflight is now observed rather than assumed. **DEPLOYMENT.md gained a pre-deploy in-flight workflow check (2026-07-28, `47dd284`)** — a worker restart replays every running workflow's history on the new code, so a deploy is safe for in-flight runs only when the new code accepts what those histories recorded; the check is deliberately manual and applies to any deploy touching workflow code, a model validator, or a `forge/presets.py` value. **Production was deployed to `d6af5be` on 2026-07-28** (the first use of that procedure, both namespaces drained beforehand; all four pollers verified `prod-<role>@d6af5be`, no `-dirty`), so T5.6 and its follow-up are live. **A records-only change-set landed 2026-07-29 (`bd7b4d1` + `cfe6bc1`)** — CLAUDE.md's Landed narrative became a phase ledger (3,552 → 735 words, per-task depth delegated to CHANGELOG rows, task files, and handoffs), the handoff-sweep skill's Step 3(b) was amended in the same change-set so the ledger form maintains itself, the markdownlint invocation was made cwd-proof in the sweep skill and PROCESS.md (`apps/pbook/.markdownlint.yaml` deleted, since config discovery stops at the invoking directory), and T8.2 gained a dated note that the restructure consumed part of its premise; no source, test, or deploy file changed, so no gates were owed and production is unchanged. **Next: T10.1 — migrate forge onto sax-datastores** (opened 2026-07-28 as `d6af5be`, IN PROGRESS — **forge was rebuilt onto the shared stacks 2026-07-31**: both lanes live, production on Temporal `:7243`/`forge-prod` with its store on `:5442`, schemas created fresh by the Alembic chains at worker startup; pbook's return and the WORKERS.md/DEBUGGING.md rewrites remain. Detail: forge's data layer moves onto the external fleet consolidation — environment = instance, project = role + database — retiring the single-instance topology, the superuser app role, and forge's own unmonitored backup leg; owner-sequenced ahead of the migration queue, and it closes the plan review's A7). **T10.1 advanced 2026-07-30:** `6173166` recorded two owner rulings — the legacy production data is junk, so the cutover creates fresh schemas through the three migration chains instead of migrating data, and pbook becomes a first-class datastore product — and committed the two `datastore-registration.yaml` records the registration flow takes as its request; `a7a1f0b` landed ST3's Temporal half, deleting `require_namespace_coherence` in favor of `resolve_temporal_target`, which derives a frozen `TemporalTarget(address, namespace)` from `FORGE_ENV` and the product slug, so the incoherent address/namespace pairings the old guard rejected are no longer expressible (`FORGE_TEMPORAL_NAMESPACE` is deleted outright; `FORGE_TEMPORAL_ADDRESS` survives only as an override that dev and prod refuse unless it restates their own server). Zero workflow code, and the 16 committed replay histories passed unregenerated. **Both lanes are now cut over and every legacy container is gone** — the `:7233` Temporal server and the `:5434` Postgres no longer exist, their pre-release data discarded by owner ruling. **D104 landed 2026-07-31 (`104c14b`) and production was deployed to it the same day** (three pollers `prod-<role>@104c14b`; the pbook worker's launchd label is disabled): forge owns no infrastructure — `deploy/local-stack/`, the `forge-stack` boot agent, and forge's own nightly backup leg are deleted, the shared stacks boot themselves, and the operator's nightly dump plus its scheduled restore check cover every database. **The schema-change request process was agreed with the sax-datastores operator 2026-08-02** (`a6d7768` + `15f184b`; canonical doc `sax-datastores/docs/schema-changes.md`): forge authors Alembic revisions and ships them as committed offline-SQL request artifacts, the administrator applies them, product code performs no DDL, and worker startup becomes a verify-only version check. The remaining work is the ten-item list in T10.1's 2026-08-02 Development Note. **T5.7 — execution-time reference repair follows the cutover.** Live queue: [../development-plans/TASKS.md](../development-plans/TASKS.md).
- **Work shipped outside the phase roadmap:** OCR pipeline (sync + batch — since extracted to the `ocr` app, now the `apps/ocr` workspace member), OCR separation (platform/consumer split via the shared `forge-contracts` package, merged 2026-06-05; the package was later absorbed into `sax_platform.contracts` and retired at T3.4), store externalization (Postgres + S3 + survivable writes), transcript ingestion (`forge ingest` → pbook), planner evaluation framework, and secure remote access (mTLS + EC2 deploy; that infrastructure was retired by D99 in favor of the local-first deployment — client-side TLS code remains, dormant).
- **The orchestrator is code-first in practice.** Despite the "task-agnostic" framing, context discovery is Python-specific (see tech debt below).

## Implemented capabilities

Module paths are under `src/forge/` unless a workspace member (`sax_platform.*`) is named explicitly.

- **Core loop** — universal workflow step, transitions, git worktree isolation: `workflows/task.py` (`ForgeTaskWorkflow`), `blocks/step.py`, `step_logic.py`, `git.py`.
- **Planning & fan-out** — single-pass planner producing ordered `PlanStep`s with optional parallel `sub_tasks`; LLM conflict resolution: `activities/planner.py`, `workflows/subtask.py` (`ForgeSubTaskWorkflow`), `blocks/gather.py`, `activities/conflict_resolution.py`.
- **Context** — import-graph + PageRank + token-budget assembly, plus LLM-guided exploration: `code_intel/`, `activities/context.py`, `activities/exploration.py`.
- **Output & validation** — diff-based edits with 4-level fuzzy fallback; ruff + optional tests with error-aware retries: `activities/output.py`, `activities/validate.py`.
- **Model routing & thinking** — capability tiers and the `ThinkingPolicy` (adaptive effort; the old `budget_tokens` knob is gone) single-sourced in `sax_platform.llm.tiers` and re-exported by `forge.models`; extended thinking for planning; prompt caching. The runtime call runs through `sax_platform.llm.AnthropicLLM` structured outputs (D90/T3.5), not forced tool use: `models.py`, `activities/llm.py`, `activities/planner.py`.
- **Batch** — per-workflow timer-loop transport (D88, T4.1): each workflow submits, then polls `batch_status` on a sleep loop until the batch ends and fetches its own result (no shared poller, no signal). Forge's batch transport is **anthropic-only** (T4.2; `blocks/transport.py`, `activities/batch_*`) — every forge batch is an Anthropic batch; OCR submits its own Mistral batches inside the `apps/ocr` app (`workflow_submit.py`, `workflow_store.py`) and detects completion via a stateless status-hint broadcast tracker (T4.4/D101).
- **Knowledge** — extraction activities → playbooks (manual add with LLM review; the scheduled re-extraction loop was removed in T1.8), transcript ingestion to pbook: `activities/extraction.py`, `manual_playbook_workflow.py`, `ingestion_workflow.py`.
- **Observability** — SQLite/Postgres store, Alembic migrations, CLI inspection: `store.py`, `alembic/`, `cli.py` (`forge status`).
- **OCR consumer (cross-queue)** — OCR lives in the `apps/ocr` workspace member and owns its full Mistral batch lifecycle (submit, fetch/store, and completion detection via a stateless status-hint broadcast tracker — T4.4/D101) through `sax_platform.ocr.MistralOcr`; it shares none of forge's transport (T4.2 deleted the batch SPI). The only cross-queue link is the shared batch ledger: ocr writes `batch_jobs` rows via `persist_block` activity calls on `forge-task-queue` and reads that ledger read-only, both from `sax_platform.contracts` (the former `forge-contracts` package, absorbed at T3.4); neither package imports the other.
- **LLM client** — forge completes structured outputs through `sax_platform.llm.AnthropicLLM` (`complete[T]` on the sync lane, the batch builder + fetch/parse on the batch lane); the former `sax-llm` provider protocol + registry are gone (package deleted at T3.5, forced tool use retired per D90). Anthropic is the only chat backend; Mistral survives solely as the OCR capability (`sax_platform.ocr.MistralOcr`).

## Requirements: complete vs. remaining

The behavioral spec lives in [requirements/](requirements/) (18 Gherkin feature files, frozen this pass). Sixteen feature areas are implemented; two are specified but not built.

**Remaining (specified, not implemented):**

| Feature spec | State | Evidence |
| --- | --- | --- |
| `requirements/ocr_web_api.feature` | **Not built** — no OpenAPI/paginated OCR web service; OCR now lives in the `apps/ocr` workspace member, so this API belongs there if built | No web framework (FastAPI/Flask/Starlette/uvicorn) exists anywhere in the repo |
| `requirements/human_in_the_loop.feature` | **Not built** — no structured pause/resume/approval primitive | No `HumanInputRequest`/`emit_user_prompt`; forge runs zero `@workflow.signal`, and ocr's only signal is the T4.4 status-hint tracker broadcast — neither is a pause/resume/approval channel; human gating is out-of-band git review + manual playbook approval |

## Remaining work (beyond requirements)

- **Phase 13 — tree-sitter multi-language** (deferred): [PHASES.md](PHASES.md) → [planning/PHASE13.md](planning/PHASE13.md).
- **LSP-based context generation** (deferred, D38): [planning/LSP_INTEGRATION_PLAN.md](planning/LSP_INTEGRATION_PLAN.md).
- **Multi-transform DAG planner** — a richer planner (classify → clarify → DAG → adversarial judges) is a **draft design, not implemented**; it would replace the single-pass `activities/planner.py`: [planning/task-management/DECOMPOSITION.md](planning/task-management/DECOMPOSITION.md). The structured human-in-the-loop requirement above is part of this design.

## Known issues & technical debt

Mined from the four code reviews in `archive/merged/code-review/` (≈2026-02-16) and re-triaged against current code on 2026-06-04. Items already resolved by later phases were dropped; what remains is live. None of the reviews' themes are cleanly closed.

**Open** (no mechanism in code):

1. **Execution is unsandboxed.** Providers run `pytest`/`git`/`ruff` and `validate.py` runs arbitrary test commands via raw `subprocess.run` — no allowlist, sandbox profile, or network-egress policy. Only path-traversal guards exist (`activities/output.py::resolve_file_paths`).
2. **No run-level cost/latency budget.** The only budget is context-assembly token packing (`code_intel/budget.py`); there is no per-run dollar/token/wall-clock cap, kill-switch, or admission control for fan-out/exploration/retry cost.
3. **Fuzzy edits aren't semantically verified.** `activities/output.py::_fuzzy_match` applies edits at 0.6 similarity (D56) with no post-edit AST/parse check, and fuzzy matching can't be disabled per task/policy.
4. **Model routing is static.** `models.py::resolve_model` is a fixed tier→model map; no calibration, canary, or quality/cost-driven fallback.
5. **Stored prompts/outputs have no privacy controls.** `store.py::Interaction` persists prompts and results as plaintext with no retention/TTL, redaction, or encryption.

**Partial** (mechanism exists but stops short):

| Theme | Residual gap | Pointer |
| --- | --- | --- |
| Transition vocabulary | Policy-violation / budget-exhausted / partial-success / blocked-on-human collapse into retryable or terminal | `models.py::TransitionSignal` (3 members) |
| Plan contract | File-level targets only — no symbol-level write-sets, budgets, or confidence. Structural checks (duplicate ids, overlapping fan-out targets, unsafe paths, output-less leaves) gate at plan acceptance since T5.6; cycle/forward-reference and context-file plausibility remain eval-only because they need the repo file set, which the workflow cannot read without adding an activity | `models.py::PlanStep`, `plan_checks.py::PREFLIGHT_CHECKS`, `eval/deterministic.py` |
| Conflict resolution | File-granularity merge accepted on path-completeness alone; no intent/regression verification | `activities/conflict_resolution.py` |
| Retry strategy | Errors are fed back, but one undifferentiated reset+retry; no failure-class classifier | `activities/context.py::build_error_feedback` |
| Exploration cost | Bounded only by round count + char truncation; no quota/ROI/dedup | `blocks/exploration.py::run_exploration_loop` |
| Halt-when-confused | Terminal halts only: the opt-in sanity-check ABORT and, since T5.6, the always-on plan halts (`plan_preflight` after three planner attempts, `plan_revision` on an over-cap or still-invalid REVISE); no escalation queue, paging, or resume | `activities/sanity_check.py`, `plan_checks.py`, `models.py` (`sanity_check_interval=0`) |
| Validation depth | ruff + optional tests only; no SAST/secrets/contract/perf/behavior-diff gates | `activities/validate.py` |
| Eval as release gate | Compares plan quality (baseline vs candidate) but isn't a CI gate; no end-to-end/adversarial coverage | `eval/runner.py` |
| Domain-agnosticism | Prompts/validation parameterized per domain, but context discovery is Python/import-graph-specific; non-code domains have no positive validators | `domains.py::DomainConfig`, `code_intel/` |
| Human-in-the-loop | Only out-of-band merge gating + manual playbook approval; no structured intervention primitive — forge runs no signal, and ocr's sole signal is the T4.4 status-hint tracker broadcast, not a human channel | `manual_playbook_workflow.py` |
| Multi-provider parity | No provider abstraction remains — forge calls `sax_platform.llm.AnthropicLLM` directly (the `sax-llm` protocol + registry were deleted at T3.5). Anthropic is the only chat backend; Mistral is OCR-only (`sax_platform.ocr.MistralOcr`). A second chat provider would mean reintroducing an abstraction, and there is no cross-provider conformance suite | `sax_platform/llm/` |

**Tooling & ops debt** (surfaced 2026-07-16 during the monorepo/deployment work; Phase 2 closed the CI/gates/findings rows — GitHub Actions CI, import-linter DAG contracts, all six workspace packages at the time gated at 85% coverage, mypy strict across the workspace (T2.3a–d), the four standing findings fixed; T3.4 subsequently retired `forge-contracts` and T3.5 deleted `sax-llm`, leaving four packages):

| Item | Detail | Pointer |
| --- | --- | --- |
| `batch-status` skill is dead | `.claude/skills/batch-status/batch-status.sh` still resolves a SQLite store (`FORGE_DB_PATH` → `$XDG_STATE_HOME/forge/forge.db`) — broken since the Postgres externalization. Needs a rework against `FORGE_DB_URL` or retirement (its `.agents/` Codex copy too) | `.claude/skills/batch-status/` |
| pbook skill's eval harness is dead | The `skill-pbook` repo's Makefile seeds a SQLite test DB (`sqlite3 .backup`, `PBOOK_DB_PATH`); pbook has been Postgres+pgvector-only since its cutover. Trigger evals unaffected | `~/repos-sax/skill-pbook` |
| Desktop is the availability story | Accepted by D99: there is one host, and batch polling (D88 timer loops) plus the ocr status tracker (T4.4 Schedule) stall whenever it is off or asleep. System sleep is disabled on AC power (`pmset -c sleep 0`, applied 2026-07-16), so the residual exposure is reboots, unplugging, and hardware — not idle sleep | [operations/DEPLOYMENT.md](operations/DEPLOYMENT.md) |
