# Changelog

One row per completed task, appended on completion (newest first) per [PROCESS.md](PROCESS.md).
Work completed before this file was introduced is recorded in `git log` — including the Phase 1–14 build, store externalization (Postgres + S3), the OCR pipeline, transcript ingestion, the planner eval framework, and mTLS remote access.

| Date | Task | PR |
| ------ | ------ | ---- |
| 2026-07-27 | [T5.5 — Harness rebuild + replay tests: the workflow-test harness is per-test state, not module globals. `tests/test_workflows.py` (6,179 lines / 40 classes / 100 tests) is deleted; in its place `tests/support/workflow_harness.py` (one `ScenarioState` per test plus one canonical `build_activities` producing all 26 named `@activity.defn` mocks as closures over it), `tests/support/block_mocks.py` (the four T5.2/T5.3 factories, moved verbatim — they already used the target pattern and their tests assert exact call sequences, so folding them into the canonical set would have changed what they prove), and eleven per-mode files under `tests/workflows/`. **Scenario conservation 100 → 100, class for class**, plus the new crash-recovery scenario (suite 1,482 → 1,483); no test existed in two places at any point. Counts: 55 module-level mutable scenario bindings → 0 (only two `__all__` lists remain, AST-verified), 19 `global` → 0, 12 `_reset_*` → 0, 184 `@activity.defn` → 86 with each of the 26 names defined once. **The ~1/8 flake is fixed at its mechanism** (`TestRecursiveFanOutNestedFailure::test_failure_propagates`): scripting was one shared list drained in call-arrival order, so with two grandchildren in parallel the terminal token meant for `gc2` was sometimes drawn by `gc1`; outcomes are now keyed by compound sub-task id (D3), proven by 20/20 repeated runs (`test_fan_out.py` likewise 20/20, same defect class). Two further latent instances removed: the recursive `% 3` and mixed-plan 1..4 output counters. Scripts handed in are read-only (`MappingProxyType` tables; consumption uses an internal cursor — no `pop(`, no `.clear()`, no handler pointers). Batch-lane identity is recovered from `request_id` (submit records it, fetch names it in the body, parse looks it up), because `ParseResponseInput` carries only a `task_id` and cannot tell two children apart. Discovery: **every legacy scenario ran on the batch lane** (`sync_mode` defaults False; the sync `call_llm` mocks were dead code and the `call_llm` log entries came from the parse handler), so no test's lane was changed and three assertion families were renamed to the batch-lane entry they actually observed — the only assertion-text change in the migration. Also fixed: a `TestBatchPlanned` comment claiming a "distinctive" planner thinking policy proved passthrough, when the value is `ThinkingPolicy()`'s default and `_plan_task` overwrites the assemble activity's policy anyway (assertion kept, comment corrected). **Replay histories 6 → 13**: all four hard ACs (`planned_step_sync`, `nested_fan_out_sync` + its depth-1 child, `exploration_sync`, `sanity_check_sync`) and all three stretch items (`conflict_resolution_sync`, the sync-lane planner inside `planned_step_sync`, `worktree_reset_retry_sync`) as new files; the pre-existing six are byte-identical (`git diff` empty) and passed unregenerated throughout. `regenerate.py` gained argv scenario selection so new histories never re-emit committed ones. **Crash recovery** (`test_crash_recovery.py`): a worker is shut down with `assemble_context` in flight, a second worker finishes the same run — SUCCESS, the stranded activity re-executed once, the worktree not re-created, and the run asserted `RUNNING` at the kill so it cannot pass vacuously; both workers set `max_cached_workflows=0` because a dead worker's sticky queue drains only after a schedule-to-start timeout the time-skipping server does not fast-forward. `pytest-xdist>=3.6` added to the root dev group and **demonstrated**: `14 workers [101 items] … 101 passed in 3.45s`. Test-side only — zero changes under `src/`, `libs/`, `apps/`. `make gates` EXIT=0 (forge 1,483 / pbook 376 / ocr 238 / sax-platform 517; forge cov 91.24%; mypy ×4; import-linter 6 kept)](tasks/T5.5-harness-rebuild-replay-tests.md) | — |
| 2026-07-27 | [T5.4 — Split the monolith (pure reorganization, zero behavior change): `workflows.py` became the `forge/workflows/` package — `task.py` (`ForgeTaskWorkflow`) and `subtask.py` (`ForgeSubTaskWorkflow`), one driver per module, `__init__` exporting the two classes so every importer (worker, CLI, replay harness, four test modules) stood unchanged; `workflow_blocks.py` dissolved into `blocks/transport.py` (the D88 submit/poll/fetch/parse choke point + `BATCH_WAIT_FAILURES`) and `blocks/worktree.py` (removal + post-failure cleanup), with the `persist_block` pass-through re-export deleted in favor of `sax_platform.contracts.persist` (the T5.3 `FORGE_TASK_QUEUE` precedent — update importers, no shims); the exploration loop left the task class for `blocks/exploration.py::run_exploration_loop(host, ...)` plus a pure `format_exploration_context`; and the loose ambient trio behind `DispatchHostBase.configure` became one frozen `RunSettings` (with a `RunInput` Protocol + `from_input` so the seconds→timedelta conversion is written once), full parameter-threading still rejected because `_persist_occurrences` is genuinely mutable run state. Re-baselined 2026-07-27 before implementation: the 2026-06-10 premise (1,861-line monolith, pipeline ×3, gather ×2, ~1,100 LOC duplication) was largely consumed by T5.1–T5.3, so the duplication AC closed on measurement — the monolith surface (`workflows.py` + `workflow_blocks.py`) went 2,367 → 0 lines across T5.1–T5.4, the two drivers now hold 816 code lines between them, and the only text they still share is 24 lines of spec keyword-argument plumbing. `THINKING_MAX_TOKENS` deliberately stayed in `presets.py` (T5.3 ST8's home) rather than moving to transport as the plan's text said. Zero `@workflow.signal`/`query`/`update` in forge source, verified at close. **Replay histories passed UNREGENERATED after every sub-task and at close (7/7)** — the exact proof for a reorganization, since Temporal registers workflows by class name; no test assertion changed (three stale comments and one docstring were the entire test diff). ARCHITECTURE.md's module map + wait-loop pointer and OVERVIEW.md's component pointers trued to the new layout; PHASES.md's historical per-phase column deliberately left. `make gates` EXIT=0 (forge 1475 / pbook 376 / ocr 238 / sax-platform 517; forge cov 91.24%; mypy ×4; import-linter 6 kept)](tasks/T5.4-split-the-monolith.md) | — |
| 2026-07-26 | [T5.3 — Single gather + dispatch: the typed LLM-dispatch layer and the fan-out gather each exist once. `blocks/dispatch.py`'s `typed_dispatch` + the pure five-row `ARMS` table (role / sync activity / its timeout / batch output type / max_tokens) replaces five copies of the lane fork — planner and sanity inline in `workflows.py`, generation and conflict resolution as hand-rolled twins in `workflow_blocks.py`, exploration inline and divergent — with one `call_stats` mapping the eight stats fields and the interaction persist happening *inside*, uniformly; the T4.1 transport stays in `workflow_blocks.py` and is imported, not moved. `blocks/gather.py`'s `run_fan_out_gather(spec, host)` + the two-row `GATHER_POLICIES` table (borrowed worktree + commit / owned worktree + never commits, D16) replaces the parent and nested gathers, with child-input construction in one pure `build_child_input` so the T1.5 propagation (`resolve_conflicts`/`thinking`/`model_routing`) cannot drift again, and a neutral `GatherSuccess \| GatherFailure` the callers stamp through the T5.1 builders. **Per-child failure isolation** (the headline AC): a child that raises becomes a failed `SubTaskResult` (`failure_kind="child_crashed"`, the tenth value) while its siblings run to completion and the parent still returns a `TaskResult` and writes its run record — before, one crashed child propagated a `ChildWorkflowError` out of `run()` and TERMINATE killed the in-flight siblings, orphaning their paid batches; `ParentClosePolicy.TERMINATE` is now explicit (owner-approved, same proto value the SDK defaulted to, ABANDON rejected). Exploration stopped being the arm that persisted nothing: new `ExplorationCallResult` envelope (spend + the prompts the activity assembles internally, which used to die in a trace span), and a latent crash in `build_persist_interaction` (assumed every `response` had `explanation`) fixed on the way. `DispatchHostBase` (plain non-`@workflow.defn` base, sandbox+Replayer verified by spike before adoption) holds the lane/cadence/persist-counter state both workflow classes duplicated verbatim. Nested gather's worktree leak closed (created with no exception wrap; six hand-repeated removals collapse into one exit path). `forge/presets.py` is the one home for every activity timeout/retry/token preset (four homes before), each value diffed against its pre-move value — zero drift, because these are `ScheduleActivityTask` command attributes; the two same-bodied worktree-cleanup helpers folded into `cleanup_worktree_after_exception`. `workflows.py` 1,632 → 917 lines. **Replay histories pass UNREGENERATED (7/7) at every step** — the behavior-preservation proof for the pinned batch-planner and both generation lanes. Known pre-existing flake left for T5.5 (verified reproducible at `83a2e07`): `TestRecursiveFanOutNestedFailure::test_failure_propagates` races on module-global scenario state across two parallel children. `make gates` EXIT=0 (forge 1475 / pbook 376 / ocr 238 / sax-platform 517; forge cov 91.17%; mypy ×4; import-linter 6 kept)](tasks/T5.3-single-gather-dispatch.md) | — |
| 2026-07-25 | `deploy/prod-ocr` (owner-directed) + handoff sweep + README command audit: one-command production ocr access — embeds the D102 ceremony (ack + profile via the parse-don't-eval `load-env.sh` loader, sanctioned by the plist precedent: the ceremony prevents accidental use, and invoking a script named `prod-ocr` is the explicit act) and execs `uv run --package ocr ocr` **from the pinned prod checkout** so client and workers run the same commit; stderr banner (namespace, checkout sha, command); exit 78 when `forge-prod` is missing — never falls back to the live tree; zsh (interactive operator tooling, outside the launchd bash family); smoke-verified live against production (`tracker-status` fresh, banner correct, refusal path pinned). Same day: `HANDOFF-2026-07-25-t5.3-start.md` + CLAUDE.md/OVERVIEW.md status-of-record truth pass, and the README audited against the actual CLI — 11 discrepancies fixed, notably the env-guard prerequisite on every example (bare invocations exit 78 since D102), the false `--test-command`-implies-`--run-tests` claim, `forge start`'s incomplete workflow list, and `eval-planner`'s nonexistent corpus path | — |
| 2026-07-25 | Pinned prod checkout + clean-code guard (owner-directed; **D103**, amends D99): production runs a detached git worktree at `~/repos-sax/forge-prod` — `deploy/prod-deploy.sh <ref>` (`make prod-deploy REF=`) is the only sanctioned deploy path (ref→commit via `rev-parse <ref>^{commit}`; worktree created/pinned detached; a prod tree with local modifications is never clobbered; gitignored stack `.env` copied; `uv sync --all-packages`; restart gated on `ProgramArguments[0]` of the installed plist pointing under `$PROD_ROOT`, else the one-time `forge-prod` installer command is printed and exit 3 — restarting against wrong-tree plists would silently deploy the wrong checkout); `require_clean_prod_code(env)` (pure `clean_prod_violation` + shell over `code_version()`, in `sax_platform.temporal.identity`) runs in all three workers' composition roots right after `FORGE_ENV` resolution — prod + dirty-or-unknown version → stderr mechanism message + exit 78 (D102 guard family); dev/test exempt (editing under a dev worker is the staging lane's point). Motivation recorded in D103: the realized 2026-07-25 incident (installer on a dirty tree shipped uncommitted code to prod; caught only by the `-dirty` identity stamp) and the podman-machine deployment direction (checkout-at-commit + sync ≈ image build minus the image). Stack agent moves with the checkout — data verified XDG-external (223M live Postgres under `~/.local/share/forge/`). Docs truth pass incl. CLAUDE.md Running-the-System. First-time adoption is post-commit operator action: `make prod-deploy REF=main` then the printed `forge-prod` installer run. `make gates` EXIT=0 (forge 1433 / pbook 376 / ocr 238 / sax-platform 517; mypy ×4; import-linter 6 kept) | — |
| 2026-07-25 | Lane-based worker identity bases (owner-directed; supersedes the previous row's `desktop-` example format): the vestigial `desktop-` base (D99 EC2-retirement label — stopped distinguishing anything once the desktop became the only host) is replaced by the lane, uniformly across all six worker roles — prod launchd `prod-forge-worker-1/2` / `prod-ocr-worker` / `prod-pbook-worker` (install.sh derives `prod-<agent>` for every agent; ocr and pbook launchd workers get identity bases for the first time), dev tmux `dev-<app>-worker` (`make dev-worker` exports the base after the profile source, so a stale dev.env value can never contradict the lane) — each version-stamped to `<base>@<sha>[-dirty]`, so every poller identity reads lane + role + version and agrees with FORGE_ENV, the namespace split, and `make workers-status`. ocr and pbook CLIs gain forge's `--worker-identity` option (`envvar=FORGE_WORKER_IDENTITY`, one worker per process); `run-worker.sh` exports the identity for any worker. Documented plist mechanics (WORKERS.md, launchd README): `make workers-restart` relaunches from the *loaded* job definition — new ProgramArguments land only via the installer's bootout/bootstrap, run with the same opt-in flags as the original install; bootout cuts the 5-min graceful drain, so prefer a quiet queue. `make gates` EXIT=0 (forge 1432 / pbook 375 / ocr 238 / sax-platform 504; mypy ×4; import-linter 6 kept) | — |
| 2026-07-25 | Worker version identity stamp (owner-directed): every worker's Temporal identity now carries the launch-time git version — new `sax_platform.temporal.identity` (pure `compose_identity`; `code_version` = `git rev-parse --short HEAD` + `status --porcelain` → `-dirty`, 5s timeout, any failure → None so stamping can never block a worker start; version withheld when the commit is known but the dirty check failed — a bare SHA would assert a clean tree nobody verified), stamped in each worker's `run_worker` immediately before connect (`desktop-forge-worker-1@bb64d88[-dirty]`; unnamed workers `pid@host@sha`; pbook gained the identity kwarg it lacked — its connect previously passed none). `temporal task-queue describe --task-queue <q> [--namespace forge-dev]` is now the authoritative "which code is this worker running?" query, both lanes, documented in WORKERS.md ("Which code is a worker running?"). Exported lazily (PEP 562) — `subprocess`/`socket` are sandbox-restricted and workflow modules import `sax_platform.temporal.polling`, so an eager export would drag them into the workflow-sandbox import graph. Takes effect per worker at its next restart. `make gates` EXIT=0 (forge 1432 / pbook 373 / ocr 236 / sax-platform 503; mypy ×4; import-linter 6 kept) | — |
| 2026-07-25 | [T5.2 — Single step block: the five-move step pipeline exists once — `blocks/step.py`'s `run_step_attempts(spec, host)` with the pure three-row `MODE_POLICIES` table (assemble activity / worktree ownership fresh-keep·borrowed·fresh-dispose / commit policy task·step·never) replaces the three diverged copies in `workflows.py` (net −290 lines; the unnamed behavior divergences — single_step's terminal-failure autopsy commit, sub_task's dispose-even-on-success, conditional model stamping — preserved as table rows, `StepSpec.__post_init__` structurally rejecting a borrowed/fresh worktree-handle mismatch); exploration is a per-attempt hook inside the retry loop (ordering pinned by a full-call-sequence test across a retry); worktree-ownership cleanup wraps close the leak class — a mid-step exception removes the worktree **and** its `forge/<task_id>` branch in all three modes (block wraps the fresh modes; `_run_planned` wraps the borrowed worktree it creates; batch-wait shapes deliberately deferred to `run()`'s existing T1.6b handler, which also keeps the batch-ceiling replay history valid); git seams idempotent under activity retry — `create_worktree` prune → force-remove stale registration → `worktree add -B` over crashed leftovers (an *unregistered* leftover directory deliberately still raises; git.py never deletes untracked paths), `commit_changes` retry-after-landed returns HEAD's SHA on message match, `remove_worktree` no-op when already gone — with raise-pinning real-git tests for every surviving error; `_call_generation` → `call_generation` (the `StepHost` Protocol seam). **Replay histories pass UNREGENERATED (7/7)** — the behavior-preservation proof; planned-step's sequence is pinned by workflow tests alone (no committed history exercises `assemble_step_context`). Presets duplicated workflows.py↔block deliberately until T5.3/T5.4. `make gates` EXIT=0 (forge 1432 / pbook 373 / ocr 236 / sax-platform 482; forge cov 90.62%; mypy ×4; import-linter 6 kept)](tasks/T5.2-single-step-block.md) | — |
| 2026-07-24 | [T5.1 — Pure step logic (D95 implemented; Phase 5 opened): the `evaluate_transition` activity is deleted and `determine_transition` inlined at all five workflow call sites via the new zero-`temporalio` `forge/step_logic.py` — also holding `failure_summary`/`subtask_failure_summary` (5+2 duplicated joins dissolved), ~15 terminal-result builders stamping the new nine-value `failure_kind` Literal, `merge_resolution → MergedFiles \| MissingResolutions` (two inline copies), the slim builders (contents travel once in `TaskResult.output_files`; embedded Step/SubTaskResults carry sha256 `output_digests`; nested nodes slim children before returning; `conflict_resolution.resolved_files` emptied post-merge — payload test pins a 24×25KB fan-out at ~0.6MB serialized with each content string appearing exactly once), the `LLMRunTotals` aggregator on TaskResult (D97's spend visibility; surviving calls only — the interactions table stays authoritative), and the relocated child-ID/timeout helpers; `TransitionInput` + `_TRANSITION_TIMEOUT` deleted; 12 by-name transition mocks removed — workflow tests now drive the signal through `validate_output` pass/fail (scripted sequences translated, sticky terminal token); 6 replay histories regenerated (deliberate workflow-logic change; Replayer 7/7); CLAUDE.md + ARCHITECTURE.md universal-step truth pass (D3's "Amended by D95" banner pre-existing). Pre-implementation scope reconciliation recorded in the task file (persist-key bullet found already landed via T1.6a; `derive_execution_timeout` deliberately left in `models.py`). Before the next prod worker restart: confirm no in-flight forge workflows (replay determinism changed). `make gates` EXIT=0 (forge 1405 / pbook 373 / ocr 236 / sax-platform 482; forge cov 89.86%; mypy ×4; import-linter 6 kept)](tasks/T5.1-pure-step-logic.md) | — |
| 2026-07-23 | Staging-lane hardening + Principle 8 (owner-directed): `--env` made position-independent across all three CLIs (per-command `_EnvCommand` seam; guard moved from group callback to command invoke; deliberate contract change — `--help` works env-less at every level; `--env prod` still cannot bypass the ack, both positions test-pinned); `make dev-worker` made crash-safe (remain-on-exit dead panes, `tee` to persistent logs, running/CRASHED/died-at-startup status — a crashed dev worker had vaporized its session and scrollback undiagnosed); WORKERS.md records that a functional dev lane needs the ocr **and** forge workers (cross-queue `persist_block` — an OCR submission blocked 26 min on the missing pair, healed within one tracker cycle of starting it; the full pipeline then completed a 43-page document in `forge-dev`); **Architecture Principle 8** recorded (application access is through Temporal; infra exceptions named; bounded first-step CLI waits adopted, large-result S3 claim-check parked) + six parked follow-ups in TASKS.md. forge 1368 / pbook 373 / ocr 236 / sax-platform 482; mypy ×4 | — |
| 2026-07-22 | Staging lane (owner-directed T0.9 follow-ups): per-env Temporal namespace isolation + `--env` profile flag + `ocr migrate` — `TemporalSettings.namespace` (`FORGE_TEMPORAL_NAMESPACE`, default `default`) threaded through all six entrypoints and every CLI connect site (pbook's four hand-rolled `Client.connect` consolidated onto `connect_temporal`); pure `require_namespace_coherence` enforced before every connect (prod ⇒ `default`; dev/test ⇒ non-default — a namespace-less dev process exits 78 instead of polling production's queues; verified live) with the `forge-dev` namespace registered (72h retention); `--env <name-or-path>` on each CLI's top-level group (**before** the subcommand) via pure `parse_env_profile` (export-prefix tolerated, one quote pair stripped, braced-`${VAR}`-only expansion, no execution; never supplies the prod ack — `--env prod` without the exported ack still exits 78, test-pinned); `ocr migrate` runs the ocr Alembic chain standalone (credential-free output; parity with `pbook migrate`); `forge_dev` ocr chain migrated; conftests pin `FORGE_ENV=test` + `forge-test`. forge 1365 / pbook 370 / ocr 233 / sax-platform 482; mypy ×4; lint-imports 6 kept | — |
| 2026-07-22 | [T0.9 — Application store rehomed to local Postgres + explicit-environment guard (D102, amends D99): app store of record moved from Supabase to the local `forge-postgres` podman instance as separate databases beside Temporal's — `forge` (forge + ocr chains) and `pbook` (pbook's schema, pgvector; `PBOOK_DATABASE_URL` set for the first time, ending the silent store-disabled state) — migrated by owner-executed cutover script (pg17 bridge for Supabase's PG 17.6; `extensions.vector`→`public.vector` + RLS-strip transforms; exact row-count verification across all seven tables; Supabase frozen as fallback); explicit-environment guard live at all six entrypoints (`FORGE_ENV` prod/dev/test with NO default, `envs/<env>.env` tagged profiles, prod requires tagged profile + `FORGE_PROD_ACK=yes` declared in the launchd plists, failures exit 78/EX_CONFIG; interactive prod use needs `set -a` sourcing — the guard rejected a plain `source` in live verification, by design); nightly `db-backup` launchd agent (03:30, both DBs → S3 `db-backups/`); initdb parity for fresh stacks; verified live — tracker `fresh` against local prod, bare shell exit 78; stale T4.1 `forge-batch-poller` Schedule deleted server-side; owner history correction recorded in D102 (the Supabase-access-uncertainty period → podman for disposable test DBs). Records: D102 + D99 banner, DEPLOYMENT.md rewrite, WORKERS.md guard section, CLAUDE.md, TOC.md; markdownlint clean](tasks/T0.9-local-db-rehoming-env-guard.md) | — |
| 2026-07-22 | `ocr tracker-status` — operator-requested health probe for the T4.4 tracker: Temporal-free direct-DB command printing `checked_at_gmt` first (always, even on error paths) then the `ocr_tracker_heartbeat` fields, live-job count (reuses `get_tracker_heartbeat` + `execute_list_live_ocr_jobs` — no duplicated queries), and a `fresh`/`stale`/`never-ran` verdict from a pure `derive_tracker_status` core; exit codes 0 fresh / 1 stale-or-never-ran / 2 stale-with-live-jobs (the API-failure signature) / 3 probe-error (missing `FORGE_DB_URL` or unreachable DB — clean fail-fast with an actionable stderr hint, mirroring the worker's `MISTRAL_API_KEY` check; found live: bare shells don't carry `FORGE_DB_URL`, only run-worker.sh's sourced forge.env does); `--stale-after` default 300s; naive-vs-aware `last_run_at` normalized (sqlite/postgres). Documented in WORKERS.md. ocr 210 passed, 96.80% cov; mypy clean; ruff clean | — |
| 2026-07-22 | [T4.4 — ocr Mistral status tracker (D101, amends D100): per-waiter provider polling deleted from ocr — the stateless `OcrBatchTrackerWorkflow` (120s Schedule, overlap=SKIP + 5-min run execution timeout via the reintroduced `_ensure_schedule` guard, installed/reconciled at ocr worker startup) checks the DB for live jobs (idle cycle → heartbeat only, zero Mistral calls), sweeps the Mistral batch list endpoint once via the new paged status-only `MistralOcr.list_batch_statuses` (server-side `created_after`), and broadcasts `ocr_status_hint` signals to the ledger-recorded store children; `OcrStoreWorkflow` converted to a signal-wait state machine (pure `next_state` transition table; duplicate/forbidden hints absorbed; the hint never authoritative — fetch keyed by the child's own ids; unchanged 25h ceiling → MISSING, T1.6b symmetry); single-row `ocr_tracker_heartbeat` (ocr Alembic 002, in-SQL counter increment) makes a stale tracker a system alert; deliberately no per-task fallback (owner-adjudicated: tracker failure is latency-only). Deleted workspace-wide: `ocr_batch_status` + `OcrBatchStatusInput` + `_POLL_STATUS_TO_STATE`, platform `BackoffSchedule`; kept: `wait_batch_ended`/`FixedInterval` (forge's loop), `MistralOcr.get_batch_status` (single-batch diagnostic primitive); forge replay histories green unregenerated. Record: D101 + `Amended by D101` banner on D100, TEMPORAL_PATTERNS rule-8 amendment, signal-claims sweep (no signals in forge; ocr's tracker broadcast is the sanctioned hint pattern); markdownlint clean. `make gates` EXIT=0 (forge 1348 / pbook 353 / ocr 189 / sax-platform 431; mypy ×4; import-linter 6 kept)](tasks/T4.4-mistral-status-tracker.md) | — |
| 2026-07-20 | [T4.3 — Transport decisions sweep; **Phase 4 complete**: new D100 (Phase 4 transport as shipped — shared `sax_platform.temporal.polling` primitive with replay-proof extraction, ocr BackoffSchedule cadence superseding D88's 300–600s range, forge anthropic-only, the `poll_batch` split, the derived timeout tree + `MAX_PLAN_STEPS`, `batch_jobs` as-shipped with spend columns recorded as deferred); `Amended by D100` banner on D88, as-shipped note under D80, reaffirmation banners on D76/D79 (D77/D78/D81 banners verified already present from D88's writing); pbook TEMPORAL_PATTERNS rule 8 amended (shared primitive, ≥300s floor, ~11 events/poll history budget, continue-as-new escape). Docs-only; markdownlint clean ×2](tasks/T4.3-transport-decisions-sweep.md) | — |
| 2026-07-19 | [T4.2 — ocr owns its batches end to end; the cross-queue batch SPI is deleted: ocr submits via its own `submit_ocr_batch` (blob fetch + `MistralOcr.submit_batch`) and polls with the shared `sax_platform.temporal.polling` loop (new in this task: `wait_batch_ended`, `FixedInterval`/`BackoffSchedule` — 300s first hour, ×2 to 1800s cap, jittered; forge's `batch_submit_and_wait` cut over as the first consumer with the T4.1 replay histories passing unregenerated as the determinism proof); `OcrGatherWorkflow` deleted — `OcrSubmitWorkflow` awaits non-ABANDON store children (execution timeouts derived from the 25h ceiling) and reassembles inline, so a failed chunk fails the document promptly (AC test pins `failed`-not-`missing`); one `fetch_and_store_ocr_result` activity keeps result bytes out of workflow history; `PersistBatchOutcome` (moved to contracts) written cross-queue on every terminal path; contracts shed `BatchSubmitSpiInput`/`BatchResult`/`resolve_batch_result`/`BATCH_RESULT_SIGNAL` (`BatchSubmitResult` → `forge.models`); forge's transport is anthropic-only; six ocr activities gained typed inputs; `OcrProcessingStatus`/`OcrJobDerivedStatus` enums with a pure exhaustive `_derive_status`; **zero `@workflow.signal` platform-wide**. Operator-visible: `ocr submit` is start-only; ocr worker requires `MISTRAL_API_KEY`. Grounding recorded: one batch per chunk (1,000-doc burst ⇒ ≥1,000 waiters); Mistral publishes no tier numbers (admin console only; ≤100k requests/batch; batch processing exempt from real-time limits). `make gates` EXIT=0 (forge 1348 / pbook 353 / ocr 149 / sax-platform 439; mypy ×4; import-linter 6 kept)](tasks/T4.2-ocr-own-polling-gather-restructure.md) | — |
| 2026-07-19 | T4.1 follow-up — `MistralOcr.poll_batch` split into `get_batch_status` (one `jobs.get`, no download — Mistral status responses carry file IDs only, verified against the API) + `fetch_batch_results` (download once, after ENDED); `poll_batch` + the `BatchPollResult` envelope deleted workspace-wide (no-live-users seam fix, no compat shim); kills the one duplicate result download+parse per completed mistral batch and gives `MistralOcr` the same status/fetch shape as the anthropic lane (the primitives T4.2's ocr timer loop needs). sax-platform 404 / forge 1365 / ocr 118; mypy ×4; lint-imports 6 kept. Details in [T4.1 Dev Notes](tasks/T4.1-forge-timer-loop-transport.md) | — |
| 2026-07-19 | [T4.1 — Timer-loop batch transport (D88 implemented, reversal R1): every forge batch wait is per-workflow submit → `workflow.sleep`+`batch_status` loop → `fetch_batch_result` → `parse_llm_response` — request_id minted in the workflow via `workflow.uuid4()` (submit-retry orphan window closed; duplicate-retry test pins one paid batch identity), provider threaded explicitly (mistral-routes test), claim-check fetch (inline ≤256KB else S3 envelope), 25h ceiling + T1.6b symmetry preserved and extended to children; `batch_jobs` reduced to the forge-internal audit ledger (written at submit + terminal outcome; monotonic `update_batch_status` with error-preserve; statuses submitted → ended \| failed \| expired \| missing, processing legacy; mirror-drift sync test); timeout tree closed (mode-aware `_child_timeout`, pure `derive_execution_timeout`, `MAX_PLAN_STEPS=25`, fan-out child survives >20-min batch turnaround); replay scaffold (six committed histories under `tests/replay/` + Replayer test + `make replay-histories`); deleted BatchPollerWorkflow + Schedule, all forge signal handlers/correlation dicts, T1.2/T1.3 interims, forge `BatchResult` re-export, dead `BatchActivities` temporal-client dep; docs truth-pass (README/OVERVIEW/ARCHITECTURE/WORKERS/USAGE). Known gap until T4.2: ocr submits via the SPI but nothing polls its batches. `make gates` EXIT=0 (forge 1365 / pbook 353 / ocr 118 / sax-platform 398; mypy ×4; import-linter 6 kept)](tasks/T4.1-forge-timer-loop-transport.md) | — |
| 2026-07-19 | Phase 0 complete — T0.1 [Observability defaults](tasks/T0.1-observability-defaults.md) (root logger INFO / `forge` DEBUG closes the third-party-DEBUG prompt-leak channel; tracing exporter defaults `none`, console opt-in; `FORGE_OTEL_ENDPOINT` verified already gone), T0.2 [CLI helper fixes](tasks/T0.2-cli-helper-fixes.md) (one overloaded `_submit(ForgeTaskInput, *, wait)` replaces the duplicated 14-param helpers and dissolves the sync-mode default inversion; eval cases discovered once), T0.3 [Dead code](tasks/T0.3-dead-code-stale-artifacts.md) (`build_interaction_dict`, `scripts/basenames.py`, forge-level `scripts/nushell/` deleted; boto3 moved `[project]` → dev group — re-decided, since forge's moto fixture imports it directly), T0.4 [Docs truth sweep](tasks/T0.4-docs-truth-sweep.md) (archive moves + link sweeps for the stale plan copies; `archive/merged/` rename; D1/D8 supersession banners; `diataxis/` deleted as stale generated output; README/TOC/OVERVIEW/requirements staleness fixed; two items recorded as already fixed by earlier passes), T0.5 [Idempotent edit application](tasks/T0.5-idempotent-edit-application.md) (stage-then-write, resolved-path mutual exclusion before any write, idempotent-skip on retry with documented residual ambiguities; retry regression test + first `hypothesis` property suite), T0.6 [Eval judge integrity](tasks/T0.6-eval-judge-integrity.md) (judge prompts carry capped repo-context file listings; `JudgeVerdict` validates exactly one score per criterion; named ±0.5 band + sample-size caveat; benchmark stays deferred/operator-optional), T0.8 [Operating decision](tasks/T0.8-operating-decision.md) (closed retrospectively in the TASKS.md Phase 1 preamble — operated=yes per D99, interims justified, T4.1 owns their deletion). `make gates` EXIT=0 (forge 1327 / pbook 353 / ocr 118 / sax-platform 398; mypy ×4; import-linter 6 kept) | — |
| 2026-07-18 | [T3.6 — Composition roots everywhere (D93 implemented; Phase 3 complete): frozen `ForgeSettings`/`OcrSettings`/`PbookSettings` built once at each worker main (CLI commands build only the groups they need); class-based activities with bound methods (forge `StoreActivities`/`ContextActivities`/`LlmActivities`/`BatchActivities` in `forge/activities/roots.py`; ocr `OcrStoreActivities`; pbook `StoreActivities`/`LlmActivities`/`EmbeddingActivities` in `pbook/roots.py`); one DB engine + one SDK client per process; all nine module-global client/provider/engine seams deleted; settings groups the only keyed env readers (documented XDG / `allowlist_env` / click-`envvar=` exceptions); `sax_platform.testing` (temporal_env, FakeLLM, FakeMistralOcr) imported explicitly by app conftests; `S3Blobs` class replaces the `s3_blobs` module fns; `get_store_url` deleted; `make_mistral_client(api_key)` required; pbook worker onto `connect_temporal` + the platform run_worker scaffold with migrations via alembic `cfg.attributes`. Behavior changes: ocr fails fast at startup on missing `FORGE_OCR_S3_BUCKET` (was first-use error) and builds one engine per process (was one per activity call); pbook pg pool capped 5+5 (was uncapped); pbook log rotation 10MB×5 (was 5MB×3). AC sweeps all ZERO; `make gates` EXIT=0 (forge 1299 / pbook 353 / ocr 118 / sax-platform 398; mypy ×4; import-linter 6 kept; postgres 7)](tasks/T3.6-composition-roots.md) | — |
| 2026-07-17 | [T3.5 — Forced tool use retired platform-wide (D90 implemented): forge sync+batch and pbook moved onto `sax_platform.llm` (thinking/effort support and stored-bytes failure classification added to the platform); both string-keyed output-type registries replaced by frozen `OUTPUT_TYPES` mappings; typed refusal/truncation/mismatch failures with retry guidance on both lanes (batch parse non-retryable on deterministic bytes); `libs/sax-llm` deleted (package-identity retirement inherited from T3.4); workspace now four packages; `make gates` green (forge 1310 / pbook 358 / ocr 115 / sax-platform 387 tests; import-linter 6 contracts kept)](tasks/T3.5-forced-tool-use-retirement.md) | — |
| 2026-07-17 | [T3.4 — Platform plumbing modules: `sax_platform.{contracts,temporal,db,embeddings,config,logging}`; forge-contracts retired and forge/ocr/pbook migrated onto the platform; Alembic hardened (render_as_batch, advisory lock, clean autogenerate); worker drain 30s→5min; workspace is now five packages; `make gates` green (forge 1289 / pbook 354 / ocr 115 / sax-llm 96 / sax-platform 371 tests; import-linter 7 contracts kept); sax-llm's package-identity retirement deferred to T3.5](tasks/T3.4-platform-plumbing-modules.md) | — |
| 2026-07-17 | Graceful worker shutdown on SIGTERM/SIGINT (drain + exit 0 + KeepAlive relaunch); `make workers-restart` / `workers-status` — operator-requested restart-without-launchctl | [#41](https://github.com/stevegsax/forge/pull/41) |
| 2026-07-17 | Review fixes — 10 verified findings from the Phase 3 diff (workflow-backed review): thinking caps + stop_reason telemetry (migration 003), legacy-payload validator, fallback polarity, null-response guards, fail-fast API key, endpoint normalization, CLI provider validation, cached mistral resolver; judge pin deferred to T0.6 | [#40](https://github.com/stevegsax/forge/pull/40) |
| 2026-07-17 | [T3.3 — MistralOcr in sax_platform; Mistral chat deleted from sax-llm; ocr DI seam (e2e deferred to post-merge)](tasks/T3.3-mistral-ocr-chat-deleted.md) | [#39](https://github.com/stevegsax/forge/pull/39) |
| 2026-07-16 | [T3.2 — One tier registry + thinking migration: pins live-verified (opus-4-8/sonnet-5/haiku-4-5), budget_tokens deleted platform-wide, --effort CLI](tasks/T3.2-tier-registry-thinking-migration.md) | [#38](https://github.com/stevegsax/forge/pull/38) |
| 2026-07-16 | [T3.1 — Platform LLM client, both lanes: sax_platform born (98 tests, 99% cov; rec-7 + live-API pre-work recorded)](tasks/T3.1-platform-llm-client.md) | [#37](https://github.com/stevegsax/forge/pull/37) |
| 2026-07-16 | [T2.3d — mypy strict: pbook (177 errors; 16 recorded stub-boundary ignores) — Phase 2 complete](tasks/T2.3d-mypy-strict-pbook.md) | [#36](https://github.com/stevegsax/forge/pull/36) |
| 2026-07-16 | [T2.3c — mypy strict: ocr (zero ignores)](tasks/T2.3c-mypy-strict-ocr.md) | [#35](https://github.com/stevegsax/forge/pull/35) |
| 2026-07-16 | [T2.3b — mypy strict: sax-llm (zero ignores; one boundary cast)](tasks/T2.3b-mypy-strict-platform-llm.md) | [#34](https://github.com/stevegsax/forge/pull/34) |
| 2026-07-16 | [T2.3a — mypy strict: forge-contracts (zero ignores); member mypy wired into make + CI](tasks/T2.3a-mypy-strict-platform-contracts.md) | [#33](https://github.com/stevegsax/forge/pull/33) |
| 2026-07-16 | [T2.2 — Root gates: GitHub Actions CI, import-linter DAG contracts, 85% coverage gates everywhere](tasks/T2.2-root-gates.md) | [#32](https://github.com/stevegsax/forge/pull/32) |
| 2026-07-16 | [T0.7 — Local deployment: retire EC2 (D99); status markers closed out](tasks/T0.7-deploy-hardening.md) | — |
| 2026-07-16 | [T2.1 — COMPLETE: monorepo workspace in forge (all increments + Python 3.14 bump)](tasks/T2.1-workspace-creation.md) | — |
| 2026-07-16 | [T2.1 increment 3 — ocr absorbed into apps/ocr; D98 repo consolidation complete; task continues (3.14 bump)](tasks/T2.1-workspace-creation.md) | — |
| 2026-07-16 | [T2.1 increment 2 — sax-llm + forge-contracts absorbed into libs/; Finding A closed; ocr repointed; task continues](tasks/T2.1-workspace-creation.md) | — |
| 2026-07-16 | [T2.1 increment 1 — pbook absorbed as a workspace member at apps/pbook (D98); task continues](tasks/T2.1-workspace-creation.md) | — |
| 2026-07-16 | [T1.0 amendment — sax-llm editable everywhere; pin workaround unwound (pbook + forge)](tasks/T1.0-uniform-editable-sibling-sources.md) | — |
| 2026-07-15 | [T1.8 — Small dedup batch + kill runs-extraction](tasks/T1.8-small-dedup-batch.md) | — |
| 2026-07-15 | [T1.6b — Batch-wait failure symmetry](tasks/T1.6b-batch-wait-failure-symmetry.md) | — |
| 2026-07-15 | [T1.6a — Idempotency rekey](tasks/T1.6a-idempotency-rekey.md) | — |
| 2026-07-15 | [T1.5 — Nested fan-out propagation fix](tasks/T1.5-nested-fan-out-propagation.md) | — |
| 2026-07-15 | [T1.7 — Env scrub at model-influenced subprocess seams](tasks/T1.7-env-scrub-subprocess-seams.md) | — |
| 2026-07-15 | [T1.4 — Unblock the worker event loop](tasks/T1.4-unblock-worker-event-loop.md) | — |
| 2026-07-15 | [T1.3 — INTERIM minimal poller patch](tasks/T1.3-interim-poller-patch.md) | — |
| 2026-07-15 | [T1.2 — INTERIM batch-result correlation stopgap](tasks/T1.2-interim-batch-result-correlation.md) | — |
| 2026-07-15 | [T1.1 — Delete the dead provider stack; repatriate sax-llm's tests](tasks/T1.1-delete-dead-provider-stack.md) | — |
| 2026-07-15 | [T1.0 — Uniform editable sibling sources](tasks/T1.0-uniform-editable-sibling-sources.md) | — |
