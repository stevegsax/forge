# Handoff — CLAUDE.md became a phase ledger; resume T10.1 (still mid-ST1)

**Date:** 2026-07-29
**Status:** Two commits landed since the last sweep, both records-only —
the pbook markdownlint config deletion **`bd7b4d1`** and the CLAUDE.md
ledger restructure + paired sweep amendment **`cfe6bc1`**. **No gates
were re-run and none were owed**: both change-sets touch `.md` files
plus one deleted `.yaml`, with zero changes under `src/`, `libs/`,
`apps/**/src`, or `tests/`. The latest recorded gates therefore remain
T5.6's follow-up numbers (T5.6 task file, § Follow-up): `make gates` →
**EXIT=0** (forge **1,538** / pbook 376 / ocr 238 / sax-platform 517;
forge coverage **91.47%**; mypy clean ×4; import-linter 6 contracts
kept), `tests/test_replay.py` **17 passed**, `pytest -n auto
tests/workflows/` 112 passed on 14 workers. **Production runs
`d6af5be`** — re-derived this sweep by `git -C ~/repos-sax/forge-prod
rev-parse --short HEAD` on a clean tree; **no deploy happened this
period**, so prod is unchanged and neither landed commit is (or needs
to be) deployed. Next task: **T10.1 — migrate forge onto
sax-datastores**, still **IN PROGRESS**, still resuming mid-ST1.

This is a state-of-the-world note for the next session. It does not
restate the work queue ([TASKS.md](TASKS.md)), the decisions
([../docs/DECISIONS.md](../docs/DECISIONS.md)), the status-of-record
([../docs/OVERVIEW.md](../docs/OVERVIEW.md)), or the lifecycle
([SDLC.md](SDLC.md)) — it says what changed since the
[2026-07-28 T10.1-start handoff](HANDOFF-2026-07-28-t10.1-start.md),
what to know before touching anything, and where to start. **This
handoff closes a records-improvement unit, not a task**: T10.1 has not
advanced, and its resume point has moved only in that the external
repo diverged further. **Read items 2 and 3 of "What to know" before
doing anything with T10.1** — a *third* Plan decision is now superseded,
and the owner's prod-placement decision landed externally.

## What landed since the last handoff

- **`bd7b4d1` — `apps/pbook/.markdownlint.yaml` deleted (owner
  commit).** The member-local config is gone so the project root
  config governs. Mechanism worth carrying: markdownlint-cli2's config
  discovery walks *up from the invoking directory and stops there*, so
  deleting the member config makes root-invoked linting correct but
  makes a bare run from inside `apps/pbook` fall back to the tool's
  built-in defaults — a silently different rule set, not an error.
  That gap is what motivated the `--config` codification below.
- **`cfe6bc1` — CLAUDE.md ledger + paired sweep amendment + truth
  fixes.** Owner-directed CLAUDE.md improvement run through the newly
  installed `claude-md-management` plugin's improver skill (audit →
  quality report → three approved proposals):
  - **The Landed narrative became a phase ledger.** Re-derived this
    sweep: the ledger paragraph went **3,552 → 735 words** and the file
    **7,342 → 4,532 words** (`git show 47cae47:CLAUDE.md | wc -w`
    versus `wc -w CLAUDE.md`; the commit message says 4,525, a 7-word
    drift in the message only — the ledger figure is exact). Completed
    phases are one sentence each with D-numbers and pointers; Phase 5,
    still open, keeps per-task one-liners with SHAs. Per-task depth is
    delegated to CHANGELOG rows, task files, and these handoffs.
    Everything forward-binding survived: the open adjudications, the
    D97 halt-versus-recover spend boundary, the latest gates, the
    T10.1 zero-workflow-code invariant, and the Next-up sentence.
  - **The paired sweep-skill amendment** (same change-set, per SDLC's
    documentation-theory rule 1): `.claude/skills/handoff-sweep/SKILL.md`
    Step 3(b) now says to append one-liners to the *open-phase* entry
    and to collapse a phase into a single sentence at close, so the
    ledger form maintains itself instead of decaying back into
    narrative. This sweep is the first run under the amended step.
  - **T8.2 gained a dated partial-premise-consumption note** — the
    ledger restructure consumed part of its premise. Its OVERVIEW.md
    rewrite half and the A6 re-adjudication are untouched and still
    open.
  - **Truth fixes.** Root CLAUDE.md: the stale "(5433 is taken by
    another project)" port parenthetical replaced with current state.
    `apps/pbook/CLAUDE.md`: "(Supabase-hosted)" replaced by the D102
    local store with a guard/profiles pointer, and its coverage-gate
    figure corrected 84% → 85% to match `pyproject.toml`'s
    `--cov-fail-under=85`.
  - **The cwd-proof lint invocation codified** in sweep Step 4 and
    PROCESS.md "After Coding" step 1:
    `markdownlint-cli2 --config "$(git rev-parse --show-toplevel)/.markdownlint-cli2.jsonc"`.
- **Nothing else.** T10.1 did not advance; no forge source, test, or
  deploy file changed; production was not touched.

## Owner rulings

- **All three improver proposals approved** — the ledger restructure,
  the paired sweep-skill amendment, and the truth fixes. The ledger
  restructure explicitly consumes part of T8.2's premise, and that is
  recorded in T8.2 rather than left implicit.
- **The `--config` codification requested** rather than left as
  convention, because a bare member-directory run degrades silently.
- **The member-local pbook lint config deleted** in favor of project
  defaults.
- **Externally (sax-datastores, 2026-07-28): prod co-locates on this
  machine, temporarily.** See item 3 — it answers the open ST6
  prod-host premise question and imposes a hard gate on prod bring-up.
- **No new rulings on A1 or A3–A6, A8.** A7 remains absorbed by T10.1.
- **No ruling yet on the T9 queue-structure question** (item 9).

## What to know before touching anything

1. **The tree carries two items and neither is yours.**
   `git status --porcelain --untracked-files=all` gives exactly:
   `M development-plans/tasks/T10.1-forge-sax-datastores-migration.md`
   — the in-flight T10.1 Plan and sub-tasks with `Status: IN PROGRESS`,
   still uncommitted, unchanged since the last handoff — and `??
   .claude/settings.json`, an 87-byte file whose entire content enables
   the `claude-md-management` plugin. The settings file is the owner's
   to commit or ignore (it is a real repo-scoped setting, not a
   scratch artifact); `.claude/settings.local.json` alongside it is
   gitignored and does not appear. Leave both alone.

2. **A third T10.1 Plan decision is now superseded — decision 2, boot
   ownership.** The previous handoff flagged decisions 1 and 4 (the
   temporal container staying in forge's deploy; `forge_temporal`
   provisioned on both stacks), both retired by Temporal becoming
   org-wide infrastructure. Decision 2 reads: forge's `start-stack.sh`
   gains `make -C ~/repos-sax/sax-datastores prod-up`/`dev-up` before
   forge's own `stack-up` — "one boot owner on this host". That premise
   is now false: sax-datastores took ownership of its own boot in
   `4749e61`. Its README "Host boot" section specifies a LaunchDaemon
   running `podman machine start` at boot with no GUI login, plus the
   user-scope `podman-restart.service` enabled *inside* the VM so every
   `restart: unless-stopped` container comes up with the machine — a
   per-VM mechanism that covers sax-temporal's containers too. **The
   LaunchDaemon is already installed**: `/Library/LaunchDaemons/com.saxcapital.podman-machine-start.plist`
   exists, `root:wheel`, dated 2026-07-29 08:15, so the "sudo install
   pending" note in that commit's message is stale. Start order is
   explicitly *not* orchestrated — dependents rely on connection
   retries. Consequence for T10.1: forge should stop planning to boot
   the shared stacks and instead verify its workers tolerate a losing
   race, which is a different sub-task shape and a different ST7 reboot
   test. Owner's call, not this sweep's.

3. **sax-datastores moved again, and the push the last handoff was
   waiting on happened.** Re-derived: local `main` is **`4749e61`**,
   `origin/main` is **`9b04d4e`**, `git rev-list --left-right --count
   origin/main...HEAD` → `0 1`. So the seven previously-unpushed
   commits **were pushed** (GitHub Actions ran at 2026-07-28T20:09:25Z),
   and exactly one new commit is local-only. That commit,
   `4749e61` "docs+ops: prod co-locates on the dev machine (temporary)
   — rationale §19", is a decision record:
   - **Prod runs on the same machine as dev for now**, explicitly
     temporary, with a dedicated host still the target — so nothing in
     the prod stacks may become host-specific. Co-location needed no
     redesign because the environment split never relied on host
     separation: non-default prod ports, separate compose
     projects/networks/secrets files, and the auth wall.
   - **Two accepted risks, named by the owner.** Single failure domain
     (one VM, one disk, one power event now covers both environments),
     and agent reach (anything holding the podman socket controls prod
     containers and volumes; the sandbox profile will additionally
     block the socket).
   - **The gate that matters to T10.1:** with prod co-located, the
     off-machine S3 backup is the only copy that survives the machine,
     so `docs/backups.md`'s bucket/IAM/cron wiring becomes **the gate
     before prod holds data, not an after-task**. In T10.1's sub-task
     list that is **ST5**, currently sequenced *after* ST3/ST4 and
     before ST6 — the ordering survives, but ST5 is now a hard external
     precondition for ST6 rather than a convenience.
   - Also applied: the "a rogue minio endpoint finds nothing listening"
     property weakens to *fails at auth* while dev MinIO listens on
     :9000 of the same host; podman machine memory raised 2 → 8 GiB
     before prod bring-up, because resizing later means stopping the
     machine and that will be a prod outage.
   - `~/repos-sax/sax-temporal` also moved: HEAD is **`900dec0`**
     ("docs: prod placement decided — co-located on the dev machine for
     now"), up from `d7c1c0f`. **That repo has no git remote at all**
     (`git remote -v` is empty), so it has no off-machine copy and can
     never trigger CI.

4. **The GHCR image is still unpublished, and the push did not fix
   it.** `gh workflow list` shows `publish-postgres` **active**;
   `gh run list --workflow=publish-postgres.yml` returns **zero rows**
   even after the 2026-07-28 push, which fired only `secret-scan` and
   `check-registrations`. The paths filter still does not match. A
   locally built `ghcr.io/stevegsax/sax-postgres-rum:17` is what the
   running dev stack uses, so nothing is blocked today — but T10.1
   Plan step 3 moves forge's CI testcontainers onto the GHCR image, so
   `gh workflow run publish-postgres.yml` must succeed before that
   acceptance criterion can pass. Waiting for a natural trigger is now
   proven not to work.

5. **The live topology, re-measured this sweep** (`podman ps`, `lsof`):

   ```text
   127.0.0.1:5432  sax-dev-postgres      ghcr.io/stevegsax/sax-postgres-rum:17   up 16h, healthy
   127.0.0.1:9000  sax-dev-minio         quay.io/minio/minio:latest              up 16h, healthy
   127.0.0.1:7236  sax-temporal-dev      temporalio/server:1.25.2                up 16h, healthy
   127.0.0.1:8236  sax-temporal-dev-ui   temporalio/ui:2.31.0                    up 16h
   127.0.0.1:5434  forge-postgres        pgvector/pgvector:pg16                  up 16h, healthy  (legacy)
   127.0.0.1:9002  forge-minio           minio RELEASE.2025-09-07                up 16h, healthy  (legacy)
   127.0.0.1:7233  forge-temporal        temporalio/auto-setup:1.25.2            up 16h           (legacy, prod)
   127.0.0.1:8233  forge-temporal-ui     temporalio/ui:2.31.0                    up 16h           (legacy)
   ```

   Unchanged from the previous handoff except uptimes. Ports **5433
   and 5442 are still free** — **the prod stack has still never been
   up**, and every registration's `applied: prod` is still `pending`.
   `registrations/forge.yaml` still reads `applied: dev: pending` with
   a grandfathered bare-slug request (`buckets: []`,
   `extra_databases: {}`), so ST1's registration update is still
   entirely unstarted.

6. **Nothing on the forge side has moved.** Re-derived from the
   profiles: `~/.config/forge/envs/prod.env` → `@localhost:5434/forge`
   and `@localhost:5434/pbook`, temporal at `127.0.0.1:7233`;
   `~/.config/forge/envs/dev.env` → `@localhost:5434/forge_dev`
   **twice** (forge and pbook sharing one database — Problem defect 3,
   still live), temporal at `127.0.0.1:7233` in namespace `forge-dev`.
   No forge repo file has been changed for T10.1. The dev lane still
   runs entirely on the legacy stack.

7. **CLAUDE.md is now a ledger, and keeping it one is a procedure, not
   a preference.** Step 3(b) of the sweep skill is the enforcement
   point: a landed task gets **one line** in the open-phase entry
   (task id, date, SHA, one sentence, the proof clause, any
   forward-relevant deviation) and depth goes to the CHANGELOG row, the
   task file, and the handoff. When a phase closes, its one-liners
   collapse to a single sentence with pointers. If a future session
   finds itself writing a paragraph into CLAUDE.md, that is the signal
   the content belongs somewhere else.

8. **Open adjudications: A1, A3–A6, A8** (plan review §2–§3; A2 was
   ruled — the T5.5 → T5.6 order; **A7 is absorbed by T10.1**). Do
   **not** act on them: A1 (T6.7 before T7.1/T7.3/T7.4/T7.5), A3 (tier-map
   swap before or into T6.2), A4 (T6.1's direction versus Architecture
   Principle 8), A5 (`batch_jobs` spend columns — decline or park), A6
   (T8.2's "OVERVIEW Open items closed" instruction — note the ledger
   restructure consumed a *different* part of T8.2, not this one), A8
   (the WORKERS.md ops section). The owner walkthrough is the ruling
   vehicle and resumes at T6.1 after the datastore work.

9. **Still-unowned records debt, carried forward unchanged.** (a) The
   **three T9 task files remain absent from TASKS.md** — verified
   again this sweep: `rg -n "T9\." development-plans/TASKS.md` returns
   nothing, while `tasks/T9.{1,2,3}-*.md` exist and read `NOT STARTED`.
   Whether they get a top-level section like T10.1's is an open owner
   ruling on queue structure. (b) The 2026-07-28 T9 review findings
   (T9.1's false `filtered.txt` reference at `T9.1:128`; the corpus
   manifest having no versioned home; undecided Principle 8 access
   paths for the T9.2 harness; the OneDrive Files-On-Demand hydration
   hazard; "WADM" undefined at first use) and the sax-datastores
   repo-side findings (restore-check's 48h staleness gate only fires on
   a monthly manual run, so detection *latency* is the weak leg;
   `template1` keeps PUBLIC connect; role DDL logs passwords; MinIO
   pinned to `:latest`) still live only in session context. (c) New
   this sweep: `apps/pbook/design/DATA_MODEL.md:8` still says
   PostgreSQL "(Supabase-hosted)" — the same falsehood `cfe6bc1` fixed
   in `apps/pbook/CLAUDE.md`. Its owner is **T8.3** (pbook design-docs
   truth pass), so it is flagged, not fixed.

10. **Standing directives**, reproduced from [SDLC.md](SDLC.md)
    § Standing directives as it reads today (13 items, four groups;
    that section is the canonical home — amend it there, never here).
    *Authority and scope:* commits and merges are the owner's on
    explicit word only, and no agent, skill, or sweep commits;
    implementation is dispatched to `implementation-driver` with
    hard-bounded scopes, and Temporal workflows, the environment guard,
    concurrency, and migrations are never delegated to Sonnet;
    pre-existing uncommitted changes in the tree belong to other work
    and stay untouched. *Engineering:* no compatibility shims — forge
    has no external consumers, so fix the seam in place and update all
    callers; activity timeout/retry presets come from
    `forge/presets.py` only, because the values are
    ScheduleActivityTask command attributes and changing one is
    replay-breaking; no inline copies of the step/gather/dispatch
    pipelines in the `forge/workflows/` drivers; new workflow tests
    pass `sync_mode=True` and script outcomes keyed by identity, never
    arrival order, with no module-level mutable containers (the
    pytest-xdist blocker T5.5 removed); read
    `~/.claude/python-guidelines.md` before writing or reviewing
    Python, and require the same of any spawned code-writer.
    *Working style:* mechanism-level explanations — the underlying
    cause, act-now versus act-later versus no-action, concrete command
    sequences, and never a safety caveat compressed into a bare
    clause; challenge weak arguments and spot missing data, do not
    praise. *Tool hazards:* `rg -n`, never `rg -rn` (`-rn` parses as
    `--replace n` and fabricates output); never start a wrapped `.md`
    prose line with `+`, because the formatter hook re-parses it as a
    list marker and rewrites the list; `gsed`, not `sed`, when
    stream-editing is unavoidable — prefer Edit.

## Where to start

- **T10.1 is IN PROGRESS, mid-ST1 — still a resume, not a pickup, and
  the reconciliation debt grew.** Read order: the task file's Plan
  (six recorded decisions, seven sub-tasks) → its Problem and
  Background → then `~/repos-sax/sax-datastores/docs/product-migration.md`
  § "Current state (2026-07-28)" and § forge, and
  `docs/rationale.md` §19, all of which are newer than the Plan and
  authoritative on the external side.
- **First move is still a reconciliation, not a command — and it is
  now three decisions, not two.** Decisions 1 and 4 (temporal in
  forge's deploy; `forge_temporal` on both stacks) are retired by
  Temporal becoming org infrastructure; decision 2 (forge's
  `start-stack.sh` as the one boot owner) is retired by
  sax-datastores' own boot wiring, already installed. **The Plan edit
  is still uncommitted**, so amending it means editing an already-dirty
  file — write the amendment with a dated Development Note recording
  what the divergence retired and why, then hand the whole file to the
  owner as one commit.
- **Then ST1, which still needs an access decision before it can move
  at all.** File tools in a forge session are sandboxed to the forge
  directory, so edits inside `~/repos-sax/sax-datastores` and
  `~/repos-sax/sax-temporal` cannot be made from here. The owner picks:
  grant write access to `~/repos-sax/sax-datastores`, or take a patch
  file written into scratchpad, or hand-apply. Nothing in ST1 proceeds
  until one is chosen.
- **What ST1 then does**, given item 5's measurements — all of it on
  the sax-datastores side, all of it through the registration flow
  rather than compose or `.env` edits (that file shape no longer
  exists): update `registrations/forge.yaml` to declare the MinIO
  bucket `saxcapital-forge-blobs-dev` and pbook (first-class `pbook`
  slug per the Plan's decision, or the `forge_<env>:pbook_<env>`
  extra-DB groups), declare **nothing** temporal, run the
  intake/validate path, apply to the dev stack, record `applied: dev`,
  and run the auth-isolation probes.
- **Prod bring-up is gated externally now.** Item 3: with prod
  co-located, the off-machine backup wiring is the gate before prod
  holds data. T10.1's ST5 is that wiring, and it needs owner acts
  (bucket prefix + lifecycle rule, two IAM credentials,
  `BACKUP_S3_URI`). Treat ST5 as blocking ST6, not adjacent to it.
- **Invariant for the whole task, unchanged:** zero workflow code, so
  the 16 committed replay histories must pass **unregenerated** end to
  end. The one sanctioned forge code change is the environment guard's
  new URL-suffix coherence check — the database twin of
  `require_namespace_coherence`, exit 78 on mismatch — and the guard is
  explicitly never delegated to Sonnet.
- **After T10.1:** T5.7 (execution-time reference repair), then the
  owner walkthrough resumes at T6.1 before any Phase 6 work is planned.
- Normal pickup reconciliation still applies — verify the numbers above
  against the tree before planning.
