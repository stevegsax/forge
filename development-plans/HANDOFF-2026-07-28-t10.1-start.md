# Handoff — T5.6 follow-up landed and deployed; resume T10.1 (mid-ST1)

**Date:** 2026-07-28
**Status:** Three commits landed since the last sweep — the owner-ruled
T5.6 follow-up **`887bbdc`**, the DEPLOYMENT.md pre-deploy in-flight
check **`47dd284`**, and the T10.1 task file + queue section
**`d6af5be`**. Gates as recorded in T5.6's task file (§ Follow-up,
"Gates for the follow-up change-set"): `make gates` → **EXIT=0** (forge
**1,538** / pbook 376 / ocr 238 / sax-platform 517; forge coverage
**91.47%**; mypy clean ×4; import-linter 6 contracts kept),
`tests/test_replay.py` **17 passed** (16 histories + the
populated-directory guard), `uv run pytest -n auto tests/workflows/`
112 passed on 14 workers. **Production runs `d6af5be`** — verified this
sweep by `git -C ~/repos-sax/forge-prod rev-parse --short HEAD` (clean
tree) and by `temporal task-queue describe` on all three prod queues:
`prod-forge-worker-1@d6af5be`, `prod-forge-worker-2@d6af5be`,
`prod-ocr-worker@d6af5be`, `prod-pbook-worker@d6af5be`, none `-dirty`.
**T5.6 and its follow-up are therefore live**, superseding the previous
handoff's "production runs `e0e3214` … T5.6 is not deployed". Next
task: **T10.1 — migrate forge onto sax-datastores**, already **IN
PROGRESS**, resuming mid-ST1.

This is a state-of-the-world note for the next session. It does not
restate the work queue ([TASKS.md](TASKS.md)), the decisions
([../docs/DECISIONS.md](../docs/DECISIONS.md)), the status-of-record
([../docs/OVERVIEW.md](../docs/OVERVIEW.md)), or the lifecycle
([SDLC.md](SDLC.md)) — it says what changed since the
[2026-07-27 T5.7-start handoff](HANDOFF-2026-07-27-t5.7-start.md), what
to know before touching anything, and where to start. **Read item 2 of
"What to know" before doing anything with T10.1**: the external repo the
task migrates onto moved seven commits during the same day, and two of
the Plan's six recorded decisions are retired by that movement.

## What landed since the last handoff

- **T5.6 follow-up (`887bbdc`, four owner rulings).** (1) A preflight
  **halt now reports every attempt**: `PlanPreflightHalt` carries
  `attempts: tuple[PlanAttempt, ...]` (each `PlanCallResult` paired with
  the violations that rejected it, so stats and reasons cannot drift),
  `plan_preflight_failure` stamps the summed `planner_stats` plus an
  `llm_totals` counting every attempt (new pure
  `step_logic.sum_llm_stats`), and the error names each attempt's own
  violations. The spend boundary is recorded in the task file: a *halt*
  reports every attempt (nothing survived, so reporting the last would
  understate money spent — it had been under-reporting by two thirds),
  while a run that *recovers* on retry keeps ordinary D97
  surviving-call semantics, with the interactions table authoritative
  either way. Also recorded: `MAX_PLAN_REVISIONS` is per-**run**
  cumulative (`revision_count` lives at `_drive_plan` scope).
  (2) **Replay histories 13 → 16** — `preflight_retry_sync`,
  `preflight_halt_sync`, `plan_revision_halt_sync`: the retry arm and
  both plan-halt terminals are replay-pinned for the first time, and the
  existing 13 stayed byte-identical and replayed unregenerated after the
  aggregation change (the no-command-change proof). (3) **Batch-lane
  preflight is observed, not assumed** — `TestPreflightOnTheBatchLane`
  runs reject-then-accept across two full planner submit/poll/fetch/parse
  cycles with *distinct* request ids, and a three-strikes halt across
  three; zero harness changes, because `plan_sequence` was already
  lane-independent through `request_id`. (4) Records: the TASKS.md queue
  line shows T5.6's AC-1 partial, plus the dated task-file section and a
  CHANGELOG row.
- **Pre-deploy in-flight workflow check (`47dd284`).** DEPLOYMENT.md
  gained, directly above the D103 deploy command, the
  `temporal workflow list --query 'ExecutionStatus="Running"'` check
  with its mechanism: a worker restart makes every running workflow
  **replay its recorded history on the new code**, so a deploy is safe
  for in-flight runs only when the new code accepts what those histories
  already recorded. T5.6 supplied both live examples (a recorded plan
  the gate now rejects replays into a different command sequence; an
  empty-`LLMResponse` payload no longer deserializes) — either way
  Temporal retries the failed workflow task forever and the run hangs
  until a human terminates or resets it. Deliberately manual: whether an
  in-flight run may be sacrificed is a judgment call. Generalized to any
  deploy touching workflow code, a model validator, or a
  `forge/presets.py` value.
- **Production deployed to `d6af5be`** (2026-07-28) — the first use of
  that procedure, with both namespaces verified drained beforehand. All
  four pollers re-verified during this sweep (see the Status block).
  T5.4/T5.5 were already live; **T5.6 and the follow-up went live with
  this deploy**.
- **T10.1 created and queued (`d6af5be`).** New task file
  [T10.1 — Migrate forge onto sax-datastores](tasks/T10.1-forge-sax-datastores-migration.md),
  plus a new top-level **"Datastore consolidation (approved
  2026-07-28)"** section in TASKS.md. The section is deliberately
  outside the migration's task count (still 59) and models the queue
  pattern for post-migration projects; **T5.7 remains the first
  unchecked task in TASKS.md file order** while T10.1 carries the
  owner-priority note in its section header and `IN PROGRESS` in its
  task file. The task grounds five defects in forge's current data
  layer (accidental topology on one PG16 instance; the app role is the
  cluster superuser; dev is not topology-parity with prod; Temporal's
  databases are never backed up — plan-review finding A7; repo-local
  extension surface) and carries nine ACs including auth-level
  isolation proof, guard URL-suffix coherence, a reboot test, and zero
  workflow-code changes with replay histories unregenerated.
- **T10.1 opened — the Plan and Sub-tasks are written and Status is
  IN PROGRESS, and that edit is still uncommitted.** It is the single
  `M development-plans/tasks/T10.1-…` in `git status --short`. It is
  task work, not sweep work: this sweep did not touch it.
- **Three T9 task files (`1a89aac`)** — T9.1 provenance ledger, T9.2
  classifier evaluation, T9.3 document classifier app. Committed
  2026-07-27 20:50, i.e. **37 minutes before** the previous sweep's own
  commit (`75512a2`, 21:27) but absent from that handoff, so they are
  recorded here. **They are not in TASKS.md** — there is no Phase 9
  section and no count change (verified: `rg -n "T9\." TASKS.md` returns
  nothing). See item 8.

## Owner rulings

- **T10.1 created and sequenced "right away"** — ahead of T5.7 and ahead
  of the sax-datastores guide's own recommended product order
  (annotation_manager → epub-reader → forge). Rationale in the task
  file: the consolidation retires a defect class forge is living with
  daily, and forge's cutover is the largest of the fleet, so its
  rehearsal value is highest first.
- **The T5.x-before-migration question ruled:** nothing in T5.x blocks
  the datastore work, and the ordering was deploy-first — T5.6 to
  production, then start T10.1. Both halves executed the same morning.
- **The pre-deploy in-flight check codified** into DEPLOYMENT.md rather
  than left as handoff lore, and kept manual by decision.
- **T5.7 follows the cutover**, not the other way round.
- **No new rulings on A1 or A3–A8.** One clarification: **A7 (the
  nightly backup being verified-once and unmonitored) is deliberately
  absorbed by T10.1** — its AC set retires forge's own backup leg in
  favor of the shared nightly plus `make prod-restore-check`, whose 48h
  staleness gate is the alarm A7 was about. Do not open separate work
  for A7.

## What to know before touching anything

1. **The tree carries one uncommitted file and it is not yours.**
   `development-plans/tasks/T10.1-forge-sax-datastores-migration.md`
   holds the Plan, the seven sub-tasks, and `Status: IN PROGRESS`
   (`git diff --stat`: 116 insertions, 4 deletions). It belongs to the
   in-flight T10.1 work. Leave it in place; the owner commits it.

2. **The external repo moved seven commits after T10.1's Plan was
   written, and two of the Plan's six decisions are now retired by it.**
   This is the single most important fact in this handoff. Derivation:
   `~/repos-sax/sax-datastores` local `main` is **`9b04d4e`**, seven
   commits ahead of `origin/main` = **`b0f371f`** (which is the SHA the
   Plan and the ST1 notes were written against — unpushed, so a fresh
   clone still sees the old shape). What changed:
   - `9f995f3` **secrets left the repo**: per-stack `.env.example` and
     `scripts/gen-env.sh` are **deleted**, replaced by committed
     non-secret `stacks/{dev,prod}/manifest.env` plus XDG-mode-600
     secret files written by `make {dev,prod}-secrets`
     (`gen-secrets.sh`, `with-stack-env.sh`, `print-url.sh`,
     `secrets-lib.zsh`), with gitleaks in CI. **Any instruction to edit
     `.env.example` or run `gen-env` is stale.**
   - `974a6e7` **a project-registration system**: `registrations/*.yaml`
     approval records, `scripts/check-registrations.sh` diffing records
     against deployment files, an intake script, a `register-project`
     skill, and a CI check. Adding a project is now a registration
     change, not a manifest hand-edit.
   - `6711a8c` + `72049b9` **Temporal became an org-wide service**
     under its own slug, in a **new fourth repo `~/repos-sax/sax-temporal`**
     (HEAD `d7c1c0f`), with `temporal_<env>` owning
     `temporal_visibility_<env>`; applied on dev.
   - `54fb611`, `46fc4e6` provisioning fixes; `9b04d4e` the guide
     rewritten to live state.

3. **Temporal is no longer forge's to move — the plan's `forge_temporal`
   slug is retired.** `docs/product-migration.md` § forge constraint 3
   now reads: "Temporal is org infrastructure now, not forge's
   (superseded 2026-07-28; the shared dev server is LIVE) … forge's
   migration DROPS its embedded temporal entirely; forge consumes gRPC
   plus the `forge` namespace … The old plan — forge_temporal as a
   forge-owned slug riding forge's deploy — is retired." Mechanism that
   makes this a real change and not a rename: **workflow histories do
   not move between Temporal clusters**, so this is a *drain*, not a
   dump/restore — point new starts and workers at the shared server,
   let the legacy server finish its in-flight runs (both run
   concurrently, which is exactly why the shared dev server sits on
   interim ports **7236/8236** while forge's legacy server holds
   7233/8233), then stop `forge-temporal` + `forge-temporal-ui` and let
   the shared server reclaim the default ports. The guide states it is a
   **separable change-set that shares nothing with the database move and
   may happen first**. Consequences for T10.1 as written, all of them
   the owner's or the task's to settle, not this sweep's: Plan decision
   1 (temporal container stays in forge's deploy, reaching
   `host.containers.internal:5442` as `forge_temporal_prod`) and Plan
   decision 4 (`forge_temporal` provisioned on both stacks, dev pair as
   rehearsal) are both superseded; Scope's "local-stack shrinks to
   Temporal(+UI) only" likely becomes "local-stack goes away"; and the
   `forge` namespace **already exists** on the shared dev server
   (verified: `temporal operator namespace list --address
   127.0.0.1:7236` lists `forge`).

4. **The live topology, measured this sweep — dev is already up and
   provisioned; prod is not.** `podman ps`, `lsof`, and a read-only
   catalog query give:

   ```text
   127.0.0.1:5432  sax-dev-postgres      ghcr.io/stevegsax/sax-postgres-rum:17   up 43m, healthy
   127.0.0.1:9000  sax-dev-minio         quay.io/minio/minio:latest              up 43m, healthy
   127.0.0.1:7236  sax-temporal-dev      temporalio/server:1.25.2                up 15m, healthy
   127.0.0.1:8236  sax-temporal-dev-ui   temporalio/ui:2.31.0                    up 15m
   127.0.0.1:5434  forge-postgres        pgvector/pgvector:pg16                  up 6d, healthy  (legacy)
   127.0.0.1:9002  forge-minio           minio RELEASE.2025-09-07                up 6d, healthy  (legacy)
   127.0.0.1:7233  forge-temporal        temporalio/auto-setup:1.25.2            up 6d           (legacy, prod)
   127.0.0.1:8233  forge-temporal-ui     temporalio/ui:2.31.0                    up 6d           (legacy)
   ```

   Ports **5433 and 5442 are free**; the legacy annotation_manager
   squatters are gone, so the "free 5432/9000" owner gate is **cleared
   and already consumed** by the dev stack itself. On the dev instance,
   roles and same-named databases exist for `annotation_manager_dev/_test`,
   `epub_reader_dev/_test`, `forge_dev/_test`, `temporal_dev/_test`, plus
   `temporal_visibility_dev/_test` owned by the temporal roles. **No
   `pbook` role or database exists** — `PROVISION_PROJECTS` in both
   manifests is `"epub_reader annotation_manager forge temporal"`, and
   `MINIO_BUCKETS` (dev) is `saxcapital-reader-epubs-dev` only, so
   forge's `saxcapital-forge-blobs-dev` is **not** yet declared. **The
   prod stack is not stood up** (5442 free, no prod containers, every
   registration's `applied: prod` is `pending`), and
   `registrations/forge.yaml` still reads `applied: dev: pending` with a
   grandfathered bare-slug request.

5. **Nothing on the forge side has moved yet.** Both profiles still
   point at the legacy instance:
   `~/.config/forge/envs/prod.env` → `@localhost:5434/forge` and
   `@localhost:5434/pbook`; `~/.config/forge/envs/dev.env` →
   `@localhost:5434/forge_dev` **twice** (forge and pbook sharing one
   database — that is Problem defect 3, still live). No forge repo file
   has been changed for T10.1. The dev lane therefore still runs
   entirely on the legacy stack and is unaffected by the new stacks
   being up.

6. **The deploy hazard that motivated `47dd284` is discharged for the
   code now running, but the rule generalizes.** The empty-`LLMResponse`
   validator and the preflight gate are both live on `d6af5be`, and both
   namespaces were drained before the deploy, so no in-flight run
   carries an incompatible payload today. Act-now: nothing. Act-later:
   run the DEPLOYMENT.md check before **every** deploy that touches
   workflow code, a model validator, or a `forge/presets.py` value —
   T10.1's cutover is exactly such a deploy (its Plan step 6 already
   opens with it), and it deliberately bundles config with
   infrastructure, so an in-flight run at that moment is the worst case.

7. **Open adjudications: A1, A3–A6, A8** (plan review §2–§3; A2 was
   ruled — the T5.5 → T5.6 order; **A7 is absorbed by T10.1**, see Owner
   rulings). Do **not** act on them: A1 (T6.7 before T7.1/T7.3/T7.4/T7.5),
   A3 (tier-map swap before or into T6.2), A4 (T6.1's direction versus
   Architecture Principle 8), A5 (`batch_jobs` spend columns — decline or
   park), A6 (T8.2's "OVERVIEW Open items closed" instruction), A8 (the
   WORKERS.md ops section). The owner walkthrough is the ruling vehicle
   and resumes at T6.1 after the datastore work.

8. **Two review results exist only in session context and have no
   owning record — they will be lost otherwise.** (a) The 2026-07-28
   review of the three T9 task files: T9.1 constructible; T9.2 mostly so
   (labels, sample, and text-access unspecified); T9.3 correctly parked.
   Five tensions, of which three are decisions rather than nits — the
   corpus manifest `filtered.txt` has **no versioned home** and T9.1's
   reference to it in the design repo is false (`T9.1:128` cites
   "`filtered.txt` in the design-record repository"); Principle 8 access
   paths are undecided for the T9.2 harness; and **the T9 entries are
   absent from TASKS.md**, which is an open owner ruling on queue
   structure now that T10.1 has established the "new top-level section"
   pattern. Also unaddressed: the OneDrive Files-On-Demand hydration
   hazard, and "WADM" is never defined at first use (`T9.2:64`,
   `T9.3:8`). (b) The sax-datastores project review: verdict sound, but
   the repo-side findings are recorded nowhere in that repo —
   restore-check's 48h staleness gate only fires on a monthly manual
   run (so detection latency, not detection, is the weak leg);
   `template1` keeps PUBLIC connect; role DDL logs passwords; MinIO
   pinned to `:latest`; no stack boot wiring. **This sweep flags both as
   unowned rather than filing them** — filing is task/records work.

9. **Standing directives**, reproduced from [SDLC.md](SDLC.md)
   § Standing directives as it reads today (13 items, four groups; that
   section is the canonical home — amend it there, never here).
   *Authority and scope:* commits and merges are the owner's on explicit
   word only, and no agent, skill, or sweep commits; implementation is
   dispatched to `implementation-driver` with hard-bounded scopes, and
   Temporal workflows, the environment guard, concurrency, and
   migrations are never delegated to Sonnet; pre-existing uncommitted
   changes in the tree belong to other work and stay untouched.
   *Engineering:* no compatibility shims — forge has no external
   consumers, so fix the seam in place and update all callers; activity
   timeout/retry presets come from `forge/presets.py` only, because the
   values are ScheduleActivityTask command attributes and changing one
   is replay-breaking; no inline copies of the step/gather/dispatch
   pipelines in the `forge/workflows/` drivers; new workflow tests pass
   `sync_mode=True` and script outcomes keyed by identity, never arrival
   order, with no module-level mutable containers (the pytest-xdist
   blocker T5.5 removed); read `~/.claude/python-guidelines.md` before
   writing or reviewing Python, and require the same of any spawned
   code-writer. *Working style:* mechanism-level explanations — the
   underlying cause, act-now versus act-later versus no-action, concrete
   command sequences, and never a safety caveat compressed into a bare
   clause; challenge weak arguments and spot missing data, do not
   praise. *Tool hazards:* `rg -n`, never `rg -rn` (`-rn` parses as
   `--replace n` and fabricates output); never start a wrapped `.md`
   prose line with `+`, because the formatter hook re-parses it as a
   list marker and rewrites the list; `gsed`, not `sed`, when
   stream-editing is unavoidable — prefer Edit.

## Where to start

- **T10.1 is IN PROGRESS, mid-ST1 — this is a resume, not a pickup.**
  Read order: the task file's Plan (six resolved decisions and the
  seven sub-tasks) → its Problem and Background → then, before acting,
  `~/repos-sax/sax-datastores/docs/product-migration.md` § "Current
  state (2026-07-28)" and § forge, which are newer than the Plan and
  authoritative on the external side. Pickup reconciliation is not
  optional here: two of the Plan's decisions are already superseded
  (item 3 above).
- **First move is a reconciliation, not a command.** Take the Plan's
  decisions 1 and 4 back to the owner: temporal now leaves forge's
  deploy entirely, the drain is separable and can precede the database
  move, and ST2's rehearsal target changes shape because a shared dev
  Temporal already runs on PG17. Whether T10.1 absorbs the temporal
  drain as its own sub-task or splits it into a sibling task is an owner
  call, and it changes the sub-task list.
- **What ST1 still actually needs**, given item 4's measurements — all
  of it on the sax-datastores side, all of it now via the registration
  flow rather than compose/`.env` edits: update
  `registrations/forge.yaml` (declare the MinIO bucket
  `saxcapital-forge-blobs-dev`, and pbook — first-class `pbook` slug per
  the Plan's decision, or the `forge_<env>:pbook_<env>` extra-DB groups),
  run the intake/validate path, apply it to the dev stack, record
  `applied: dev`, and run the auth-isolation probes. The eight
  compose/`.env.example` edits described in the pre-existing session
  notes are **obsolete** — that file shape no longer exists.
- **Access constraint, unchanged:** file tools in the forge session are
  sandboxed to the forge directory, so edits inside
  `~/repos-sax/sax-datastores` and `~/repos-sax/sax-temporal` cannot be
  made from here. The owner's three options stand — grant access, take a
  patch written into scratchpad, or hand-apply — and one of them has to
  be chosen before ST1 can close.
- **The GHCR image is still unpublished from CI.** `gh workflow list`
  shows `publish-postgres` **active**, and `gh run list` shows **zero
  runs** (the initial push missed the paths filter). A locally built
  `ghcr.io/stevegsax/sax-postgres-rum:17` exists and is what the running
  dev stack uses, so nothing is blocked today — but forge's CI
  testcontainers swap (Plan step 3) pulls from GHCR, so
  `gh workflow run publish-postgres.yml` has to succeed before that AC
  can pass. Note the seven unpushed sax-datastores commits: pushing them
  is what would trigger the workflow naturally.
- **Prod is untouched and stays that way until the rehearsal is done.**
  The prod stack has never been up (5442 free, no volumes), so its first
  boot will provision cleanly; the dev stack, by contrast, already has
  state and its own provisioning history. Prod cutover is ST6, behind
  the ST2 rehearsal, and it begins with the DEPLOYMENT.md in-flight
  check.
- **Invariant for the whole task:** zero workflow code, so the 16
  committed replay histories must pass **unregenerated** end to end. The
  one sanctioned forge code change is the environment guard's new
  URL-suffix coherence check — the database twin of
  `require_namespace_coherence`, exit 78 on mismatch — and the guard is
  explicitly never delegated to Sonnet.
- **After T10.1:** T5.7 (execution-time reference repair), then the
  owner walkthrough resumes at T6.1 before any Phase 6 work is planned.
- Normal pickup reconciliation still applies — verify the numbers above
  against the tree before planning.
