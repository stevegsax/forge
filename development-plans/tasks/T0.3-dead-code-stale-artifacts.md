# T0.3 — Dead code and stale artifact deletion

**Status:** DONE
**Phase:** 0 — Standalone (no phase dependency)
**Repos:** forge
**Depends on:** —
**Last updated:** 2026-07-18

> Extracted 2026-07-08 from [T8.4](T8.4-final-sweep.md)'s sweep inventory:
> verified-dead code and broken artifacts, delete-safe today.

## Problem

Four verified-dead items sit in the tree:

- `build_interaction_dict` (`store.py:166-209`) — zero callers (the
  persist path uses `build_persist_interaction`). Trap if resurrected: it
  emits no `idempotency_key`, so its rows would be NULL-keyed and
  `insert_or_ignore(index_elements=["idempotency_key"])` can never dedupe
  them (NULLs never conflict in SQLite or Postgres).
- `scripts/basenames.py` — queries the `ocr_results` table removed in the
  OCR split; broken since.
- `scripts/nushell/` — contains only a README documenting a nonexistent
  `ocr.nu` module and OCR commands that left with the split.
- `boto3` in forge's `pyproject.toml` (line 20) — never imported by forge
  source; forge-contracts needs and declares it independently.

## Scope

Delete all four; sweep for dangling references.

## Acceptance Criteria

- [ ] rg shows zero references to `build_interaction_dict`,
      `basenames.py`, `ocr.nu`, or a forge-level `boto3` import.
- [ ] Suite green; `uv sync` + worker boot clean without the `boto3`
      declaration.

## References

- [forge-review-2026-07-08.md](../../forge-review-2026-07-08.md) §4.8
  (findings F12/F35/F38)
- [archive/reports/code-review-2026-02-26.md](../../archive/reports/code-review-2026-02-26.md)
  triage stamp (2026-07-08)

## Plan

Grounding sweep (2026-07-18) re-verified all four items; two carry
re-decisions recorded below. Execute the deletions, then sweep for dangling
references.

1. **`build_interaction_dict`** — delete the function + its `store.py`
   docstring mention. It is used only by `tests/test_store.py`: directly in
   `TestBuildInteractionDict`, and as a row-builder helper inside the
   roundtrip/migration tests. Delete `TestBuildInteractionDict` (and the
   now-orphaned `_make_context`/`_make_llm_result` helpers); replace the
   helper usages in the *unrelated* roundtrip/migration tests with a small
   local `_interaction_row(**overrides)` factory so those tests keep
   working. Trim the now-unused `forge.models` imports.
2. **`scripts/basenames.py`** — delete.
3. **`scripts/nushell/`** — delete the forge-level directory (README-only,
   documents a nonexistent forge-level `ocr.nu`). Leave the real
   `apps/ocr/scripts/nushell/ocr.nu`.
4. **`boto3`** (re-decision) — *move* `"boto3>=1.34"` from root
   `[project].dependencies` to `[dependency-groups].dev` (runtime is
   transitive via sax-platform; forge's tests import boto3 directly for the
   moto S3 fixture). Then `uv sync --all-packages` and verify worker import
   - the forge store suite.

## Sub-tasks

- [x] Delete `build_interaction_dict` + docstring mention in `store.py`.
- [x] Rework `tests/test_store.py`: drop `TestBuildInteractionDict` +
      `_make_context`/`_make_llm_result`, add `_interaction_row`, trim
      imports.
- [x] Delete `scripts/basenames.py`.
- [x] Delete forge-level `scripts/nushell/`.
- [x] Move `boto3>=1.34` to `[dependency-groups].dev`; `uv sync
      --all-packages`.
- [x] Verify: `pytest tests/test_store.py` green; ruff clean; worker import
      OK; `rg` shows zero dangling references.

## Development Notes

**2026-07-18 — grounding verdicts and re-decisions.**

- Item 1 (`build_interaction_dict`): confirmed zero production callers (the
  persist path uses `build_persist_interaction` in `persist_models.py`).
  The function is used in `tests/test_store.py` far beyond its own test
  class — it is the row-builder for `TestInteractionRoundtrip`,
  `TestRunMigrations`, `TestMigration003`, and `TestStopReasonColumn`.
  Those are *unrelated* tests (roundtrip/migration coverage), so per the
  "keep unrelated tests" instruction they were preserved by swapping the
  `build_interaction_dict(...)` + `InteractionRow(**data)` pattern for a
  single-line `_interaction_row(...)` factory. Deleting the function also
  orphaned `_make_context`/`_make_llm_result` and six `forge.models`
  imports (`AssembledContext`, `ContextStats`, `FileOutput`,
  `LLMCallResult`, `LLMResponse`, `PlanCallResult`) — all removed to keep
  ruff green.
- Item 3 (`scripts/nushell/`): the task's original AC ("zero references to
  `ocr.nu`") is **stale as written** — the real `ocr.nu` legitimately lives
  at `apps/ocr/scripts/nushell/ocr.nu` and stays. The honest AC is zero
  *forge-level* references to `ocr.nu`; only the forge-level
  `scripts/nushell/` (a README describing a nonexistent forge-level module)
  was deleted.
- Item 4 (`boto3`) — **re-decision (orchestrator-approved).** The task said
  to *delete* forge's `boto3>=1.34` because forge-contracts declared it. But
  forge-contracts is deleted, and forge's `tests/conftest.py` now imports
  boto3 directly for the moto S3 fixture. Correct hygiene is to **move**
  `"boto3>=1.34"` from `[project].dependencies` to
  `[dependency-groups].dev`: runtime access stays transitive via
  sax-platform (which declares boto3), and the tests get it from the dev
  group. Straight deletion would have left the test import unprovisioned.
