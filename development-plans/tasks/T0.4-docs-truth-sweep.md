# T0.4 — Docs truth sweep (pre-migration)

**Status:** DONE
**Phase:** 0 — Standalone (no phase dependency)
**Repos:** forge (docs)
**Depends on:** —
**Last updated:** 2026-07-18

> Extracted 2026-07-08 from [T8.4](T8.4-final-sweep.md)'s sweep inventory
> plus [forge-review-2026-07-08.md](../../forge-review-2026-07-08.md)
> recommendation 2: the status-of-record should not stay wrong for the
> duration of the migration. Docs-only; independent of every phase.

## Problem

Live docs carry claims falsified by shipped code, and spent planning docs
sit in live locations:

- `docs/OVERVIEW.md:26,31` documents the dead `llm_providers/` stack as a
  live capability — contradicting the repo's own 2026-06 review.
- `docs/ARCHITECTURE.md` module map lists a `src/forge/ocr/` subtree (10
  files) that was deleted in the OCR split, and its OCR-subsystem section
  cites `forge ocr-jobs`, which no longer exists in `cli.py`.
- `TOC.md:15` says "D1–D85"; DECISIONS is at D97.
- `docs/operations/WORKERS.md:69` cites superseded D81 with no migration
  note.
- `development-plans/HANDOFF-architecture-review-2026-06-10-tasks.md` —
  converted one-for-one into the task files on 2026-06-10 and diverged
  since (no T5.6, no amendments); a fifth plan representation.
- `archive/to-merge/` — nothing is left to merge (code reviews mined
  2026-06-04; completion documents integrated); the name implies pending
  work.
- Completed plans at `development-plans/` top level
  (`separate-ocr-into-its-own-repo.md`, `HANDOFF-separate-ocr.md`,
  `externalize-store-postgres-s3.md`) — archive convention says these
  belong in `archive/`.
- `diataxis/` — disposable generated output describing the pre-migration
  system, still being lint-maintained.
- *(2026-07-08 sweep)* `docs/DECISIONS.md` D1 and D8 lack the supersession
  banners the log applies rigorously elsewhere: D1's "batch enables true
  parallelism" rationale is stale under D79 single-request batches (batch
  = cost per D76; parallelism comes from Temporal fan-out), and D8's
  "sequential execution" is amended by D69 recursive fan-out and D71
  conflict resolution (D74 still defers parallel task execution).

## Scope

- Correct OVERVIEW's provider-stack claims (mark `llm_providers/` dead,
  pending deletion in T1.1 — which finishes the job).
- Remove ARCHITECTURE.md's dead OCR module-map subtree and `forge
  ocr-jobs` reference.
- Fix TOC.md's DECISIONS range; add the D81 supersession note to
  WORKERS.md.
- Move the HANDOFF-tasks file to `archive/` with a dated header note;
  sweep the task files' "Task source" links to the new path or drop them.
- Rename `archive/to-merge/`; move the three completed plans into
  `archive/` with README table rows.
- Decide `diataxis/`: delete until regenerated post-migration, or freeze
  (stop lint-maintaining). Owner decision — record it in Development
  Notes. (Regeneration itself stays in T8.4.)

## Acceptance Criteria

- [ ] No live doc claims contradicted by code among the items listed
      above (spot-check each).
- [ ] rg shows no dangling links to moved/renamed files.
- [ ] markdownlint green.

## References

- [forge-review-2026-07-08.md](../../forge-review-2026-07-08.md) §3.3 and
  §7 recommendation 2; the 2026-07-08 obsolete-docs assessment (session
  record)

## Plan

A 2026-07-18 grounding sweep re-verified every item; several original
findings were already corrected by the 2026-07-17 truth pass and T3.5 (see
Development Notes — record, don't redo). Remaining work, per item:

- **c** — `TOC.md:19` "D1–D98" → "D1–D99".
- **d** — annotate the D81 citation in `WORKERS.md:79` as interim (the code
  is still accurate; the poller lives until T4.1), mirroring
  `ARCHITECTURE.md:341`'s "*(… interim: Phase 4, per D88, replaces …)*"
  style.
- **e** — move `HANDOFF-architecture-review-2026-06-10-tasks.md` into a new
  `development-plans/archive/` (with a README following the top-level
  `archive/README.md` table convention); sweep all links.
- **f** — rename `archive/to-merge/` → `archive/merged/`; update
  `archive/README.md` rows + live path references.
- **g** — move the three completed plans
  (`separate-ocr-into-its-own-repo.md`, `HANDOFF-separate-ocr.md`,
  `externalize-store-postgres-s3.md`) into `development-plans/archive/`;
  sweep links.
- **h** (owner-approved) — **delete `diataxis/`**; clean the live surface
  (CLAUDE.md section + reference, TOC, markdownlint + gitignore config).
- **i** — add supersession/amendment banners to D1 and D8 in `DECISIONS.md`.
- **Bonus** — `README.md:12` member list; `docs/requirements/README.md:128`
  row + delete `llm_providers.feature`; `docs/OVERVIEW.md:17` Phase-3-done.

## Sub-tasks

- [x] c: TOC.md DECISIONS range → D1–D99.
- [x] d: WORKERS.md D81 interim annotation.
- [x] e: create `development-plans/archive/` + README; move HANDOFF-tasks;
      sweep ~40 task-file links + docs links.
- [x] f: rename `archive/to-merge/` → `archive/merged/`; update README rows
      + TOC + OVERVIEW.
- [x] g: move three completed plans to `development-plans/archive/`; update
      TASKS.md links + intra-file refs.
- [x] h: delete `diataxis/`; clean CLAUDE.md, .markdownlint-cli2.jsonc, root
      .gitignore.
- [x] i: D1 + D8 amendment banners.
- [x] Bonus: README.md member list; requirements README row + feature
      deletion; OVERVIEW Phase-3 status.
- [x] Verify: markdownlint on edited files; dangling-link + diataxis sweeps.

## Development Notes

**2026-07-18 — grounding verdicts (already-fixed items, record only).**

- Item **a** (OVERVIEW `llm_providers/` provider stack): **already fixed**
  by the 2026-07-17 truth pass + T3.5. `docs/OVERVIEW.md` now describes
  `sax-llm` as deleted and Anthropic as the only chat backend (lines 34, 79);
  no live "dead provider stack documented as a capability" claim remains.
- Item **b** (ARCHITECTURE `src/forge/ocr/` subtree + `forge ocr-jobs`):
  **already fixed**. `rg "forge/ocr|ocr-jobs|src/forge/ocr"
  docs/ARCHITECTURE.md` returns nothing.

**2026-07-18 — re-decisions and scope notes.**

- Item **d**: the original AC implied D81 was superseded, but the shared
  poller is still live and accurate until Phase 4 (T4.1). So this is an
  *interim* annotation (Phase 4, per D88), not a supersession note — mirrors
  the existing `ARCHITECTURE.md:341` batch-transport wording.
- Items **e/g**: the development-plans-origin artifacts go to a new
  `development-plans/archive/` (created here, with its own README table),
  keeping the migration's history under `development-plans/`. The top-level
  `archive/` stays for the older phase/design docs. Only `archive/to-merge/`
  is touched there (item f rename).
- Item **f**: renamed to `archive/merged/` per the orchestrator's suggestion.
  Note: the name is a slight over-claim for `completion-documents/` (the
  README records it as "never integrated"); kept the accurate per-row
  "Superseded by" text so the table stays honest.
- Item **h** — **DELETE `diataxis/` (owner/orchestrator-approved).**
  Rationale: it is disposable generated output describing the pre-migration
  system (three phases stale), CLAUDE.md itself declares it never
  authoritative, and it is regenerable post-migration. Deleting it also
  retires ~half the repo's non-authoritative markdown that was being
  lint-maintained (a cost the 2026-07-08 review flagged). Cleaned the live
  surface: CLAUDE.md "Diataxis Documentation" section + the
  `learning-loops.md` reference on the pbook line; `.markdownlint-cli2.jsonc`
  ignore + comment; root `.gitignore` build-output patterns. Left historical
  mentions in `forge-review-2026-07-08.md`, `grill-me-sessions/`,
  `docs/reviews/`, `DECISIONS.md`, and `development-plans/` (same
  non-authoritative-record convention). `TOC.md` has no diataxis entries.
  **Out of boundary (recorded, not fixed):** `apps/ocr/.gitignore` and
  `apps/pbook/.gitignore` still carry dead `diataxis/` ignore patterns — the
  apps are off-limits this task; harmless leftover patterns.
- Item **Bonus** (`llm_providers.feature`): **deleted** the feature file and
  removed its `docs/requirements/README.md` table row (it mapped to the
  deleted `sax_llm` / `libs/sax-llm/`). Adjusted the status line "16 of these
  18 specs are implemented" → "15 of these 17" to keep the README internally
  consistent with the removed row.
- **Out-of-boundary dangling refs (recorded, not fixed):** moving the three
  completed plans (item g) leaves two plain-text (non-link) path references
  to `development-plans/separate-ocr-into-its-own-repo.md` in
  `apps/ocr/README.md:23` and
  `libs/sax-platform/src/sax_platform/contracts/s3_blobs.py:20`. Both are
  under the apps/libs hard boundary; neither is a markdown link, so
  markdownlint is unaffected. Left for the owning packages.
