# T10.1 — Migrate forge onto sax-datastores

**Status:** IN PROGRESS
**Phase:** 10 — Datastore consolidation
**Repos:** forge, sax-datastores
**Depends on:** nothing in the forge queue. External: the
`~/repos-sax/sax-datastores` repo (present; reviewed 2026-07-28) and
one host-side precondition (see Scope). Owner-sequenced to run
immediately — ahead of T5.7 and ahead of the sax-datastores guide's
recommended product order (annotation_manager → epub-reader → forge).
**Last updated:** 2026-07-28

> Added 2026-07-28 by owner decision, immediately after the
> sax-datastores review. The consolidation's design record is the
> external repo: `docs/rationale.md` there (every decision with
> mechanisms and rejected alternatives) and `docs/product-migration.md`
> §forge (the audited per-product specifics this task executes).

## Problem

Forge's data layer predates the fleet datastore consolidation and
carries five defects the sax-datastores design retires:

1. **Accidental topology.** One PG16 instance
   (`pgvector/pgvector:pg16`, host port 5434 via a gitignored override;
   compose default 5433 because sibling projects squat 5432/5433)
   hosts production (`forge`, `pbook`), the dev lane (`forge_dev`), and
   Temporal's persistence (`temporal`, `temporal_visibility`) side by
   side — environments are separated by nothing but database name.
2. **The app role is the cluster superuser** (`POSTGRES_USER=forge`
   via the image entrypoint) — every worker connection could drop any
   database on the instance, including Temporal's.
3. **Dev is not topology-parity with prod.** pbook's dev store shares
   the `forge_dev` database (`deploy/local-stack/README.md:129`) while
   prod separates `forge` and `pbook` — so dev never rehearses the
   real two-database layout.
4. **Temporal's databases are never backed up.** The nightly leg
   (`backup-app-dbs.sh`, launchd `db-backup` agent) dumps forge+pbook
   only, and it is verified-once/unmonitored — plan-review finding A7,
   still open.
5. **Extension surface is repo-local** (initdb SQL) rather than
   parity-by-construction, the failure class that forced another
   product's search rewrite.

sax-datastores replaces this class fleet-wide: **environment =
instance** (dev postgres :5432 + MinIO; prod postgres :5442, no MinIO,
S3 only), **project = role + database** (`forge_dev`/`forge_test`/
`forge_prod` roles own same-named databases with PUBLIC connect
revoked, so a wrong-project or wrong-environment URL fails at auth),
one canonical PG17+rum+pgvector image everywhere, fail-closed
provisioning, and a nightly backup with a scheduled restore proof.

## Background and Detailed Explanation

sax-datastores is the shared datastore infrastructure repo for all SAX
projects: two podman compose stacks (dev and prod), an idempotent
provisioning script that creates per-project roles and databases with
the load-bearing `REVOKE CONNECT … FROM PUBLIC`, one canonical
postgres image, and backup/restore-check scripts. Forge is the largest
migrant: it brings four production databases, a running Temporal
server whose persistence lives in the instance being replaced, and
launchd-supervised workers that must find their databases up at boot.

What forge gets from the move, concretely: environment separation
that fails closed (a dev URL cannot reach prod — different port,
role absent); the superuser demotion (the forge role owns its own
databases and nothing else — extensions are pre-installed by
provisioning as superuser, so the Alembic chains'
`CREATE EXTENSION IF NOT EXISTS` lines become no-ops, which is the
compensation that makes demotion safe); dev/prod topology parity for
pbook for the first time; Temporal's databases inside the nightly
backup for the first time (databases are enumerated from the live
instance, so nothing rides on a manifest); and **A7's mechanism
closed** — `make prod-restore-check` proves the newest dumps restore
and fails loudly when they are older than 48h, which is the alarm for
a silently dead backup job that forge's current leg never had.

The three risky seams, named up front. **Temporal's persistence
moves**: the temporal server container stays in forge's deploy but
its databases relocate to the shared prod instance, which means a
planned downtime window (workers and temporal down, dump/restore,
repoint, up) and cross-compose networking — the temporal container
reaches the shared postgres through the rootless-podman gateway path,
which the stack's pg_hba RFC1918 rows already anticipate; the exact
mechanism is a Plan decision. **Boot ordering**: sax-datastores has no
boot wiring of its own, and forge's workers boot via launchd expecting
the database up — stack-boot ownership must be decided, not left to
`restart: unless-stopped`. **PG16 → PG17**: the move is dump/restore
per database plus `REINDEX` of pbook's vector indexes, rehearsed on
the dev stack before prod cutover.

What this task deliberately does not change: **zero workflow code**
(the committed replay histories must pass unregenerated — this is
infrastructure, and the one forge code change is the environment
guard's new coherence check); the D103 pinned-checkout deploy model;
and the loopback-only network posture — tailnet exposure of the dev
instance exists in the sax-datastores design but is deferred here, so
D99's no-remote-access stance is unchanged by this task. Retiring
forge's own backup leg amends shipped decisions (D102's backup design,
D99/D102's stack composition), so the change-set mints a new decision
record with banners per the Specification Changes policy.

## Scope

**Precondition (owner act, before the dev stack can bind :5432):**
stop the legacy annotation_manager containers currently holding
5432/5433/9000 (volumes kept; that product migrates later from its own
repo). The prod stack (:5442) conflicts with nothing and could go up
first, but the PG16→17 rehearsal wants the dev stack up before prod
cutover, so freeing the ports is step one. The alternative — dev stack
on a temporary port — is rejected: it forfeits the default-port-gravity
design and bakes a second port story into every consumer.

**sax-datastores side:**

- `pbook` and `forge_temporal` become first-class projects in
  `PROVISION_PROJECTS` with password lines in both stacks' `.env` and
  compose `environment:` blocks; `PROVISION_EXTRA_DBS` attaches the
  visibility database per env
  (`forge_temporal_<env>:forge_temporal_visibility_<env>`).
- Forge's dev bucket names join `MINIO_BUCKETS` (dev object data is
  re-derivable and is not migrated).

**forge side:**

- Profiles (`~/.config/forge/envs/{prod,dev}.env`) and the temporal
  server's environment repoint at the new roles/DBs/ports.
- `deploy/local-stack/` shrinks to Temporal(+UI) only: the postgres
  and minio services, the initdb SQL, and `FORGE_PG_PORT` are deleted;
  the temporal server points at the shared instance.
- Boot wiring: the shared stacks come up before the workers at boot
  (extend `start-stack.sh`/launchd or equivalent — Plan decision).
- The environment guard gains **URL-suffix coherence**: `FORGE_ENV`
  must match the connection URL's role/database suffix before any
  connect, exit 78 on mismatch — the database twin of
  `require_namespace_coherence`.
- Backup retirement: the launchd `db-backup` agent,
  `backup-app-dbs.sh`, and the `deploy/s3` lifecycle leg are retired
  in favor of the shared nightly + restore-check; new decision record
  - banners on D102 (and D99 as touched), same change-set.
- CI: the postgres-marker testcontainers image moves to the canonical
  `sax-postgres-rum:17` — a recorded deviation from the guide's
  shared-`_test`-database step (testcontainers is stronger isolation
  and matches the stack's own CI philosophy).
- Docs rewritten in the same change-set: DEPLOYMENT.md,
  deploy/local-stack/README.md, WORKERS.md, CLAUDE.md "Running the
  System".

**Prod cutover (runbook shape; detail in the Plan):** stacks up +
provisioned → pre-cutover backup of the old instance → in-flight
workflow check (the DEPLOYMENT.md pre-deploy procedure — prefer a
drained queue) → workers + temporal down → dump/restore ×4 (`forge`,
`pbook`, `temporal`, `temporal_visibility`) with exact row-count
verification (the D102 precedent) → `REINDEX` pbook's vector indexes →
repoint + up → verify pollers/tracker/dev-lane run → shared nightly +
restore-check proven → old instance's volume kept for a decided
retention window, then removed.

**Decisions to settle in the Plan (each recorded with rationale):**
temporal-to-shared-postgres networking mechanism; stack-boot
ownership; pbook first-class vs extra-DB-under-forge (lean:
first-class, per the guide — it is a product); dev-lane data (lean:
re-seed `forge_dev`/`pbook_dev` rather than dump/restore — dev data is
disposable); verify the deployed temporal server version supports
PG17 before cutover; the new decision record's scope (which decisions
it amends).

**Out of scope:** tailnet exposure of either instance; adopting
`product-ci.yml` (forge CI keeps its own workflow); splitting ocr into
its own database slug — **ocr rides the forge database by design**
(D102); this task documents that in the sax-datastores guide rather
than "fixing" it; the other products' migrations (their own repos).

### Amendments (2026-07-30 — owner rulings + reality reconciliation)

Two owner rulings and two days of external landings reshape the task;
the Plan's original decisions 1, 2, and 4 are superseded and the
Acceptance Criteria below are restated in full (the originals assumed
a data migration that no longer happens).

- **Ruling: the legacy prod data is junk — discard it.** No
  dump/restore, no row-count verification, no `REINDEX` of migrated
  data, no PG16→17 data rehearsal. The cutover creates **fresh
  schemas on the empty PG17 databases** via the three migration chains
  (forge's Alembic + ocr's `alembic_version_ocr` into `forge_prod`;
  pbook's `pbk_alembic_version` into `pbook_prod`). What is discarded
  with the old instance: run/interaction (spend) history, the
  `batch_jobs` ledger, stored ocr results, and pbook entries — all
  ruled disposable. The old data dir is kept only until the cutover is
  verified, then removed on the owner's word (the 14-day window loses
  its purpose).
- **Ruling: pbook is its own product** — first-class slug with its own
  credential and blast radius, matching the temporal registration's
  rationale. Registered via its own `datastore-registration.yaml`.
- **The registration flow replaces all sax-datastores edits** (the
  2026-07-28 access-path question is moot): a product commits
  `datastore-registration.yaml` in its **own** repo — the commit hash
  is the request — and the admin applies it with the
  `register-project` skill. Forge authors two records: an updated
  forge registration at the repo root (declares
  `saxcapital-forge-blobs-dev`; formalizes the grandfathered
  2026-07-28 record) and pbook's at `apps/pbook/` (monorepo placement
  noted in the file; the approval record's `file:` field carries the
  path).
- **Plan decision supersessions:** decision 1 (temporal→postgres
  networking) and decision 4 (`forge_temporal` on both stacks) died
  with the org-wide Temporal service — `sax-temporal` runs per-env
  servers, both **live** (prod `:7243`, dev `:7236`, interim ports;
  dev reclaims 7233/8233 after forge's legacy server retires), with
  the `forge` namespace already existing (empty, and recreated as
  `forge-dev` at the drain — it is not the name forge ends up on);
  decision 2 (boot ownership)
  is superseded — sax-datastores boots itself (LaunchDaemon +
  `podman-restart.service`, installed 2026-07-29); decision 3 (pbook
  first-class) is now owner-ratified; decision 5 (dev re-seed) is
  subsumed by the junk ruling; decision 6 (D104) stands with its
  timing moved to the cutover change-set.
- **The temporal move is a drain, and forge's staging-lane model
  changes shape:** isolation moves from one-server/two-namespaces
  (`default`/`forge-dev` on forge's legacy server) to **two servers,
  each with its own per-env namespace** — `forge-dev` on the dev
  server, `forge-prod` on prod, per the org `<slug>-<env>` convention
  (`sax-temporal/docs/namespaces.md`, decided 2026-07-30).

  *Amended 2026-07-30 — this bullet previously said one namespace named
  `forge` on both servers.* Identical names across servers make an
  address mistake silent: a dev-configured process aimed at prod finds
  a namespace of the same name and polls production's queues. Distinct
  names fail closed instead — the wrong server does not have the
  namespace. The bare slug `forge` and `default` are retired and must
  not exist on any server; the empty `forge` namespace already created
  on `:7236` is recreated as `forge-dev`. Note `default` does not exist
  on `:7243` and was deliberately never created, so the current prod
  rule is unsatisfiable there regardless.

  Rather than validate an address+namespace pair, both values are
  **derived** from `FORGE_ENV` plus the product slug, and
  `FORGE_TEMPORAL_NAMESPACE` is deleted — there is no free-form
  variable left to set wrong. `require_namespace_coherence` becomes
  that derivation. The suffix convention is the backstop for
  everything outside the code path (the `temporal` CLI, one-off
  scripts, an operator in a shell), where no validation runs at all.
  The guard and the deployed profiles must change in one commit, or
  every worker fails closed at startup (exit 78).

  The drain shares nothing with the database move and may precede it
  (both servers run concurrently while legacy in-flight work
  finishes).
- **`deploy/local-stack` is deleted outright**, not shrunk: with
  temporal org-owned and postgres/minio shared, nothing remains.
  `start-stack.sh` and the `forge-stack` launchd agent retire with it.
- **Sequencing corrections:** ST5's remainder (backup LaunchDaemon
  installed + one observed scheduled run — owner acts) still gates
  the cutover per sax-datastores rationale §19; forge's own
  `db-backup` agent retires **in the cutover change-set**, not
  before — until the profiles leave `:5434` it guards nothing that
  matters (junk), but retiring early buys nothing and the agent dies
  with the local-stack deletion anyway.
- **External state anchors** (derived 2026-07-30): prod stack live
  with `forge_prod` provisioned and empty; backup bucket + both IAM
  credentials wired, conditional-write enforced, end-to-end
  backup + restore-check **passed 2026-07-30**; GHCR
  `sax-postgres-rum:17` published 2026-07-29 (manual dispatch;
  weekly cron maintains it). sax-datastores had 10 unpushed commits
  at derivation — flagged to the owner.

## Acceptance Criteria

*(Restated in full 2026-07-30 — see Amendments; the originals assumed
a data migration the junk ruling removed.)*

- [ ] Registrations approved and applied: forge's updated record
  (bucket declared) and pbook's first-class record — `pbook_dev`,
  `pbook_test`, `pbook_prod` roles/databases exist and
  `saxcapital-forge-blobs-dev` exists in dev MinIO.
- [ ] Temporal drain complete: every forge worker and workflow start
  uses the shared per-env servers (prod `:7243` namespace
  `forge-prod`, dev `:7236` namespace `forge-dev`), with no bare-slug
  `forge` or `default` namespace left on either; legacy
  `forge-temporal`/`forge-temporal-ui` removed; the sax-datastores
  admin told (dev Temporal reclaims 7233/8233);
  `require_namespace_coherence` derives both the address and the
  namespace from `FORGE_ENV`, and `FORGE_TEMPORAL_NAMESPACE` is gone.
- [ ] Store cutover: prod profile points `FORGE_DB_URL` at
  `forge_prod@:5442` and `PBOOK_DATABASE_URL` at `pbook_prod@:5442`
  (dev at `forge_dev`/`pbook_dev`@:5432); schemas created fresh by
  the three migration chains on the canonical PG17 image; the legacy
  `:5434` data discarded per the 2026-07-30 ruling — no dump/restore.
- [ ] Guard coherence enforced: a `FORGE_ENV` that contradicts the
  URL's role/database suffix exits 78 before any connect; unit-tested.
- [ ] Isolation proven at auth: `psql` as `forge_prod` reaches only
  its own databases; a non-forge role is refused at CONNECT by every
  forge database (and vice versa).
- [ ] All four prod pollers report `prod-<role>@<sha>` (no `-dirty`)
  after cutover, polling through the shared prod Temporal;
  `ocr tracker-status` exits 0; one dev-lane document run completes
  end to end.
- [ ] Boot proof: after a host reboot, the shared stacks and
  sax-temporal come up with no forge involvement, no forge-owned
  containers remain, and the workers find their services.
- [ ] Backups: the sax-datastores nightly LaunchDaemon is installed
  with one observed scheduled run (owner acts; gates the cutover per
  rationale §19); forge's `db-backup` agent, `backup-app-dbs.sh`, and
  the `deploy/s3` leg retire in the cutover change-set with the new
  decision record + banners on D102/D99.
- [ ] `deploy/local-stack` is deleted entirely (compose, initdb SQL,
  `FORGE_PG_PORT`, `start-stack.sh`, the `forge-stack` launchd agent,
  the stack make targets); CI's testcontainers run the canonical
  image.
- [ ] Zero workflow-code changes: committed replay histories pass
  unregenerated; `make gates` green.
- [ ] Docs truth: DEPLOYMENT.md, WORKERS.md, and CLAUDE.md "Running
  the System" describe the new layout in the same change-set
  (deploy/local-stack/README.md dies with its directory).

## References

- Design + audit (external, authoritative):
  `~/repos-sax/sax-datastores/docs/rationale.md`,
  `…/docs/product-migration.md` §forge, `…/docs/backups.md`,
  `…/scripts/provision-roles.sh`.
- Forge's current layout: `deploy/local-stack/compose.yaml` (+ initdb
  SQL), `deploy/launchd/` (`db-backup`, `start-stack.sh`),
  `deploy/s3/README.md`, `docs/operations/DEPLOYMENT.md`,
  `docs/operations/WORKERS.md`.
- Decisions touched: D99 (local-first deployment), D102 (local store
  of record + guard + nightly backup), D103 (pinned prod checkout —
  unchanged, interacts at cutover).
- Open finding closed: A7 (nightly backup verified-once/unmonitored —
  the 2026-07-27 plan review's blocking-soon operational item).

## Plan

*(Amended 2026-07-30 — see Scope § Amendments. Decisions 1, 2, and 4
below are superseded, 3 is owner-ratified, 5 is subsumed by the
junk-data ruling, 6 stands with cutover timing; steps 2 and 6 are
replaced by the fresh-schema rehearsal and the simplified cutover in
the Amendments. Do not execute this section without reading them.)*

Written 2026-07-28 at pickup. Reconciliation: the file is hours old and
its premises were re-verified from the tree today (temporal 1.25.2
reaches postgres as `POSTGRES_SEEDS: postgres` in-pod with literal
`forge:forge` credentials; the guard lives in `sax_platform/config.py`;
prod is on `d6af5be` with both namespaces drained — the T5.6 deploy
landed this morning, so the cutover inherits a quiet, current system).

**1. The six decisions, resolved:**

- **Temporal networking:** the temporal container reaches the shared
  prod instance as `forge_temporal_prod` at
  `host.containers.internal:5442` (podman's host alias; the stack's
  pg_hba RFC1918 rows exist for exactly this gateway-source shape).
  Rejected: joining the sax-datastores compose network — cross-project
  compose coupling. The `SQL_TLS_ENABLED: "false"` comment is rewritten
  honestly: traffic now crosses pod → host loopback, still on-box.
  Literal `forge:forge` credentials retire; the password comes from the
  stack's `.env` registry via the compose environment.
- **Boot ownership:** forge's `start-stack.sh` (the launchd RunAtLoad
  entry) gains `make -C ~/repos-sax/sax-datastores prod-up` and
  `dev-up` before forge's own `stack-up` — one boot owner on this
  host, ordering guaranteed; revisit when another product needs the
  stacks at boot. Temporal's `depends_on: [postgres]` is deleted;
  auto-setup's connect retry plus script ordering covers the wait.
- **pbook is first-class** (`pbook_{dev,test,prod}` role + database):
  it is a product, its worker already carries its own URL var, and dev
  gains prod-parity topology for the first time.
- **forge_temporal provisions on both stacks**; the dev pair's standing
  purpose is rehearsal — one temporal server is the D99 reality and dev
  isolation stays namespace-based (WORKERS.md), so the dev pair hosts
  no live server after ST2.
- **Dev-lane data: re-seed** (empty `forge_dev`/`pbook_dev` + alembic
  upgrade). Dev data is disposable and the old bind-mount dir is the
  retention copy regardless.
- **D104 — fleet datastore consolidation as adopted by forge:**
  supersedes D102's backup leg and its beside-Temporal topology
  sentence, amends D99's stack composition. Banners in the same
  change-set (Specification Changes policy).

**2. Rehearsal before any prod act** — also the PG16→17 and
temporal-on-PG17 verification: dump the four prod databases, restore
into the dev instance (`forge_test` as scratch + the
`forge_temporal_dev` pair), `REINDEX DATABASE` each (pgvector indexes
plus the ICU/collation hazard of a major bump), boot a throwaway
auto-setup container against the dev pair and run
`temporal workflow list` through it, and record timings — the cutover
window estimate comes from here.

**3. Forge change-set A — code + config (one commit; the dev lane
swaps with it).** Guard URL-suffix coherence: a pure checker in
`sax_platform/config.py` (the URL's role/database suffix must equal
`FORGE_ENV`; mismatch exits 78; unit-tested — the database twin of
`require_namespace_coherence`). Local-stack compose shrinks to
temporal + UI (postgres, minio, minio-init, initdb SQL, and
`FORGE_PG_PORT` deleted; temporal env repointed per decision 1;
`SKIP_SCHEMA_SETUP=true` once the first boot against restored schemas
succeeds). `start-stack.sh` extension; Makefile stack targets
adjusted; CI and the testcontainers image move to the canonical
`sax-postgres-rum:17` (precondition: sax-datastores pushed to GitHub
so GHCR publishes the image). **The dev tmux worker execs the live
tree**, so the dev profile swap (`~/.config/forge/envs/dev.env` →
`forge_dev@5432`, `pbook_dev`, shared MinIO :9000) happens in the same
act, and the dev worker restart is the guard's first live proof.
Invariant: zero workflow code; the replay suite stays green untouched.

**4. Forge change-set B — backup retirement + records:** delete the
`db-backup` launchd agent, `backup-app-dbs.sh`, and the `deploy/s3`
leg (existing dump objects age out under the old lifecycle rule); D104
plus banners; DEPLOYMENT.md, deploy/local-stack/README.md, and
WORKERS.md rewritten to the new layout.

**5. Backup wiring (owner acts):** `pg/` prefix + 30-day lifecycle rule
in the backup bucket; the two IAM credentials (PutObject-only writer on
the prod host; Get+List reader); `BACKUP_S3_URI` in the prod stack
`.env`; the nightly scheduled as a launchd agent invoking
`make prod-backup` (house pattern; the stack accepts any scheduler).
The gating `prod-restore-check` pass happens post-cutover, when the
databases are real.

**6. Prod cutover (the window; rehearsed timings decide its length):**
in-flight check on both namespaces → workers + temporal + old stack
down → final dump ×4 → restore into :5442 as the new owners
(`--no-owner --role=<owner>`), exact row-count verification (the D102
precedent), `REINDEX DATABASE` ×4 → prod profile swap
(`~/.config/forge/envs/prod.env`: `forge_prod@5442`,
`pbook_prod@5442`) → `make prod-deploy REF=<cutover sha>` — the one
deploy that deliberately bundles config with infrastructure, because
the config *is* the migration → stack + workers up → verify: four
pollers at the SHA, `tracker-status` 0, a dev-lane document run, the
auth-isolation probes → `make prod-backup` by hand +
`make prod-restore-check` (the production-ready gate; closes A7).

**7. Rollback stance and retention:** until the retention window
closes, rollback is: stop the new stacks, `make prod-deploy
REF=<pre-cutover sha>` (the pin makes this exact), restore the old
profiles — the old data dir (`${XDG_DATA_HOME}/forge/postgres`) is
untouched by the whole migration and is kept **14 days** post-cutover,
then removed by hand.

## Sub-tasks

*(Rewritten 2026-07-30 per the Amendments.)*

- [ ] ST1 — author + commit the two registrations (forge root:
  bucket; `apps/pbook/`: first-class pbook); *(owner)* apply via
  `register-project`; verify `pbook_{dev,test,prod}` + the bucket
  exist; auth-isolation probes recorded
- [x] ST2 *(forge + ocr; pbook pending)* — fresh-schema rehearsal: all three migration chains run
  clean against the empty dev-stack databases on the canonical PG17
  image
- [ ] ST3 — change-set A. **Temporal half landed 2026-07-30 as
  `a7a1f0b`** (gates as recorded at that commit: `make gates` EXIT=0 —
  forge 1538 / pbook 376 / ocr 238 / sax-platform 530, replay 17
  unregenerated):
  `require_namespace_coherence` deleted and replaced by
  `resolve_temporal_target` / `temporal_namespace_for`, returning a
  frozen `TemporalTarget(address, namespace)` derived from `FORGE_ENV`
  - slug (`<slug>-<env>`) — a derivation, not a pair check, so an
  incoherent pairing is unconstructible; `TemporalSettings.namespace`
  and `FORGE_TEMPORAL_NAMESPACE` deleted; `TemporalSettings.address`
  now an override defaulting to `None`, refused for dev/prod unless it
  restates that environment's server; `TEMPORAL_NAMESPACE` constant
  replaced by `PRODUCT_SLUG`; `connect_temporal(namespace=...)`
  required; all six connect sites and every `--temporal-address`
  default updated; env examples and `deploy/prod-ocr` updated.
  **Still owed:** guard URL-suffix coherence for the DB URLs; profiles
  gain per-env DB URLs; CI/testcontainers image swap. (The dev-lane
  swap and dev-worker proof are done — `dev-forge-worker@a7a1f0b`
  polls `:7236` ns `forge-dev`.)
  **Ordering constraint, derived 2026-07-30 after the commit: the
  DB-URL suffix guard cannot land before ST6.** It requires the URL's
  database to carry the `_<env>` suffix, and the live profiles still
  point at the legacy unsuffixed `forge` / `pbook` / `forge_dev`
  databases on `:5434`; landing the guard first would exit 78 on every
  process. It therefore belongs to the **cutover change-set**, beside
  the profile DB-URL swap it validates. Only the CI/testcontainers
  image swap is independently landable from this sub-task's remainder.
  **The "fails closed" note written into this line at commit time is
  itself now false — see the Development Note of 2026-07-30 (deploy
  hazard inverted).**
- [x] ST4 **(done 2026-07-31 — no drain was needed; the legacy server was discarded)** — temporal drain: recreate the empty `forge` namespace on
  `:7236` as `forge-dev` (and `pbook-dev`); create `forge-prod` on
  `:7243`; dev workers → `:7236` ns `forge-dev`, prod workers →
  `:7243` ns `forge-prod` (both servers concurrent while legacy
  in-flight work finishes); confirm no `forge` or `default` namespace
  remains on either new server; legacy `forge-temporal`(+UI) removed
  when drained; admin told re 7233/8233 — separable, may precede ST6
- [ ] ST5 — *(owner)* backup LaunchDaemon installed + one observed
  scheduled run in the log — gates ST6
- [x] ST6 **(substantially done 2026-07-31 — see the execution note; pbook and the repo-side deletions remain)** — store cutover: in-flight check → workers down → prod +
  dev profile swap to `:5442`/`:5432` → `make prod-deploy
  REF=<cutover sha>` (the change-set that also deletes
  `deploy/local-stack`, retires the backup leg, lands D104 + banners
  - docs) → migration chains create fresh schemas → workers up →
  verify per ACs
- [ ] ST7 — reboot test; legacy stack removed and old data dir
  deleted on the owner's word; close-out per PROCESS.md + handoff
  sweep

## Development Notes

**2026-07-31 — the rebuild executed; forge runs on the shared stacks.**
Both lanes are live and verified. Prod: `prod-forge-worker-1@a7a1f0b`,
`prod-forge-worker-2@a7a1f0b`, `prod-ocr-worker@a7a1f0b` (no `-dirty`)
polling `:7243` ns `forge-prod`; `forge_prod` on `:5442` holds **11
tables** created by the forge and ocr Alembic chains at worker startup;
`ocr tracker-status` → `status: fresh`, first cycle `04:00:00Z`,
`cycles_total: 1`. Dev: `dev-forge-worker` on `:7236` ns `forge-dev`,
`forge_dev` on `:5432` with 5 tables. Profiles now carry
`forge_prod@localhost:5442/forge_prod` and
`forge_dev@localhost:5432/forge_dev` (dev MinIO endpoint → `:9000`),
backed up as `~/.config/forge/envs/*.bak-20260731T035535Z`. Connections
were proven per lane before any worker started (right role, right
database, 0 tables at that point).

There was **no cutover and no drain** — the legacy stack was already
gone, so this was a rebuild onto empty databases: profiles → prune the
stale worktree registration → `make prod-deploy` → start workers →
migrations create the schemas at startup. `ST2`'s rehearsal happened
on the dev lane as part of it.

**Two blockers cost real time; record them so they are not
rediscovered.**

1. **`launchctl disable` is invisible to every error message you get.**
   The teardown disabled all five agent labels — a persistent per-user
   override stored outside the plists — so `install.sh` and a manual
   `launchctl bootstrap` both failed with
   `Bootstrap failed: 5: Input/output error`, with valid plists and
   correct paths. Nothing in that message names the cause;
   `launchctl print-disabled gui/$UID` is what reveals it, and
   `launchctl enable gui/$UID/<label>` is the fix. Expect this after
   any teardown that disabled rather than merely unloaded.
2. **`install.sh` would have resurrected the discarded stack.** Its
   agent list is hardcoded `(forge-stack forge-worker-1
   forge-worker-2)` and `forge-stack` carries `RunAtLoad=true` →
   `start-stack.sh` → `make stack-up`, which at `a7a1f0b` still finds
   `deploy/local-stack/compose.yaml` and would recreate
   forge-postgres/minio/temporal. The three worker plists were
   therefore bootstrapped **directly**, leaving `forge-stack` disabled.
   Until `deploy/local-stack/` and that agent are deleted together,
   `install.sh` is not safe to run — the deletion change-set must also
   drop `forge-stack` from that array.

Also done: `com.saxcapital.db-backup` was booted out and disabled — it
dumps the `forge`/`pbook` databases out of the deleted `forge-postgres`
container, so it had been failing nightly against nothing. Its formal
retirement (plist, script, `deploy/s3` leg, D104 + banners) still
belongs to the repo-side change-set.

**Deliberately not done, with reasons.** *pbook stays down*: its
`PBOOK_DATABASE_URL` still points at the dead `:5434` because
sax-datastores flags pbook's three passwords for rotation (they were
pasted into a session transcript) and argues rotation is cheap "while
nothing consumes them" — wiring them into forge's profiles is exactly
what makes it expensive. After rotation: write both pbook lines,
`launchctl enable` + bootstrap `com.saxcapital.pbook-worker`. Forge's
transcript ingestion has no server on `pbook-task-queue` until then.
*Dev Temporal stays on `:7236`*: the flip to 7233 is free of the old
port conflict but coupled to `_TEMPORAL_ADDRESSES` in
`sax_platform/config.py` — compose and that table must move together.

**Still owed for close-out:** the DB-URL suffix guard (now unblocked —
both lanes' databases carry `_<env>` suffixes, so the guard is true on
arrival), deleting `deploy/local-stack/` + the `forge-stack` agent +
the backup leg, D104 with banners on D99/D102, the docs rewrite
(DEPLOYMENT.md, WORKERS.md, CLAUDE.md "Running the System"), the CI
testcontainers image swap, pbook, and the reboot test.

**2026-07-30 (later) — the deploy hazard inverted; the drain window is
the thing to watch.** `a7a1f0b`'s own text says a deploy ahead of ST4
"fails every worker closed (exit 78)" because the profiles still set
the retired `FORGE_TEMPORAL_*` variables. The profiles were edited
minutes after that commit and **no longer set them**, verified:
`rg -n "FORGE_TEMPORAL_ADDRESS|FORGE_TEMPORAL_NAMESPACE"` matches
nothing in either `~/.config/forge/envs/{prod,dev}.env`. With no
override, `resolve_temporal_target` derives prod to `:7243` ns
`forge-prod` — **which exists** (verified by
`temporal operator namespace list --address 127.0.0.1:7243`). So
`make prod-deploy REF=<any commit ≥ a7a1f0b>` no longer fails closed:
it succeeds and silently performs a partial ST4 — the four prod
workers move to the shared server, the `ocr-batch-tracker` Schedule is
installed into `forge-prod` while its twin lingers unpolled in
`default`, and anything in flight on `:7233` strands. It is not a
data-loss shape (`FORGE_DB_URL` still points at the legacy `:5434`
store, which is correct until ST6), but it is an unplanned lane change.

Severity is **time-dependent, not currently acute**: at derivation the
legacy `default` namespace had **zero running workflows**, so today the
stranding cost is nil — which is exactly what makes now a good drain
window. That condition is perishable: a batch-lane run holds a
workflow open for up to 25 hours, so an empty queue is a snapshot, not
a property. Re-run the DEPLOYMENT.md in-flight check immediately
before either acting on the drain or deploying.

Also derived: the stale bare `forge` namespace still exists on `:7236`
alongside `forge-dev` (ST4 requires its removal); `:7243` is clean
(`temporal-system` + `forge-prod`).

**2026-07-30 — reality reconciliation + two rulings.** Re-derivation
of both external repos and the live system found the world two days
ahead of this file: the prod stack live (`forge_prod` provisioned,
empty), `sax-temporal-prod` live on `:7243` with schema installed,
the backup path proven end to end (2026-07-30), GHCR published
(2026-07-29, manual dispatch — the initial branch push cannot match a
paths filter, so the bootstrap had to be explicit; the weekly cron
maintains it from here). The owner ruled the legacy prod data junk
(discard; no data migration) and ratified pbook as a first-class
product. The Amendments section carries the full reshaping; the two
registration files were authored the same day (forge root +
`apps/pbook/`) and await commit + admin apply. External risk flagged
to the owner, not forge's to fix: sax-datastores sat at 10 unpushed
commits at derivation — the only copy of its prod-live/backup-proof
records was this machine.
