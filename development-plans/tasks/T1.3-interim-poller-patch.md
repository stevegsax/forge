# T1.3 — INTERIM minimal poller patch

**Status:** DONE
**Phase:** 1 — Stop the bleeding (current repos)
**Repos:** forge, forge-contracts (enum member — needs T1.0)
**Depends on:** [T1.0](T1.0-uniform-editable-sibling-sources.md)
**Last updated:** 2026-07-08

> **INTERIM:** The whole poller subsystem is deleted in Phase 4
> ([T4.1](T4.1-forge-timer-loop-transport.md)/[T4.2](T4.2-ocr-own-polling-gather-restructure.md)).
> Do not extend it beyond the scope below — do not "finish" it.

## Problem

**Critical, live:** transient signal-delivery failure permanently marks the
`batch_jobs` row FAILED while the provider still holds the paid result
(batch_poll.py:178-197); rows >24h go MISSING with **no signal**, so the
waiter burns the full 25h timeout (`_BATCH_WAIT_TIMEOUT`,
workflow_blocks.py:59).

## Background and Detailed Explanation

Like T1.2, this was an explicitly interim patch on machinery already
scheduled for deletion — the shared batch poller, a background workflow
that polled the provider and signalled results to waiters. It had three
live defects that all wasted money or time. First, a transient failure
to *deliver* the result signal permanently marked the `batch_jobs` row
FAILED even though the provider still held the paid-for result — a lost
result on a healthy batch. Second, a row that aged past 24 hours was
marked MISSING with no signal at all, so the waiting workflow sat out its
entire 25-hour timeout before failing. Third (found in the capture
sweep), one errored job inside a cycle made the poller `raise`, and with
no retry cap and an overlap=SKIP schedule that one bad batch turned the
poller into a retry loop that starved every following run.

The fixes were deliberately minimal, because the whole subsystem dies in
Phase 4: on a delivery failure, leave the row SUBMITTED so the next cycle
re-polls and re-delivers (safe precisely because T1.2 made re-delivery a
no-op); on MISSING, send the waiter an error-payload signal so it fails
fast instead of burning 25 hours; and stop raising on partial per-job
errors — report them and let the next scheduled run be the retry — while
giving the schedule an explicit execution timeout and overlap policy so a
wedged run is terminated. A notable non-change: the header anticipated a
new forge-contracts enum member, but the existing `BatchResult.error` +
`result_type="errored"` convention already expressed everything the
MISSING path needed, so nothing was added to the wire contract.

The entire poller — signals, schedule, and all three of these patched
paths — was deleted by T4.1 (2026-07-19) when forge moved to per-workflow
timer-loop polling. These fixes bought correct behavior for the interim
window and are now gone with the subsystem they patched.

## Scope

Minimal — the subsystem dies in Phase 4:

- On signal-delivery failure, leave the row SUBMITTED (next cycle retries;
  safe under [T1.2](T1.2-interim-batch-result-correlation.md) dedup).
- MISSING sends the waiter an error-payload signal so it fails fast.
- Do **not** build the attempts-cap/two-column machinery.
- **Amendment (2026-07-08, capture sweep):** also fix the wedge —
  `poll_batch_results` raises `RuntimeError` when ANY job errored
  (`batch_poll.py:205-207`) and the poller workflow sets no retry policy
  (`batch_poller_workflow.py:26-32`, unlimited default attempts), so one
  bad batch turns the poller into a retry loop while the overlap=SKIP
  schedule skips every subsequent run. Stop raising on partial per-job
  errors (report `errors_found` in the result; the next scheduled run is
  the retry) or set a finite `retry_policy` on the activity call, and give
  the scheduled action an explicit execution timeout + overlap policy in
  `_ensure_schedule`.

## Acceptance Criteria

- [x] Test: signal-delivery failure leaves the row SUBMITTED and the next
      cycle retries
      (`test_signal_delivery_failure_leaves_row_submitted`,
      `test_next_cycle_retries_delivery_after_failure`)
- [x] Test: MISSING sends the waiter an error-payload signal and the waiter
      fails fast (the previously untested lossy path)
      (`test_missing_signals_waiter_with_error` + existing
      `test_batch_error_in_signal_raises` for the fail-fast half)
- [x] *(2026-07-08)* Test: one errored job does not wedge the poller — the
      cycle completes, other jobs are processed, and the schedule's next
      run fires (`test_one_errored_job_does_not_wedge_cycle`,
      `test_creates_schedule_with_expected_interval` overlap/timeout asserts)

## References

- Design rationale: [2026-06-architecture-review.md](../../docs/reviews/2026-06-architecture-review.md) + DECISIONS D86–D97 — the surviving record; the external merged-plan/wave-JSON copies were verified missing 2026-07-08 (see [forge-review-2026-07-08.md](../../forge-review-2026-07-08.md) §3.2)
- Handoff:
  [HANDOFF-architecture-review-2026-06-10.md](../HANDOFF-architecture-review-2026-06-10.md)
- Task list source:
  [HANDOFF-architecture-review-2026-06-10-tasks.md](../archive/HANDOFF-architecture-review-2026-06-10-tasks.md)

## Plan

All three fixes live in `execute_poll_batch_results`
(`src/forge/activities/batch_poll.py`) and `_ensure_schedule`
(`src/forge/worker.py`). No workflow or model file changes.

1. **Signal-delivery failure leaves the row SUBMITTED (bug 1).** In the ENDED
   branch, track a `delivery_failed` flag. If any entry's signal delivery
   raised, `continue` without calling `update_status_fn` — the row stays
   SUBMITTED and the next poll cycle re-polls and re-delivers. Safe under
   [T1.2](T1.2-interim-batch-result-correlation.md): waiters key by
   `request_id` and `setdefault`, so re-delivered signals are no-ops. Only
   advance to PROCESSING (or terminal FAILED when there are no entries) when
   every entry was delivered.

2. **MISSING wakes the waiter (bug 2).** In the >24h branch, send the waiter a
   `BatchResult(error=..., result_type="errored")` signal before marking the
   row MISSING. The waiter (`batch_submit_and_wait`, ingestion) raises on
   `result.error`, so it fails fast instead of burning the 25h
   `_BATCH_WAIT_TIMEOUT`. No new forge-contracts enum member is needed — the
   existing `BatchResult.error` + `"errored"` convention (already used by the
   terminal-failure path, and the fail-fast half already covered by
   `test_batch_error_in_signal_raises`) fully represents the payload.

3. **Un-wedge the poller (bug 3).** Delete the `raise RuntimeError` on
   `errors_found > 0` — report `errors_found` in the returned result (log a
   warning for observability) and let the next scheduled run be the retry.
   Give the scheduled action an explicit `execution_timeout` and set
   `SchedulePolicy(overlap=ScheduleOverlapPolicy.SKIP)` in `_ensure_schedule`
   (and apply them on the update path too, so an already-running schedule
   picks up the backstop), so a wedged run is terminated and the next run can
   fire.

Factor a small `_deliver_signal` helper (get handle, signal, log-and-return
False on failure) to de-duplicate the three signal sites.

## Sub-tasks

- [x] `_deliver_signal` helper; ENDED branch leaves row SUBMITTED on any
      delivery failure (bug 1)
- [x] MISSING branch sends an error signal before marking MISSING (bug 2)
- [x] Drop the `raise` on partial errors; return + warn instead (bug 3a)
- [x] `_ensure_schedule`: `execution_timeout` + `overlap=SKIP`, applied on both
      create and update paths (bug 3b)
- [x] Update the four existing tests that asserted the removed `raise`
- [x] Add tests for the three acceptance criteria
- [x] Full suite green incl. coverage gate; ocr consumer smoke check

## Development Notes

- 2026-07-08 — Amended per the capture sweep
  ([forge-review-2026-07-08.md](../../forge-review-2026-07-08.md) §4.5,
  finding G8): the scheduled-poller wedge is the same interim class as the
  two patches above and dies with the same Phase 4 deletion.
- 2026-07-15 — Implemented. All changes in `execute_poll_batch_results`
  (`src/forge/activities/batch_poll.py`) and `_ensure_schedule`
  (`src/forge/worker.py`); no workflow or model file touched.
  - **No forge-contracts change was needed.** The header anticipated an "enum
    member," but the error-payload the MISSING path must deliver is already
    fully represented by the existing `BatchResult.error` +
    `result_type="errored"` convention — the same signal the terminal-failure
    path already builds, and the one the waiter (`batch_submit_and_wait`) and
    ingestion already fail fast on (`if result.error: raise ApplicationError`).
    `BatchJobStatus.MISSING` already exists. Inventing a new marker would have
    been redundant, so per the "minimal additive change *if needed*" guidance I
    added nothing to the contract. forge-contracts stayed clean on `phase-1`
    at `v0.1.1`.
  - Bug 1: factored a `_deliver_signal` helper (get handle → signal → log +
    return False on failure) shared by all three signal sites. The ENDED branch
    tracks a `delivery_failed` flag; if any entry's delivery fails it `continue`s
    without calling `update_status_fn`, leaving the row SUBMITTED for the next
    cycle to re-poll and re-deliver (no-op on already-delivered entries under
    T1.2 setdefault dedup). Only a fully-delivered batch advances to PROCESSING;
    an entry-less ENDED batch stays terminal FAILED as before.
  - Bug 2: the >24h MISSING branch now delivers a `BatchResult(error=...,
    result_type="errored")` before marking the row MISSING. The poll-failure is
    already counted in `errors_found`; a *signal* failure on this terminal path
    is not double-counted (nothing retries a MISSING row).
  - Bug 3: removed the `raise RuntimeError` on `errors_found > 0` (it turned one
    bad batch into an unbounded workflow retry loop that overlap=SKIP then
    starved). `errors_found` is now reported in the result + a warning log.
    `_ensure_schedule` gained an `execution_timeout` kwarg and sets
    `SchedulePolicy(overlap=ScheduleOverlapPolicy.SKIP)`; poller = 10 min,
    extraction = 1 h. The update path now reconciles spec **+ action + policy**
    (not just spec) so a schedule created before T1.3 picks up the backstop —
    this changed `test_updates_existing_schedule_when_already_running` (the
    action is now replaced, so its id becomes `<schedule_id>-run`).
  - Verified `ScheduleActionStartWorkflow(execution_timeout=...)` and
    `SchedulePolicy(overlap=ScheduleOverlapPolicy.SKIP)` against the installed
    `temporalio` (`.venv`), not from memory.
  - Suite: **1209 passed** (baseline 1207; net +2 tests), coverage **87.86%**
    (gate 85%). ocr consumer smoke check: **48 passed** (unaffected — no
    contract change). No new ruff findings; the two pre-existing test_worker.py
    findings (TC003, SIM117) left as-is.
