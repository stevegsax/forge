# T2.1 — Monorepo workspace in forge (incremental absorption)

**Status:** IN PROGRESS
**Phase:** 2 — Monorepo (forge, D98)
**Repos:** forge (becomes the monorepo); pbook (absorbed this increment);
sax-llm, forge-contracts, ocr (later increments)
**Depends on:** Phase 1 complete (T1.0–T1.8)
**Last updated:** 2026-07-16

## Problem

Both redesign plans converged on a uv-workspace monorepo: single lock,
per-package pyprojects and tests, and archived old repos with pointer
READMEs. Phase 1 (T1.0) made the question urgent: uniform editable path
sources entangle the sibling locks — forge@phase-1 cannot resolve against
pbook@main — so the repos can no longer version independently even if we
wanted them to.

**Rewritten 2026-07-16 per D98:** there is no new `sax` repo. Forge itself
is the monorepo; siblings are absorbed incrementally via git-filter-repo
subdirectory rewrites, pbook first. This dissolves the original task's
open "work-queue home" question — forge and `development-plans/` survive
as the root. D87's substance (one root `uv.lock`, filter-repo over
subtree, pinned-tag uvx rule, gated 3.14 bump) carries over unchanged.

## Scope

**Increment 1 (landed 2026-07-16):** absorb pbook into `apps/pbook`
with full history (tags renamed `v*` → `pbook-v*`; the disposable
`diataxis/` Hugo site excluded from imported history); convert the forge
root to a uv workspace; fix the deploy machinery that assumed a pbook
sibling checkout; retruth the docs; tag `pbook-v0.2.2`; repoint
skill-pbook.

**Increment 2 (active): sax-llm + forge-contracts → `libs/`.** Both
land **as-is** — the `sax_platform` consolidation is Phase 3's job.
Full history via the rehearsed filter-repo pattern (tags renamed
`v*` → `sax-llm-v*` / `forge-contracts-v*`); prep commits on each main
(drop `uv.lock`, bump 0.1.1 → 0.1.2 — neither has sources to retire,
so standalone tips keep resolving, unlike pbook's); root sources become
`{ workspace = true }` for all three, retiring the `../` path sources
and **closing Finding A**; pbook bumps 0.2.2 → 0.2.3 so the new tag's
tree is self-contained for the subdirectory uvx form; **ocr** (the one
external consumer) repoints `forge-contracts` at
`../forge/libs/forge-contracts` as its interim until increment 3;
skill-pbook re-pins to the end-state form.

**Increment 3 (active): ocr → `apps/ocr`.** The last repo absorption —
after it, D98's consolidation is complete. Full history via the
rehearsed filter-repo pattern (tag renamed `v0.1.1` → `ocr-v0.1.1`,
lightweight as original); prep commit on ocr main (drop the interim
`../forge/libs/forge-contracts` source + `uv.lock`, bump 0.1.1 →
0.1.2, README truth pass). **New wrinkle:** ocr is the first member
forge does not depend on (apps never import apps), so plain `uv sync`
won't install it — uv research: `uv run` syncs *inexactly* (adds,
never removes), so `uv run --package ocr ocr <cmd>` coexists safely
with the forge workers in the shared venv; only a bare exact `uv sync`
prunes it. Docs standardize on `uv sync --all-packages` for setup.
Deployment: opt-in supervised launchd agent (`install.sh --with-ocr`),
installed now per owner; env already covered by forge.env (all
`FORGE_*`). Runtime untouched — both queues and the cross-queue batch
SPI coupling stay as-is (T4.2 owns the polling restructure).

**Later increments (tracked here, not yet started):**

- Python 3.14 bump: own commit, gated on a fresh wheel-matrix check at
  execution time (unchanged from the 2026-07-08 amendment).

**Finding A (design gate, recorded in D98):** the end-state skill
invocation `uvx --from git+…forge.git@<tag>#subdirectory=apps/pbook`
cannot resolve until sax-llm is absorbed — a fresh uvx clone of forge has
no `../sax-llm` sibling to satisfy the root's editable path source. The
skill pins the interim form
`git+https://github.com/stevegsax/pbook.git@v0.2.1` until then.
**Increment 2 closes this** — verified by the V10-bis gate below.

## Acceptance Criteria

- [x] Forge suite green from the workspace root (85% gate), same
      collection count as pre-merge (1233 passed + 5 deselected, 88.21%);
      pbook suite green from `apps/pbook` (84% gate; 345 passed, 84.98%).
- [x] History and tags survive the import: `git log --follow` and blame
      work under `apps/pbook/`; tags `pbook-v0.1.0`–`pbook-v0.2.2`.
- [x] Filter-repo rehearsal performed on a throwaway clone before the
      real import; rollback criterion documented (see Plan).
- [x] Deploy path works without a pbook sibling checkout: bootstrap
      clones forge-contracts, does not clone pbook, and the pbook unit
      uses `PBOOK_DATABASE_URL` (both live defects fixed). Verified by
      inspection + `bash -n`; first live exercise is the next
      `with_pbook` deploy, which needs the owner-gated SSM param.
- [x] **The skill's uvx invocation verified end-to-end with a pinned
      tag.** Interim form (`…pbook.git@v0.2.1`) verified against GitHub
      2026-07-16; the subdirectory form
      (`…forge.git@pbook-v0.2.2#subdirectory=apps/pbook`) is blocked on
      the sax-llm increment (Finding A — failure reproduced empirically,
      see notes) and moves to that increment's criteria.
- [x] Cross-queue ingestion behavior unchanged
      (`tests/test_ingestion_workflow.py`: 8 passed).
- [x] No live references to `../pbook`, `PBOOK_DB_PATH`, `PBOOK_REPO_URL`,
      or `pbook_ref` remain (historical docs exempt).

## References

- [D98](../../docs/DECISIONS.md) (supersedes part of D87);
  [2026-06-architecture-review.md](../../docs/reviews/2026-06-architecture-review.md);
  [../HANDOFF-architecture-review-2026-06-10.md](../HANDOFF-architecture-review-2026-06-10.md)
- Approved implementation plan: 2026-07-15 planning session (refined from
  the owner's draft; verified against the working tree).

## Plan

One commit per step on branch `T2.1-pbook-workspace` from forge@main.
Runtime behavior must not change: no file under `src/forge/` or
`src/pbook/` is modified; both Temporal workers, queues, and the
cross-queue coupling stay as-is (Phase 6 owns their future).

0. **Prerequisites:** phase-1 merged to main in forge/pbook/sax-llm;
   clean trees; podman machine up; `uvx git-filter-repo`; pbook suite
   green at main pre-prep; record `PBOOK_MAIN_SHA` and forge main tip.
0.5. **pbook prep commits (on pbook main — no active users):** drop
   `[tool.uv.sources]` + `uv.lock`, bump 0.2.1 → 0.2.2, CLAUDE.md truth
   pass. The imported tree then needs no post-merge edits.
1. **Process-first docs commit (forge):** D98 appended; this file
   rewritten; TASKS.md Phase 2 retitled.
2. **filter-repo rehearsal (throwaway clone):**
   `uvx git-filter-repo --path diataxis --invert-paths
   --to-subdirectory-filter apps/pbook --tag-rename ':pbook-'`
   (selection before rename — order is load-bearing). Verify commit
   count, all files under `apps/pbook/`, no diataxis in history, tags
   renamed with annotations preserved, `git log --follow` crosses the
   rewrite. **Rollback criterion:** everything is branch-local until the
   owner pushes — on failure delete the scratch clone, `git branch -D
   T2.1-pbook-workspace`, delete fetched `pbook-v*` tags; forge main and
   the real pbook repo untouched (filter-repo only ever runs on
   throwaway clones).
3. **History merge:** fetch the filtered clone as a temporary remote;
   `git merge --allow-unrelated-histories --no-ff` (forge has zero tags —
   no collision).
4. **Workspace conversion (root pyproject only):**
   `[tool.uv.workspace] members = ["apps/pbook"]`;
   `pbook = { workspace = true }`; sax-llm/forge-contracts stay editable
   path sources at the root; `uv lock && uv sync`; review the lock diff.
5. **Deploy fixes:** bootstrap (add forge-contracts clone, drop pbook
   clone, migrate from the forge checkout, fail-fast on unset
   `PBOOK_DATABASE_URL`); pbook-worker.service (WorkingDirectory →
   forge checkout, drop `PBOOK_DB_PATH`, tighten ReadWritePaths);
   fetch-secrets.sh (emit `PBOOK_DATABASE_URL` from SSM
   `SUPABASE_PBOOK_DB_URL`); terraform (drop `pbook_ref`/`PBOOK_REPO_URL`
   plumbing; keep `with_pbook`).
6. **Docs truth pass:** forge CLAUDE.md (workspace member + command
   discipline); DEPLOYMENT.md (pbook-as-sibling story rewritten;
   Postgres-only); TOC.md pointer; CHANGELOG row; this file's notes;
   local AGENTS.md.
7. **Tag `pbook-v0.2.2`** (annotated; continues the renamed series).
8. **skill-pbook rewrite (separate repo):** pin the interim uvx form
   after the V10 gate confirms Finding A.

## Sub-tasks

- [x] Step 0 — prerequisites verified (2026-07-16: pbook/sax-llm mains
      fast-forwarded to phase-1; `PBOOK_MAIN_SHA=96b7781` pre-prep;
      forge main `b7be7d5`; pbook suite 345 passed @ 84.98%; forge
      baseline 1233 passed + 5 deselected @ 88.21%)
- [x] Step 0.5 — pbook prep commit (`57e8b7f` on pbook main)
- [x] Step 1 — process-first docs commit
- [x] Step 2 — filter-repo rehearsal + verification (see 2026-07-16 note)
- [x] Step 3 — history merge (55 commits, tags `pbook-v0.1.0`–`0.2.1`)
- [x] Step 4 — workspace conversion + lock review (lock churn = source
      flip only; pbook 0.2.1 `../pbook` → 0.2.2 `apps/pbook`)
- [x] Step 5 — deploy fixes (both live defects fixed)
- [x] Step 6 — docs truth pass
- [x] Step 7 — tag pbook-v0.2.2 (annotated)
- [x] Step 8 — skill-pbook rewrite (V10 → interim form; edits left
      uncommitted in skill-pbook per the owner gate)
- [x] Verification matrix (see 2026-07-16 verification note)

Increment 2 (branch `T2.1-libs-workspace`):

- [x] i2 Step 0 — baselines (sax-llm 165 passed @ main; forge-contracts
      10 passed; ocr main fast-forwarded to phase-1, 48 passed) and prep
      commits (sax-llm `95999d2`, forge-contracts `2dcb9fa`)
- [x] i2 Step 1 — process-first docs commit
- [x] i2 Step 2 — filter-repo rehearsals: exact count preservation
      (sax-llm 7, forge-contracts 11 — no exclusions, no pruning), all
      files under targets, tags renamed, `--follow` crosses
- [x] i2 Step 3 — history merges (5add628, 5e463ca)
- [x] i2 Step 4 — workspace conversion (9cab346; lock churn = source
      flips + version bumps only)
- [x] i2 Step 5 — ocr repoint (ocr main `dd38fc0`; 48 passed against
      forge-contracts 0.1.2)
- [x] i2 Step 6 — docs truth pass (aa15de6)
- [x] i2 Step 7 — tags: `pbook-v0.2.3`, `sax-llm-v0.1.2`,
      `forge-contracts-v0.1.2` (annotated)
- [x] i2 Step 8 — skill-pbook re-pinned to
      `git+…forge.git@pbook-v0.2.3#subdirectory=apps/pbook` after the
      push, verified against GitHub, committed (`8515a25`)

Increment 3 (branch `T2.1-ocr-workspace`):

- [x] i3 Step 0.5 — ocr prep commit (`20df956` on ocr main; baseline
      48 passed + 3 deselected)
- [x] i3 Step 1 — process-first docs commit
- [ ] i3 Step 2 — filter-repo rehearsal + verification
- [ ] i3 Step 3 — history merge
- [ ] i3 Step 4 — workspace conversion (member, not a dependency;
      `uv sync --all-packages`; venv-coexistence checks)
- [ ] i3 Step 5 — launchd `--with-ocr` agent, installed
- [ ] i3 Step 6 — docs truth pass
- [ ] i3 Step 7 — tag `ocr-v0.1.2`
- [ ] i3 Verification matrix (five suites, ocr postgres marker,
      ruff/mypy, history/tags, sweeps, poller on ocr-task-queue)
- [x] i2 Verification matrix: forge 1233 passed (88%+ gate), pbook 345,
      sax-llm 165, forge-contracts 10, ocr 48 — all at baseline
      counts; ruff/mypy only the two known pre-existing findings;
      sweeps clean (AGENTS.md regenerated; remaining `../` hits are
      historical docs); **V10-bis PASSED** — `uvx --from
      git+file://…forge@pbook-v0.2.3#subdirectory=apps/pbook pbook
      --help` resolves and runs, uv building sax-llm from
      `libs/sax-llm` inside the same clone. Finding A closed.

## Development Notes

- 2026-07-08 — Amended pre-implementation per
  [forge-review-2026-07-08.md](../../forge-review-2026-07-08.md) §5: the
  original task combined a five-repo history rewrite, the workspace
  conversion, and a runtime major-version jump, with no rehearsal, no
  rollback plan, and an unanswered work-queue-location question.
- 2026-07-16 — Rewritten per D98: forge is the monorepo; this task is
  now the incremental absorption, of which the pbook merge is
  increment 1. The 2026-07-08 amendments are honored: the import is
  rehearsed with a documented rollback criterion, the 3.14 bump stays
  split out, and the work-queue question is moot.
- 2026-07-16 — Owner: pbook has no active users → workspace-readiness
  moved upstream into pbook prep commits (Step 0.5), shrinking the
  forge-side surgery. Standalone pbook is unresolvable at tip from that
  commit on (accepted; tags ≤ v0.2.1 still resolve).
- 2026-07-16 — Step 0 exploration confirmed two live deploy defects,
  fixed in Step 5: bootstrap never clones forge-contracts (fresh EC2
  `uv sync --frozen` fails since T1.0), and pbook-worker.service still
  sets `PBOOK_DB_PATH` (pbook is Postgres-only since the migration).
- 2026-07-16 — Rehearsal results: 65 pre-rewrite commits → 55 imported.
  The delta is exactly accounted for: 10 diataxis-only commits pruned
  as newly empty by `--invert-paths` (expected; the plan's "count =="
  check was wrong to assume equality) and 2 originally-empty PR-merge
  commits retained. Tags `pbook-v0.1.0`/`0.2.0` annotated, `0.2.1`
  lightweight — matching the originals. `git log --follow` and blame
  verified across the rewrite (store.py: 15 commits). Tip carried the
  Step 0.5 prep (0.2.2, no sources, no lock).
- 2026-07-16 — pbook prep commit is `57e8b7f` on pbook main (push
  owner-gated). AGENTS.md (untracked local Codex copy of CLAUDE.md)
  regenerated from the updated CLAUDE.md.
- 2026-07-16 — Verification: forge root 1233 passed + 5 deselected
  (88.21%), identical collection to the pre-merge baseline; pbook from
  `apps/pbook` 345 passed (84.98%); `-m postgres` 5 passed (needs
  `DOCKER_HOST` pointed at the podman machine socket +
  `TESTCONTAINERS_RYUK_DISABLED=true`, else it skips); ingestion 8
  passed; `uv lock --check` clean; both CLIs run from the one venv.
  Ruff (2× TC003 in `tests/test_worker.py`) and mypy (2 errors in
  `src/forge/alembic/versions/002_idempotency_rekey.py`) findings are
  **pre-existing on main** — this branch changes no root `.py` files.
- 2026-07-16 (post-verification) — **Owner decision: EC2 is removed.**
  The deployment target becomes the local podman stack (Temporal + both
  workers join Postgres/MinIO alongside `deploy/local-database/`);
  stores stay managed (Supabase Postgres, S3); Apple Containers hosts
  Claude Code sessions, not the runtime; podman keeps backing the test
  suites. Consequences for this branch, which merges as-is: the Step 5
  deploy fixes touch machinery (bootstrap, terraform, systemd units,
  fetch-secrets) that a follow-up branch will delete outright, and the
  "create SSM `SUPABASE_PBOOK_DB_URL`" owner gate is **moot** —
  `PBOOK_DATABASE_URL` will be provided by the local stack's env
  instead. DEPLOYMENT.md as rewritten here describes the EC2 world and
  dies with it. The follow-up owns: deleting `deploy/terraform` +
  `deploy/scripts/bootstrap-instance.sh`/`fetch-secrets.sh` + the EC2
  systemd units + the mTLS gateway surface (SECURE-REMOTE-ACCESS.md —
  it existed only because Temporal was remote), adding Temporal/worker
  services to the podman stack, and rewriting DEPLOYMENT.md for the
  local topology. Needs its own decision entry (D99) when planned.
- 2026-07-16 — V10 gate, run empirically: the subdirectory form fails
  exactly per Finding A (`uvx --from git+file://…forge@pbook-v0.2.2
  #subdirectory=apps/pbook` → uv resolves the workspace and errors
  "has no subdirectory `../sax-llm`"); the interim pinned form
  `git+https://github.com/stevegsax/pbook.git@v0.2.1 pbook --help`
  succeeds against real GitHub. skill-pbook rewritten to the interim
  form (SKILL.md gate + body identical strings; CLAUDE.md pointers;
  Makefile `PBOOK_REPO ?= ../forge`); left uncommitted there per the
  owner gate. skill-pbook's eval seed/worker targets were found stale
  from before this task (SQLite-era, pbook is Postgres-only) — flagged
  as known-stale in its CLAUDE.md, not reworked here.
