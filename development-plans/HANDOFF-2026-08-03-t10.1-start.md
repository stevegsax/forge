# Handoff — T10.1 items 1–4 and 7 landed and the maiden schema request applied; resume T10.1

**Date:** 2026-08-03
**Status:** Eight commits landed since the 2026-08-02 handoff (`65b7736`),
in commit order: **`b31ca4b`** (change request 0001 — the maiden Tier-1
artifact plus Alembic revision 004), **`48ac8ab`** (the vendored canonical
`.squawk.toml`), **`d77761f`** (item 1 — verify-only worker startup at all
three workers, `forge migrate`, `make db-migrate` repaired), **`7b5d531`**
(item 2 — the `make db-change` generator and the `make lint-sql` Squawk
gate), **`22a8730`** (item 3 — the schema-change process into PROCESS.md
and SDLC.md), **`086f4a8`** (item 4 — the canonical Postgres image in CI
and testcontainers, plus the advisory-lock/`CREATE INDEX CONCURRENTLY`
deadlock fix its verification surfaced), **`3812a32`** (item 7 — WORKERS.md
and DEBUGGING.md rewritten to the post-cutover world), and **`610aa04`**
(test log pollution fixed). `origin/main` is level with HEAD
(`git rev-list --left-right --count origin/main...HEAD` → `0 0`).
Gates **as recorded in the T10.1 task file's 2026-08-03 Development
Notes** — the sixth (latest) note records `make gates` **EXIT=0** — forge
**1564** / cov **91.51%**, pbook **383**, ocr **243**, sax-platform
**632**; the fourth note additionally records forge `-m postgres` 7 passed
and ocr 1 passed on the canonical image, with **replay 17 unregenerated**.
**Production runs `104c14b`** — `git -C ~/repos-sax/forge-prod rev-parse
--short HEAD` on a clean tree, corroborated live by `temporal task-queue
describe`: `prod-forge-worker-1@104c14b`, `prod-forge-worker-2@104c14b`
(`forge-task-queue`) and `prod-ocr-worker@104c14b` (`ocr-task-queue`), all
on `:7243` namespace `forge-prod`, none `-dirty`; `pbook-task-queue` has
no pollers on either lane. **None of this period's code is deployed.**
Next task: **T10.1 — migrate forge onto sax-datastores**, still **IN
PROGRESS**.

This is a state-of-the-world note for the next session. It does not
restate the work queue ([TASKS.md](TASKS.md)), the decisions
([../docs/DECISIONS.md](../docs/DECISIONS.md)), the status-of-record
([../docs/OVERVIEW.md](../docs/OVERVIEW.md)), or the lifecycle
([SDLC.md](SDLC.md)) — it says what changed since the
[2026-08-02 T10.1-start handoff](HANDOFF-2026-08-02-t10.1-start.md), what
to know before touching anything, and where to start. **This period closed
five of the ten remaining T10.1 items and ran the schema-change process
end to end for the first time**: request 0001 was submitted, reviewed,
refused once (spuriously), and **applied to production on 2026-08-03**.
Every claim below is re-derived from a command or file read during this
sweep. **Read items 1, 2 and 3 before touching anything** — a prod deploy
that is now both unblocked and overdue, a schema that has moved ahead of
the deployed code, and a test-path gate that has quietly opened.

## What landed since the last handoff

- **`b31ca4b` — change request 0001, the maiden Tier-1 request.**
  `datastore-changes/0001-interactions-created-at-index/` (holding
  `request.md` and `change-1.sql`) and forge's Alembic revision
  `004_interactions_created_at_index.py`: a concurrent index on
  `interactions(created_at)`, chain `003 → 004`. 3 files, 157 lines
  added. Submitted as sax-datastores issue #3, pinned to this commit.
- **`48ac8ab` — the vendored canonical Squawk config.** `.squawk.toml`
  at the repo root, a byte-identical copy of the operator's
  `templates/squawk.toml` (adopted operator-side at `fe28da2` after
  forge proposed it on issue #2). It excludes `require-lock-timeout` and
  `require-statement-timeout` because the apply runbook injects both at
  apply time, so in-artifact timeouts are *forbidden*, not missing — a
  rule that always needs a waiver is not a gate. The file states its own
  load-bearing caveat: the exclusion holds only while `make apply-change`
  is the sole path an artifact reaches a database.
- **`d77761f` — item 1: verify-only worker startup at all three
  workers.** New `sax_platform.db.verify_schema_version` (a pure
  six-verdict classifier — `AtHead` / `Ahead` / `Behind` /
  `Uninitialized` / `AmbiguousStamp` / `BrokenChain` — over already-read
  facts, plus a thin reading shell); **`Ahead` is allowed with a
  warning**, because the expand/contract window (change applied before
  code deploys) is normal and must not brick a lane on restart. All
  other non-head verdicts raise `SchemaVersionError` naming the chain,
  both revisions, the masked URL, and the two-lane fix. forge/ocr
  `_init_store` → `_verify_store_schema`; pbook `_migrate_if_configured`
  → `_verify_schema_if_configured`. New **`forge migrate`** CLI (the gap
  verify-only exposed: forge had no explicit dev apply path), and
  `make db-migrate` — **broken since T3.6**, importing the deleted
  `get_store_url` — rewritten to `uv run forge migrate`. 23 files,
  1,379 lines added against 102 removed.
- **`7b5d531` — item 2: the `make db-change` generator and the Squawk
  gate.** `sax_platform.db.change_request` (function core / imperative
  shell, 100% covered): revision-walk FROM→TO emitting one
  `change-<n>.sql` per revision step, offline SQL through a programmatic
  Alembic `Config`, a wrapper strip that reproduces the maiden artifact
  **byte-for-byte** (pinned by a golden `==` test — the discovery was
  that Alembic emits `<stmt>\n\n`, so a naive line-drop leaves doubled
  blanks), CONCURRENTLY phase classification, per-product id allocation,
  and a `request.md` skeleton mirroring the operator template. Squawk is
  pinned once (`SQUAWK_VERSION = "2.61.0"`, with a test asserting no
  second pin); `--no-lint` stamps a request **NOT LINTED** so an unlinted
  request can never read as clean. `forge db-change` / `ocr db-change` /
  `pbook db-change` are the first commands in all three CLIs deliberately
  outside the `_EnvCommand` env guard (no database, no Temporal).
  `make lint-sql` runs in `make gates` and in CI.
- **`22a8730` — item 3: the process into PROCESS.md and SDLC.md.**
  SDLC.md gained the Operator row and the counterparty-boundary
  paragraph, an "Operator requests (request-and-wait)" section with a
  deploy-ordering clause on step 9, two records-map rows
  (`datastore-changes/` roots and the vendored `.squawk.toml`), and a new
  **"Consumer/operator boundary" standing-directives subsection**
  (reproduced in item 12 below, which is how it reaches the next
  session). PROCESS.md gained the "Schema Changes (Operator Requests)"
  section — six forge-side duties, binding expand/contract, the
  emergency-lane reconciliation duty (`alembic stamp`, never execute) —
  and its "Specification Changes" opener now scopes its "no
  change-request form" claim to spec changes.
- **`086f4a8` — item 4: the canonical image, and a real deadlock found
  by verifying it.** Every Postgres image forge provisions for tests is
  now `ghcr.io/stevegsax/sax-postgres-rum:17`, from the single constant
  `sax_platform.testing.CANONICAL_POSTGRES_IMAGE`, with the one
  unavoidable copy in ci.yml pinned by a drift test that also rejects the
  retired image names. The finding:
  `test_concurrent_migrations_serialize_on_postgres` hung forever on any
  chain containing revision 004 — **pre-existing since `b31ca4b`, not
  the image swap**. Mechanism: migrator A holds the advisory lock and
  runs `CREATE INDEX CONCURRENTLY`, whose second phase waits on every
  older snapshot; migrator B's *blocking* `pg_advisory_lock` holds a
  snapshot for the whole wait; the cycle runs through a vxid wait, which
  Postgres' deadlock detector cannot see. Fix: `_acquire_advisory_lock`
  polls `pg_try_advisory_lock` with the retry sleep **client-side**
  (a server-side `pg_sleep` holds a snapshot while executing and
  recreates the bug), on an AUTOCOMMIT connection. Filed as
  sax-datastores issue #8 (open) as a shared-knowledge writeup.
- **`3812a32` — item 7: WORKERS.md and DEBUGGING.md rewritten.** Every
  port, namespace, label, poller and exit code re-derived; the
  `:7233`/`default`/`:5434` world removed, migrate-at-startup claims
  replaced by schema verification, `http://localhost:8233` corrected
  (dev UI is `:8236`; **the prod stack ships no UI by design**), and two
  wrong env-var claims fixed. The same falsehood living in *code* went
  with it: ocr's two `FORGE_DB_URL`-unset messages still pointed at the
  deleted `~/.config/forge/forge.env` and the dead port-5434 stack.
- **`610aa04` — test log pollution fixed.** CLI-invoking tests ran
  `cli.configure_logging`, whose file handler fell through to the real
  XDG state dir, appending pytest lines (sqlite tmp-dir URLs) to the
  operator's own `~/.local/state/forge/{forge,worker}.log`. An autouse
  `forge_log_dir_disabled` fixture sets `FORGE_LOG_DIR=""` — the
  documented disable value — for every test; proof was a full suite run
  leaving both real logs byte-stable.
- **The maiden request went through the operator process end to end**
  (out of repo; sax-datastores issue #3, now **CLOSED**). Reviewed and
  approved Tier 1 on 2026-08-03 (record committed operator-side as
  `schema-changes/forge/0001.yaml`), then one apply attempt **refused**
  by the pre-check reporting branched heads, then that refusal
  **withdrawn as a false positive** — `apply-change.sh` captured the
  version query with `2>&1` and folded `podman compose`'s stderr banner
  into the value, fixed operator-side at `ffae2d5` — and finally
  **applied and stamped 2026-08-03** (`applied.prod: {date: 2026-08-03,
  role: forge_prod}`). Issue #2 (the process negotiation) closed the
  same day.

## Owner rulings

**None new this period.** The 2026-08-02 ruling set — the
consumer/operator operating model, no product DDL ever, verify-only
startup by agreement, binding expand/contract, §21's credential
direction, §22's categorical no-agent-provisioning rule, R8/R9, and the
maiden-request sequencing — stands unchanged and is now partly
*implemented* rather than only agreed (items 1–4 and 7 above). One
standing owner decision governs what is still blocked: the credential
rotation that gates pbook's return was **deliberately deferred to the end
of the current round of changes** (Steven, 2026-08-02, recorded in
`~/repos-sax/sax-datastores/docs/status.md`) — see item 4.

## What to know before touching anything

1. **The prod deploy is now unblocked — and until it happens, a prod
   worker restart fails closed.** Two facts combine. (a) `forge_prod`
   is at revision **004**: `select count(*), version_num from
   alembic_version_forge` through the prod profile's own role returns a
   single row `004`, with `ix_interactions_created_at` present in
   `pg_indexes` (`alembic_version_ocr` is a single row `002`). (b) The
   deployed checkout is `104c14b`, whose chain contains only
   `001`–`003` (`git ls-tree --name-only 104c14b
   src/forge/alembic/versions/`) and whose worker startup still calls
   `run_migrations` (`git show 104c14b:src/forge/worker.py` →
   `_init_store` → `run_migrations`), i.e. `command.upgrade(cfg,
   "head")`. Alembic cannot compute an upgrade path from a current
   revision its script directory does not contain — demonstrated on the
   live chain, which raises `CommandError: Can't locate revision
   identified by '005'` for an unknown current revision. *Currently
   running workers are unaffected* — they migrated at boot on
   2026-07-31 and never re-read the chain. *Act-now before any restart,
   reboot, or crash-recovery:* run DEPLOYMENT.md's pre-deploy in-flight
   check, then `make prod-deploy REF=610aa04`; the new code verifies
   `AtHead` at 004 instead of migrating. The operator's deploy-ordering
   ask (hold the deploy until the apply is stamped) is **discharged** —
   the stamp landed 2026-08-03.

2. **The maiden request is applied; the loop is closed and the process
   is proven.** Derivations: `gh issue view 3` on sax-datastores →
   state `CLOSED`, last comment "Applied and stamped" at
   2026-08-03T17:13:54Z; the operator's record
   `~/repos-sax/sax-datastores/schema-changes/forge/0001.yaml` carries
   `applied: prod: {date: 2026-08-03, role: forge_prod}`; and the
   database itself agrees (item 1). Two things follow. T10.1 item 10's
   *request* half is done, leaving close-out. And the process now has a
   worked example on both sides — the request artifact in this repo, the
   review and apply record in the operator's — so the second request
   (pbook's baseline, deliberately sequenced second to exercise the
   validator's non-`public` schema path) has a template.

3. **Item 5's gate is open: the `_test` trust path works today.**
   The last handoff could not read the thread state; it is now readable
   and the code side is live. Verified directly by this sweep:
   `psql 'postgresql://forge_test@127.0.0.1:5432/forge_test'` returns
   `forge_test | forge_test`, and the same for `pbook_test`. No
   credential material exists on that path (pg_hba `trust`, dev stack
   only), which is exactly the §22-sanctioned agent route. *Act-later,
   unblocked:* repoint pbook's podman-fallback conftest and the
   testcontainers paths at it, and make an unreachable test database a
   **named error, not a skip** — the fourth Development Note records
   that `pytest -m postgres` currently skips all 7 tests silently unless
   `DOCKER_HOST` points at the podman machine socket, which is the same
   quiet skip that would have masked a bad image string.

4. **pbook is still down on both lanes, and the blocker is an owner
   deferral, not an operator queue.** `pbook-task-queue` shows an empty
   poller list on `:7236`/`forge-dev` and on `:7243`/`forge-prod`. Both
   profiles still export a dead credential: `PBOOK_DATABASE_URL` in
   `~/.config/forge/envs/prod.env` names `…@localhost:5434/pbook` and in
   `dev.env` names `…@localhost:5434/forge_dev` — **port 5434 is the
   deleted `forge-postgres` container**, absent from `podman ps`, and
   the dev line additionally names the *forge* database under a pbook
   variable. Because the prod profile is sourced by every worker, each
   forge and ocr worker carries that dead credential. The rotation that
   would unblock the rewrite is deferred by owner decision to the end of
   this round of changes, on the grounds that the databases hold no
   data. **That reasoning covers the two database credentials only.**
   The same 2026-07-31 `ps eww` read exposed `ANTHROPIC_API_KEY`,
   `OPENAI_API_KEY`, `MISTRAL_API_KEY` (billable) and forge's
   `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`, whose blast radius has
   nothing to do with whether our tables have rows; the operator's
   status note says so explicitly and points at `password-proposal.md`
   §0 for the sequence. Do not hand-edit a profile to guess a
   credential.

5. **The dev lane is up again — one commit behind HEAD and dirty.**
   `temporal … task-queue describe` on `:7236`/`forge-dev` shows
   `dev-forge-worker@3812a32-dirty` and `dev-ocr-worker@3812a32-dirty`
   on their queues; the ~696-workflow `ocr-task-queue` backlog recorded
   in the task file's fifth Development Note has drained to **0**. These
   are the dev lane's first live verify-only startups, and they passed:
   `forge_dev` holds a single-row `alembic_version_forge` at **004** and
   a single-row `alembic_version_ocr` at **002** (checked through the
   dev profile this sweep). That also answers the operator's dev-side
   ask on issue #3 — the concurrent-startup race left no duplicate
   version rows on dev either. The `-dirty` stamp is expected: the dev
   lane runs the live working tree by design (D103), and the tree
   carries another session's uncommitted files (item 11). Restart with
   `make dev-worker-restart WORKER=forge` / `WORKER=ocr` if you want the
   lane on HEAD.

6. **The startup-migration race was real; it just never fired here.**
   Worth recording so the fix is not mistaken for cosmetics. The
   operator's first diagnosis — that concurrent launchd worker startups
   each running `alembic upgrade head` against an empty database had
   already produced duplicate version rows — was **withdrawn**: forge
   and ocr both hold exactly one row on both stacks, and the refusal was
   a script bug (item "What landed", last bullet). The exposure was
   nonetheless real, and this period removed it twice over: workers no
   longer apply DDL at all (`d77761f`), and the surviving apply paths
   are serialized under an advisory lock whose own deadlock was fixed
   (`086f4a8`). **Production is unprotected until it is deployed** — it
   runs `104c14b`, which predates both.

7. **The Squawk exclusions rest on an invariant, not on taste.**
   `.squawk.toml` excludes the two timeout rules only because
   `make apply-change` injects `lock_timeout` and a per-phase
   `statement_timeout` at apply time. If any apply path ever exists that
   does not inject them — an emergency lane run by hand, a product
   applying its own DDL — those two rules come back, and every request
   linted under the current config becomes a weaker claim than it reads
   as. The caveat is inline in the file; do not edit the file locally
   for any other reason either (a consumer-local edit makes every "lint
   clean" claim meaningless, and the copy is re-vendored, never
   diverged).

8. **The gate surface grew.** `make gates` now runs `make lint-sql`
   (Squawk 2.61.0 over both `datastore-changes/` roots) alongside lint,
   mypy ×4, lint-imports and the four suites, and CI runs it in the lint
   job. Two consequences: a generated change artifact that fails lint
   fails the ordinary gate run, and the Squawk pin lives in exactly one
   place (`SQUAWK_VERSION` in `sax_platform.db.change_request`, which
   `make lint-sql` reads out of Python rather than copying).

9. **Unowned findings, flagged rather than fixed** — each a small
   correction with no owning task:
   - **`docs/operations/WORKERS.md:116` says "The dev lane is down"**,
     with a ~700-workflow backlog. Both are false as of this sweep
     (item 5). The section is dated and tells the reader to re-derive,
     which is why this is a flag and not a defect, but the line is
     wrong today.
   - **`docs/operations/DEPLOYMENT.md` still carries its pre-D104
     banner** ("has not had the full post-migration rewrite it is
     owed (T10.1)"), which contradicts the refreshed remaining-work
     list's preamble claim that D104 closed DEPLOYMENT.md. The banner is
     the on-the-ground truth; the rewrite sliver is unowned by any of
     the ten items. Owner to queue it or fold it into close-out.
   - **`apps/pbook/CLAUDE.md:33` still says the store is "the `pbook`
     database in the local `forge-postgres` instance"** — the deleted
     container. The same line was correctly updated for verify-only
     startup by `d77761f`, so it is half-current. T8.3 or close-out
     owns it.
   - **`docs/DECISIONS.md:741` (D104) says "`make db-migrate`
     survives: it drives `forge.store.run_migrations`"** — false twice
     over: the target had been broken since T3.6, and `d77761f`
     rewrote it to `uv run forge migrate`. Correcting a shipped
     decision's text is the Specification Changes policy's call, not
     the sweep's.
   - **`forge migrate` exits 78 on an unset `FORGE_DB_URL` while
     forge's own `_require_store_engine` exits 1** for the same
     condition (pre-existing inconsistency), and
     `format_migration_target` is now duplicated in `src/forge/cli.py`
     and `apps/ocr/src/ocr/cli.py` (a `sax_platform.db` home would
     consolidate).
   - Still open from earlier periods, re-verified this sweep:
     **pbook's Alembic `env.py` is the only remaining `CREATE
     EXTENSION` in the tree** (`rg -n "CREATE EXTENSION"` matches no
     other source file), which product code must not attempt under
     §22; **`src/forge/cli.py`'s `forge ingest` reads pbook's database
     directly**, which Architecture Principle 8 forbids and which is
     why the pbook credential is exported to every worker; **prod S3
     bucket ownership is undefined** (forge self-administers through
     `deploy/s3/` while the operator model classifies bucket lifecycle
     as administration); and **the three T9 task files remain absent
     from TASKS.md** (`rg -n "T9\." development-plans/TASKS.md` returns
     nothing) — now surviving four handoffs.

10. **Open adjudications: A1, A3–A6, A8** (plan review §2–§3; A2 ruled —
    the T5.5 → T5.6 order; **A7 absorbed by T10.1** and answered by the
    operator's nightly dump plus its restore check). Do **not** act on
    them: A1 (T6.7 before T7.1/T7.3/T7.4/T7.5 — being reworked by the
    in-flight T6.8, item 11), A3 (tier-map swap before or into T6.2), A4
    (T6.1 versus Architecture Principle 8), A5 (`batch_jobs` spend
    columns — the review's own recommendation is *decline*, which is
    itself the ruling still owed), A6 (T8.2's "OVERVIEW Open items
    closed"), A8 (the WORKERS.md ops section). **A8 survived this
    period's WORKERS.md rewrite:** its centerpiece — "a workflow RESET
    cannot recover already-paid batch results" — is still unwritten
    (`rg -ni "reset|already-paid"` matches neither WORKERS.md nor
    DEBUGGING.md). The owner walkthrough is the ruling vehicle and
    resumes at T6.1 after the datastore work.

11. **The tree is dirty with another session's in-flight work — leave it
    alone.** `git status --short`: `M development-plans/TASKS.md`,
    `M docs/reviews/2026-06-architecture-review.md`, and untracked
    `tasks/T6.7-forge-knowledge-consumption.md` and
    `tasks/T6.8-d92-amendment-knowledge-phase-resplit.md` — the parallel
    **D92 amendment** re-split, which moves the queue to 60 tasks and
    marks T6.7 **BLOCKED** pending a **D105** mechanism ruling. `rg -n
    "D105" docs/DECISIONS.md` still matches nothing, so those files
    reference a decision that does not exist in the record; that session
    owns closing the gap. Standing directive: pre-existing uncommitted
    changes belong to other work and stay untouched. Note for pickup:
    this sweep's status-of-record edits were written against that dirty
    tree, so if the parallel change-set lands first, re-read before
    editing (the 2026-07-31 stale-copy write-back that reverted three
    landed documents is the reason this warning persists).

12. **Standing directives**, reproduced from [SDLC.md](SDLC.md) §
    Standing directives as it reads today (18 items, five groups; that
    section is the canonical home — amend it there, never here).
    *Authority and scope:* commits and merges are the owner's on
    explicit word only, and no agent, skill, or sweep commits;
    implementation is dispatched to `implementation-driver` with
    hard-bounded scopes, and Temporal workflows, the environment guard,
    concurrency, and migrations are never delegated to Sonnet;
    pre-existing uncommitted changes in the tree belong to other work
    and stay untouched.
    *Engineering:* no compatibility shims — forge has no external
    consumers, so fix the seam in place and update all callers; activity
    timeout/retry presets come from `forge/presets.py` only, because the
    values are ScheduleActivityTask command attributes and changing one
    is replay-breaking; no inline copies of the step/gather/dispatch
    pipelines in the `forge/workflows/` drivers; new workflow tests pass
    `sync_mode=True` and script outcomes keyed by identity, never
    arrival order, with no module-level mutable containers (the
    pytest-xdist blocker T5.5 removed); read
    `~/.claude/python-guidelines.md` before writing or reviewing Python,
    and require the same of any spawned code-writer.
    *Consumer/operator boundary* (added 2026-08-02, landed in
    `22a8730`): no product DDL, ever — workers verify the schema at
    startup and refuse when behind, and schema changes are Alembic
    revisions requested through the change-request process and applied
    by the operator, never by an agent on either side (§22);
    expand/contract is binding, so every request leaves
    currently-deployed code working and a breaking change is three
    requests; the commit is the request, so nothing under a submitted
    `datastore-changes/` directory is ever modified — a revision is a
    new request; never write into the operator repos
    (`~/repos-sax/sax-datastores`, `~/repos-sax/sax-temporal`) and never
    read `~/.config/sax-datastores` (credential delivery is the owner
    pasting URLs into profiles — presence checks only, never print
    values); and code carrying a new schema revision deploys only after
    the operator applies the change.
    *Working style:* mechanism-level explanations — the underlying
    cause, act-now versus act-later versus no-action, concrete command
    sequences, and never a safety caveat compressed into a bare clause;
    challenge weak arguments and spot missing data, do not praise.
    *Tool hazards:* `rg -n`, never `rg -rn` (`-rn` parses as `--replace
    n` and fabricates output); never start a wrapped `.md` prose line
    with `+`, because the formatter hook re-parses it as a list marker
    and rewrites the list; `gsed`, not `sed`, when stream-editing is
    unavoidable — prefer Edit.

## Where to start

- **T10.1 is IN PROGRESS.** Read order: the task file's **six
  2026-08-03 Development Notes** (newest first — they carry the
  per-item shapes, the recorded gates, and the loose ends each item
  surfaced), then the refreshed **ten-item remaining-work list** in the
  2026-08-02 note, whose items 1, 2, 3, 4 and 7 are now struck through,
  then item 1 above for what the system actually shows.
- **The five open items, with derived readiness:**
  - **5. Conftest repoint to the `_test` trust path** — **unblocked**;
    the gate opened and the path is verified working (item 3).
  - **6. pbook's return** — blocked on the deferred rotation (item 4);
    sequence is rotate → both profile lines → `launchctl enable
    gui/$UID/com.saxcapital.pbook-worker` **then** `bootstrap` (the
    disabled label fails `bootstrap` with a bare `Input/output error`
    otherwise).
  - **8. The DB-URL guard** — still deferred to §21's credential
    landing, which itself waits on T6.8 resolving forge's database
    placement. Do not write it yet.
  - **9. The reboot test** — still open on the operator's side too
    (`sax-datastores/docs/status.md`, "Needs a host action": every run
    of the podman-machine LaunchDaemon so far hit an already-running
    machine, so it has never done its job).
  - **10. Close-out** — the maiden request half is done (item 2); what
    remains is PROCESS.md close-out: CHANGELOG row, TASKS.md checkbox,
    the task file's as-shipped truth pass, and the CLAUDE.md "Running
    the System" touch-up that the item-7 note deferred to close (this
    sweep did the parts the landed work falsified).
- **The deploy is the one act-now item outside the list** — see item 1.
  It is not part of T10.1's remaining work as written, but it is what
  makes this period's code real, and a prod worker restart before it
  fails.
- **Invariant for the whole task, unchanged:** zero workflow code, so
  the **16 committed replay histories** must pass **unregenerated** end
  to end. Every commit this period held that line (replay 17 passed, as
  recorded in the task file's notes). The environment guard is never
  delegated to Sonnet.
- **After T10.1:** T5.7 (execution-time reference repair), then the
  owner walkthrough resumes at T6.1 before any Phase 6 work is planned
  — noting that the in-flight T6.8 (item 11) is already reshaping that
  phase.
- Normal pickup reconciliation still applies — verify the numbers above
  against the tree before planning.
