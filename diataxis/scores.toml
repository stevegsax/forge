[[runs]]
timestamp = "2026-03-31T12:00:00Z"
description = "Initial scoring after first draft generation"

[runs.project]
topic_decomposition = { score = 5, justification = "13 topics appropriately scoped by concept. Core pipeline topics (1-9) get full depth; extension topics (10-13) are focused on what they add. No redundancy across topics." }
quadrant_balance = { score = 4, justification = "All four quadrants well-represented: 13 explanations, 13 references, 11 how-tos, 1 tutorial. For project-docs, the how-to and reference emphasis is appropriate. Single tutorial is by design (golden path first, iterate later)." }
audience_alignment = { score = 5, justification = "Language and complexity match internal developers with Python/Temporal/LLM background. Prerequisites stated clearly. Links to external docs for foundational topics." }
completeness = { score = 5, justification = "All 34 planned files have drafts. No topics with missing content." }

# --- Tutorial ---

[runs.files."tutorials/golden-path.md"]
coverage = { score = 5, justification = "All 9 covers items addressed with dedicated sections and concrete CLI output." }
detail_compliance = { score = 4, justification = "Concrete example, visible output at every step, ~20min read. Minor gap: several internal-mechanism explanations exceed 'brief'." }
guidance_adherence = { score = 3, justification = "Concrete examples and 'What you built'/'Where to go next' sections present. Violates 'do NOT explain the internals inline' in 3 cache-explanation passages and embeds an 11-row reference table instead of linking to explanation." }
cross_linking = { score = 4, justification = "Callout links to explanation docs after most phases. Missing callout in 'Examine the assembled prompt structure' section." }
quadrant_purity = { score = 3, justification = "Four cross-contamination violations: 11-row reference table (lines 129-149), three cache-explanation passages (lines 210-211, 242-244, 333-336) that answer 'why' instead of narrating what the learner observes." }

# --- Explanations ---

[runs.files."explanation/system-overview.md"]
coverage = { score = 5, justification = "All 6 covers items addressed. Plan-then-execute vs. dynamic evolution is implicitly covered via link." }
detail_compliance = { score = 5, justification = "High-level orientation, no code, no CLI commands. Clear mental map of the system." }
guidance_adherence = { score = 4, justification = "Principles grounded in 'why' with consequences. Links to tutorial and workflow-step present. Temporal docs linked." }
cross_linking = { score = 4, justification = "Links to workflow-step, golden-path, and reference. No link to how-to (none exists for this topic)." }
quadrant_purity = { score = 5, justification = "Clean explanation throughout. No tables, no instructions, no tutorial walkthroughs." }

[runs.files."explanation/workflow-step.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: pattern rationale, task-agnostic orchestration, Temporal relationship, stateless calls, transition vocabulary, orchestrator-in-control." }
detail_compliance = { score = 5, justification = "Conceptual deep-dive using the specified state machine analogy." }
guidance_adherence = { score = 5, justification = "Makes the case for why it matters. Contrasts with chat-loop. Links to reference and tutorial." }
cross_linking = { score = 5, justification = "Links to reference/workflow-step.md and tutorials/golden-path.md." }
quadrant_purity = { score = 4, justification = "Strong explanation. Minor: opening numbered phase list and transition vocabulary dash-list border on reference format." }

[runs.files."explanation/context-assembly.md"]
coverage = { score = 4, justification = "8 of 9 covers items addressed. All present but the '11 sections' coverage embeds reference material inline." }
detail_compliance = { score = 4, justification = "Deepest explanation as specified. Missing diagram that detail field specifically references." }
guidance_adherence = { score = 4, justification = "Decision chain explicit, cache efficiency discussed, all required links present." }
cross_linking = { score = 5, justification = "Links to reference, tutorial, how-to, and workflow-step explanation." }
quadrant_purity = { score = 3, justification = "Most contaminated explanation. 11-section list, 6-priority ordered list, and 6-step exploration procedure embed reference/how-to material." }

[runs.files."explanation/task-decomposition.md"]
coverage = { score = 5, justification = "All 9 covers items addressed with dedicated sections." }
detail_compliance = { score = 5, justification = "Thorough coverage of decomposition, gathering, and final result assembly." }
guidance_adherence = { score = 5, justification = "Answers both core questions. Planner role clear. Conflict resolution framed as workflow step instance." }
cross_linking = { score = 5, justification = "Links to workflow-step, reference, and tutorial." }
quadrant_purity = { score = 4, justification = "Strong explanation. Minor: PlanStep field list and fan-out subsection headers border on reference/procedure territory." }

[runs.files."explanation/output-processing.md"]
coverage = { score = 5, justification = "All 6 covers items addressed." }
detail_compliance = { score = 3, justification = "Side-by-side examples present but not in the comparative format specified. Heavy code block usage pushes toward reference territory." }
guidance_adherence = { score = 5, justification = "Connects to context assembly and validation. Fuzzy matching examples present. Links to reference." }
cross_linking = { score = 5, justification = "Links to reference, validation explanation, and context-assembly explanation." }
quadrant_purity = { score = 3, justification = "Four labeled 'Example:' sections with code blocks cross into reference/tutorial. Exact thresholds and LLMResponse field list are reference material." }

# --- References ---

[runs.files."reference/system-overview.md"]
coverage = { score = 5, justification = "All 5 covers items: principles table, module map, tech stack, env vars, filesystem layout." }
detail_compliance = { score = 5, justification = "Entirely tables with one-line introductions." }
guidance_adherence = { score = 5, justification = "Module map mirrors directory tree, one-line descriptions, links to topic-specific docs." }
cross_linking = { score = 5, justification = "Links to explanation, sibling reference, and tutorial." }
quadrant_purity = { score = 5, justification = "Pure reference. No instruction, opinion, or narrative." }

[runs.files."reference/workflow-step.md"]
coverage = { score = 5, justification = "All covers items: activities, workflows, TransitionSignal, retry semantics, core data models." }
detail_compliance = { score = 5, justification = "Tabular throughout with field/type/required/description format." }
guidance_adherence = { score = 4, justification = "Activities in execution order. Missing end-of-file link back to explanation." }
cross_linking = { score = 4, justification = "Links to explanation in intro only. No See Also footer." }
quadrant_purity = { score = 4, justification = "Mostly clean. Fan-out execution description uses procedural verb sequence." }

[runs.files."reference/context-assembly.md"]
coverage = { score = 5, justification = "All 7 covers items present: prompt sections, priority ordering, providers, budget algorithm, all data models." }
detail_compliance = { score = 4, justification = "Dense reference with tables. Missing code examples for provider request/response formats (explicitly required by detail field)." }
guidance_adherence = { score = 4, justification = "Structure matches guidance order. Individual sections lack back-links to explanation." }
cross_linking = { score = 5, justification = "Links to explanation and how-to in intro." }
quadrant_purity = { score = 4, justification = "Mostly clean. Token budget algorithm uses numbered procedural steps. Target utilization note reads as recommendation." }

[runs.files."reference/task-decomposition.md"]
coverage = { score = 4, justification = "5 of 6 covers items present. Missing 'Planner prompt structure and inputs' section." }
detail_compliance = { score = 5, justification = "Tabular throughout for plan format, execution modes, CLI flags." }
guidance_adherence = { score = 4, justification = "Plan format first, then modes, then CLI. Missing link to how-to." }
cross_linking = { score = 4, justification = "Links to explanation in intro. No link to howto/submit-tasks.md." }
quadrant_purity = { score = 4, justification = "Clean except git worktree lifecycle numbered steps border on how-to." }

[runs.files."reference/output-processing.md"]
coverage = { score = 5, justification = "All 5 covers items present with precise thresholds and edge cases." }
detail_compliance = { score = 5, justification = "Precise technical reference with algorithm details." }
guidance_adherence = { score = 5, justification = "Developer debugging edit failures can find answers here." }
cross_linking = { score = 5, justification = "Links to explanation, validation reference, and validation how-to." }
quadrant_purity = { score = 5, justification = "Cleanest reference file. Pure description throughout." }

# --- How-to Guides ---

[runs.files."howto/submit-tasks.md"]
coverage = { score = 4, justification = "5 of 6 covers items. Missing 'inspect plan output before execution starts'." }
detail_compliance = { score = 5, justification = "CLI-focused recipes with brief intros and commands." }
guidance_adherence = { score = 4, justification = "Replaces USAGE.md. Links to reference and explanation. Minor explanatory prose in sanity-check section." }
cross_linking = { score = 4, justification = "Links to explanation and reference. Missing link to debug-workflow.md." }
quadrant_purity = { score = 4, justification = "Strongly task-focused. Minor explanatory leakage in sanity-check and fan-out sections." }

[runs.files."howto/control-context-assembly.md"]
coverage = { score = 5, justification = "All 5 covers items addressed with CLI examples and expected output." }
detail_compliance = { score = 5, justification = "Short task-focused guides with 'I want to...' scenarios." }
guidance_adherence = { score = 5, justification = "Links to explanation, focuses on knobs, includes forge status output." }
cross_linking = { score = 5, justification = "Links to explanation and reference." }
quadrant_purity = { score = 5, justification = "Exceptionally clean how-to. All sections use 'How to...' pattern." }

[runs.files."howto/debug-workflow.md"]
coverage = { score = 5, justification = "All 7 covers items addressed as numbered sections." }
detail_compliance = { score = 5, justification = "Problem-oriented guide with symptom/diagnostic-steps format." }
guidance_adherence = { score = 5, justification = "Most practically valuable how-to. Common scenarios first, CLI commands throughout." }
cross_linking = { score = 5, justification = "Links to observability reference and explanation." }
quadrant_purity = { score = 4, justification = "Minor: explanatory asides about Temporal retry behavior and log level semantics." }

[runs.files."howto/configure-validation.md"]
coverage = { score = 4, justification = "All 4 covers items addressed. Bonus sections for auto-fix and task-file config. Missing annotated terminal output for validation results." }
detail_compliance = { score = 4, justification = "Short task-focused sections. Missing forge status sample output that guidance explicitly requires." }
guidance_adherence = { score = 4, justification = "Links to reference. Test execution section has explanatory 'why' sentence that should be in explanation doc." }
cross_linking = { score = 5, justification = "Formal Related section with three cross-links." }
quadrant_purity = { score = 4, justification = "Clean overall. Minor explanatory leakage in test execution section." }

[runs.files."howto/add-domain.md"]
coverage = { score = 5, justification = "All 5 covers items addressed with step-by-step recipe and verification." }
detail_compliance = { score = 5, justification = "Four numbered steps following ADDING_A_DOMAIN.md structure." }
guidance_adherence = { score = 4, justification = "Assumes competence. Step 2 checklist explains field semantics rather than linking to reference." }
cross_linking = { score = 4, justification = "Links to explanation and reference. Missing forward links to configure-validation.md." }
quadrant_purity = { score = 4, justification = "Task-focused. Step 2 checklist partially duplicates reference material." }

# =============================================================================
# Run 2: 2026-04-11 — Learning-loops reorganization
# =============================================================================

[[runs]]
timestamp = "2026-04-11T15:26:49Z"
description = """Targeted scoring after the learning-loops reorganization. This run covers the project level plus the 8 files written or materially edited in this session: 4 new files (explanation/learning-loops.md, explanation/transcript-ingestion.md, reference/transcript-ingestion.md, howto/ingest-transcripts.md), 2 renamed-and-edited files (explanation/forge-run-extraction.md, reference/forge-run-extraction.md), 1 file with intro-and-link updates (howto/manage-playbooks.md), and 1 comparison file (tutorials/golden-path.md, which received ## Step N: headings during the deterministic check fix). The 15 other files scored in Run 1 are unchanged and their prior scores still apply — they are deliberately not re-scored here. Prose quality is now scored on every file in this run (it was absent from Run 1)."""

[runs.project]
topic_decomposition = { score = 4, justification = "Down from 5. The reorganization added two new topics (transcript-ingestion, learning-loops) alongside the renamed forge-run-extraction, bringing knowledge-management to three topics out of 16. The explicit cross-cutting synthesis piece (learning-loops) is necessary precisely because the decomposition split a single problem domain across two implementation topics — that split is principled but makes the topic hierarchy carry more weight than ideal. An umbrella 'knowledge management' topic containing both implementation subtopics and the synthesis would be tighter; the current flat structure is correct for a project-docs set but close to the upper bound of complexity the reader should have to navigate." }
quadrant_balance = { score = 4, justification = "Unchanged. 13 explanations + 14 references + 12 how-tos + 1 tutorial across 16 topics. learning-loops adds explanation-only (correctly — a synthesis piece has no reference or how-to analog). transcript-ingestion adds all three quadrants. Heavy on reference/how-to is right for project-docs; the single tutorial is still by design." }
audience_alignment = { score = 5, justification = "Unchanged. learning-loops is marked 'advanced' with explicit prereqs; its prose uses internal-developer language (terms like 'hot-path latency', 'artifact of build order') without condescending explanation. transcript-ingestion assumes the reader knows what pbook and Temporal are. No audience drift." }
completeness = { score = 5, justification = "Unchanged. All 40 planned content files have 'draft' or better status. No empty topics." }

# =============================================================================
# Tutorial (comparison file)
# =============================================================================

[runs.files."tutorials/golden-path.md"]
coverage = { score = 5, justification = "Unchanged from Run 1. All 9 covers items still addressed." }
detail_compliance = { score = 4, justification = "Unchanged. Step-by-step structure now explicit via '## Step N:' headings (previously the sequence was implicit), but the internal-mechanism explanations in Steps 5, 9, and the token cache callout still exceed 'brief'." }
guidance_adherence = { score = 3, justification = "Unchanged. The heading fix did not address the cache-explanation passages or the embedded 11-row reference table. The guidance 'do NOT explain the internals inline' is still violated in three places." }
cross_linking = { score = 4, justification = "Unchanged. Callout links to sibling explanation docs after most steps; still missing one at Step 5 (assembled prompt structure)." }
quadrant_purity = { score = 3, justification = "Unchanged. The four cross-contamination violations from Run 1 remain: one reference table and three explanatory passages." }
prose_quality = { score = 4, justification = "Not scored in Run 1. First-person-plural tutorial voice ('we will...') is appropriate per the rubric. No throat-clearing openers found. Some 'For more on X, see Y' patterns repeat across steps but each is load-bearing. Prose is dense in the internal-mechanism asides but not padded. Could reach 5 only by cutting the inline explanations the quadrant-purity score also flags." }

# =============================================================================
# Explanations — session-modified
# =============================================================================

[runs.files."explanation/forge-run-extraction.md"]
coverage = { score = 5, justification = "All 6 covers items (scope boundary, playbook concept, independent workflow, three-activity pipeline, deterministic tags, self-learning feedback) are addressed. The 'self-learning' framing is explicit throughout." }
detail_compliance = { score = 5, justification = "Discursive, connects to core orchestration primitives, scoped tightly to forge's own run history. Inherits strong structure from the prior draft." }
guidance_adherence = { score = 5, justification = "Opens with the explicit scope note the new guidance required. Uses 'self-learning loop' framing. Links forward to learning-loops and sideways to transcript-ingestion at the top and at the end. Scope boundary stated twice — once in the intro, once in the closing paragraph — which is the right amount of repetition for a framing rewrite." }
cross_linking = { score = 5, justification = "Links to context-assembly (prereq), reference, how-to, transcript-ingestion (sibling pipeline), and learning-loops (synthesis). Every direction covered." }
quadrant_purity = { score = 5, justification = "Pure explanation. No procedural steps, no tables, no reference material. The inherited content from the prior draft was already clean; the added framing paragraphs do not introduce any new violations." }
prose_quality = { score = 4, justification = "The inherited body is tight. The added closing paragraph contains 'It is worth naming what this loop does not do' — this is exactly the kind of throat-clearing opener the rubric penalizes. Rewrite as 'This loop does not learn from anything outside Forge's own observability store.' That single edit would lift this to 5." }

[runs.files."explanation/transcript-ingestion.md"]
coverage = { score = 5, justification = "All 7 covers items addressed in dedicated sections: boundary rationale, two-workflow split, SUMMARIZATION-tier-and-batch-mode rationale, cross-queue handoff with sequence diagram, two-worker isolation, already-ingested tracking, 'why pbook entries and not forge playbooks' final section." }
detail_compliance = { score = 5, justification = "Discursive, prose-driven, explicitly avoids describing every field (those go in the reference). Mermaid sequence diagram of the cross-queue handoff earns its place. No step-by-step recipes. Length proportional to the concept." }
guidance_adherence = { score = 5, justification = "The central insight that 'ingestion and extraction are different steps owned by different projects' is named clearly in the opening section and reinforced in the final 'why pbook not forge' section. Acknowledges the parallel forge-run-extraction pipeline and links to learning-loops for the full discussion." }
cross_linking = { score = 5, justification = "Links to both prereqs (llm-dispatch, forge-run-extraction), the reference, the how-to, learning-loops for synthesis, and context-assembly. Good coverage." }
quadrant_purity = { score = 5, justification = "Pure explanation. No tables, no imperatives, no reference-style field enumerations. The mermaid sequence diagram is diagrammatic explanation, which is allowed." }
prose_quality = { score = 4, justification = "One filler phrase: 'One thing to understand about transcript ingestion is that it is lossy by design' (line 79). Rewrite as 'Transcript ingestion is lossy by design.' That one edit lifts the score to 5. The rest of the prose is tight; the 'short answer / longer answer' pattern in the final section is borderline conversational but not filler." }

[runs.files."explanation/learning-loops.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: problem statement, two-loops reality (with mermaid), side-by-side schema comparison table, historical+architectural reasons for the split, concrete convergence refactor plan (with second mermaid), hard-parts discussion (hot-path latency, operational coupling, information loss), reader takeaway section." }
detail_compliance = { score = 5, justification = "Thesis-driven discursive prose. Covers both medium-explicitness (conceptual) and hard-explicitness (concrete refactor steps). Includes the two mermaid diagrams and the schema comparison table that the detail field explicitly called for. Every paragraph carries weight — no obvious filler paragraphs." }
guidance_adherence = { score = 4, justification = "The thesis is stated clearly. The two-loops reality is named without hedging. The convergence path is concrete (numbered list, second diagram). Some hedging remains: the guidance said to use phrases like 'this is technical debt' or 'artifact of build order' — the file uses 'accident of history plus one real design tension' and 'an artifact' in passing, but never the sharper 'technical debt' phrasing. The tone is honest but one step short of the blunt candor the guidance asked for." }
cross_linking = { score = 5, justification = "Links to both prereq topics (forge-run-extraction, transcript-ingestion) at the top, context-assembly in the hot-path discussion, and back to the two implementation topics at the end. Correct links for a synthesis piece." }
quadrant_purity = { score = 4, justification = "Mostly clean explanation. One borderline: the 4-item numbered list in 'What convergence would look like' describes the refactor as numbered steps. Each item is descriptive ('becomes a preprocessor', 'is deleted') rather than imperative, which keeps it on the right side of the line, but the numbering itself visually evokes a how-to. Could be rewritten as a dashed list or as prose to remove any ambiguity." }
prose_quality = { score = 4, justification = "Stronger than the other session files — no 'It is worth' or 'One thing to understand' constructions. Opening paragraph is slightly self-referential ('This document explains why there are two...') and the navigation sentences ('If you only want to understand one...') are meta. Euphemism: 'parts that are not yet clean' hedges where the guidance asked for candor. Without those two softenings, this would score 5. Active voice throughout. Tight paragraphs. The schema comparison table and the convergence section are the strongest prose." }

# =============================================================================
# References — session-modified
# =============================================================================

[runs.files."reference/forge-run-extraction.md"]
coverage = { score = 5, justification = "All 6 covers items: playbook table schema (scoped to forge.db), tag inference rules, PlaybookEntry/ExtractionResult/ExtractionWorkflowInput/Result data models, CLI commands, injection behavior, and the new 'scope boundary' item explicitly pointing at transcript-ingestion for the pbook side." }
detail_compliance = { score = 5, justification = "Tabular throughout. Schema table, tag rule tables, model field tables, CLI option tables, injection property table. Brief one-line intros per section. No narrative." }
guidance_adherence = { score = 5, justification = "Opens with the scope note (covers the 'scope boundary' covers item), points at the transcript-ingestion reference for the pbook side, does not describe pbook's schema even briefly. Structure is schema → tags → data models → CLI → injection, matching the guidance order." }
cross_linking = { score = 5, justification = "Links to explanation, how-to, transcript-ingestion reference, context-assembly reference, and learning-loops. All directions covered." }
quadrant_purity = { score = 5, justification = "Pure reference. Declarative statements, tables, no procedural steps, no explanations, no opinions. Inherited structural quality from the prior draft." }
prose_quality = { score = 5, justification = "Brief, declarative intros under each table. No filler. The added opening paragraph is tight and does its job (scoping the reference) in three sentences." }

[runs.files."reference/transcript-ingestion.md"]
coverage = { score = 5, justification = "All 7 covers items addressed as dedicated sections: TranscriptIngestionWorkflow (with input/output/signals/early-exit tables), BatchIngestionWorkflow (with input/output tables), prepare_transcript activity, cross-queue calls table, model routing and batch config table, CLI flags table, env vars table, exit codes table, module layout table." }
detail_compliance = { score = 5, justification = "Tabular. One table per subject. Intro sentences are one or two sentences. No narrative prose beyond the intros." }
guidance_adherence = { score = 5, justification = "Structure mirrors the code (workflows → activity → cross-queue calls → CLI → module layout). The file does not explain any design decisions — it only describes. Every 'why' question is redirected to the explanation via cross-links. Opening paragraph explicitly disclaims pbook-side schema coverage and points at pbook's docs." }
cross_linking = { score = 5, justification = "Links to explanation, how-to, forge-run-extraction reference, and learning-loops at the top. A bottom 'See also' pointer to forge-run-extraction reference and learning-loops for the conceptual story." }
quadrant_purity = { score = 5, justification = "Pure reference. Every statement is declarative. No imperatives, no 'this is because', no narrative digression. Even the 'Cross-queue calls' section, which could have drifted into explaining how cross-queue calls work, limits itself to what each call does and what its arguments are." }
prose_quality = { score = 5, justification = "All intros are one or two declarative sentences. No filler. Tables do the heavy lifting, which is what a reference should do. Cannot be meaningfully shortened without losing information." }

# =============================================================================
# How-to — session-modified
# =============================================================================

[runs.files."howto/ingest-transcripts.md"]
coverage = { score = 5, justification = "All 6 covers items addressed as dedicated numbered recipes: single session, --all, project filter, dry-run preview, --force reprocess, debug stuck ingestion." }
detail_compliance = { score = 4, justification = "Short CLI-focused recipes with numbered steps. Most recipes include sample output that doubles as verification. One gap: not every recipe ends with an explicit 'verify this worked' step; some rely on implicit verification via the output sample. The guidance asked for a verification step per recipe and that is not always present." }
guidance_adherence = { score = 5, justification = "Title starts with 'How to'. Assumes the reader knows what a Claude Code session file is and where pbook stores entries (no teaching). Links to reference and explanation at the top. Jumps straight into commands without conceptual framing." }
cross_linking = { score = 5, justification = "Links to the explanation and reference at the top. The debug section references forge status, worker logs, and pbook worker — all appropriate for a how-to that stays focused on the task." }
quadrant_purity = { score = 4, justification = "Mostly task-focused. Two cross-contamination leaks: 'Ingestion runs in batch mode, so the forge worker's CPU is not pinned while waiting' (explains the timing expectation) and 'pbook's deduplication is semantic, not transactional' (explains pbook behavior). Both are helpful context but both explain rather than instruct. A stricter version would link to the explanation for those points." }
prose_quality = { score = 5, justification = "Tight, command-focused prose. No throat-clearing. Every sentence either is a step, is expected output, or is a short factual note bounding the recipe. No filler phrases." }

[runs.files."howto/manage-playbooks.md"]
coverage = { score = 5, justification = "All 4 covers items still addressed by the existing recipes. The new scope note in the intro does not change coverage." }
detail_compliance = { score = 5, justification = "Short CLI-focused recipes, now explicitly scoped to forge's own playbook store." }
guidance_adherence = { score = 5, justification = "Links to the forge-run-extraction reference and explanation. Redirects readers looking for transcript ingestion to howto/ingest-transcripts.md, exactly as the guidance requires. The edit integrated the redirection into the intro paragraph rather than appending a labeled feedback block." }
cross_linking = { score = 5, justification = "Intro links to explanation, reference, and ingest-transcripts how-to. Internal references updated to the renamed files." }
quadrant_purity = { score = 5, justification = "Pure how-to. Inherited strength from the prior draft. The new intro scope note is factual and does not drift into explanation." }
prose_quality = { score = 5, justification = "Brief, command-focused, no filler in the new scope sentence ('It is scoped to Forge's self-learning loop'). Inherits the tight style of the original. Nothing to cut." }

# =============================================================================
# Hugo migration pass (2026-04-19)
# =============================================================================

[[runs]]
timestamp = "2026-04-19T18:50:00Z"
description = "Fresh qualitative pass following the Hugo migration. All content files re-read; structure scored against diataxis.toml rubric; no scores copied from prior runs."

[runs.project]
topic_decomposition = { score = 5, justification = "17 topics with a clean progression: foundation (system-overview, golden-path, workflow-step), pipeline internals (prompt-construction, context-assembly, task-decomposition, output-processing, validation-and-retries, llm-dispatch), platform (observability), learning loops (forge-run-extraction, transcript-ingestion, learning-loops), parameterization (task-domains), extensions (ocr-pipeline, planner-eval, nushell-integration). No redundancy; each topic owns a distinct concept." }
quadrant_balance = { score = 4, justification = "42 content files across 15 explanation / 15 reference / 11 howto / 1 tutorial. For project-docs the howto+reference weighting is appropriate. The single tutorial (golden-path) is deliberate per the structure but leaves no on-ramp for extension topics (OCR, eval, ingestion) — a reader who wants a guided first run on those sub-systems has no tutorial." }
audience_alignment = { score = 5, justification = "Language assumes Python/Temporal/LLM familiarity as declared in the project prerequisites. External links (Temporal, Anthropic Batch API, Mistral OCR, Hextra) replace re-explaining foundations. Complexity scales with topic — system-overview is conceptual, reference files are field-level." }
completeness = { score = 5, justification = "All 42 files declared in diataxis.toml exist and carry content. The deterministic check-status-consistency passes. No planned files are missing or stubbed." }

# --- Tutorial ---

[runs.files."tutorials/golden-path.md"]
coverage = { score = 5, justification = "All 9 covers items addressed as explicitly labeled steps: submission, planner decomposition, context assembly, exploration, assembled prompt, structured output, edit application, validation failure+retry, commit, verbose inspection." }
detail_compliance = { score = 4, justification = "Concrete add-a-stats-command example, observable output at every step, reads in ~20 minutes. Step 5 ('Examine the assembled prompt structure') is two paragraphs of signpost text rather than a step with visible output — it correctly avoids re-explaining prompt structure but breaks the 'every step shows a result' cadence the detail field specifies." }
guidance_adherence = { score = 4, justification = "Guidance says 'do NOT explain the internals inline' — the tutorial respects this by linking out after each step. 'What you built' and 'Where to go next' sections are present as required. One minor drift: Step 9's cache-arithmetic callout ('4.9s vs 7.8s because 8,689 of 9,536 input tokens were served from cache') is explanatory material that the linked prompt-construction doc should own; the tutorial could narrate the observation and cut the interpretation." }
cross_linking = { score = 5, justification = "Every step ends with a 'For more on X, see …' callout pointing to the right explanation or reference. The 'Where to go next' section links to 7 destinations (submit-tasks, context-assembly, workflow-step, task-decomposition, output-processing, validation-and-retries, debug-workflow)." }
quadrant_purity = { score = 4, justification = "Mostly a guided walkthrough. The 'Context around error' plain-text block in Step 9 edges toward reference material (it shows the error section structure rather than narrating what the learner sees). Step 8 briefly explains why validation mapped to FAILURE_RETRYABLE — instruction-over-narration drift. None of these break the quadrant, but a stricter reviewer would trim them." }
prose_quality = { score = 4, justification = "Direct prose with frequent observable output. Occasional filler: 'Notice that we did not specify --target-file' (the next sentence explains why), 'After a few seconds, the planner output appears', 'The planner decided on three sequential steps. Each step has a description, target files, and explicit boundaries. Steps execute in order, and each commits on success before the next step begins' — the last sentence restates the prior sentence in different words. Trimmable but not disqualifying." }

# --- Explanations ---

[runs.files."explanation/system-overview.md"]
coverage = { score = 4, justification = "Five of six covers items have dedicated sections: problem, document completion, seven principles, component fit, task-agnostic orchestration. The sixth — 'plan-then-execute model vs. future dynamic task evolution' — is only implicit; the doc mentions plan-then-execute in passing but never contrasts it with the deferred dynamic-evolution design the covers item calls out." }
detail_compliance = { score = 4, justification = "High-level orientation piece, no code or CLI commands, clear mental map. The detail field says 'Diagrams showing component relationships are encouraged' — no diagrams appear. Component relationships are described in prose only." }
guidance_adherence = { score = 5, justification = "Every principle is grounded in its consequence. Batch-first connects to batch API compatibility; deterministic-work-should-be-deterministic connects to context assembly; halt-when-confused connects to escalation reports. Temporal docs and the golden-path tutorial are linked as the guidance requires." }
cross_linking = { score = 5, justification = "Links to workflow-step, golden-path tutorial, system-overview reference, and external Temporal docs. No missing sibling links." }
quadrant_purity = { score = 5, justification = "No step-by-step procedures, no field tables, no CLI output. Principles are stated and connected, not prescribed." }
prose_quality = { score = 4, justification = "Direct, concrete, well-paced. Some mild redundancy: the 'Forge inverts this relationship' paragraph near the top and the 'Forge inverts this' paragraph in the 'Document completion, not chat' section make the same point twice. The four one-sentence component descriptions in 'How the major components fit together' are tight and earn their lines." }

[runs.files."explanation/workflow-step.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: pattern rationale, task-agnostic orchestration, Temporal-vs-activity relationship, statelessness, transition vocabulary, orchestrator-in-control." }
detail_compliance = { score = 5, justification = "Conceptual deep-dive as specified. State-machine analogy used as a dedicated section. Explains why each decision matters, not how the code implements it." }
guidance_adherence = { score = 5, justification = "Contrasts with chat-loop orchestrators (required by 'design decision to keep the orchestrator in control'), grounds every claim in consequences, and links to reference and tutorial throughout. Discusses testability and observability as explicit consequences of the pattern." }
cross_linking = { score = 5, justification = "Links to reference/workflow-step and tutorials/golden-path appear in-paragraph, not only as a trailing list." }
quadrant_purity = { score = 4, justification = "Numbered phase lists and the three-row TransitionSignal description edge into reference territory, but each numbered item carries explanatory prose about why it exists. A stricter editor would push the enumerations into the reference." }
prose_quality = { score = 3, justification = "Prose is mostly tight, but there is a real inconsistency that a skeptical editor catches: the five phases are listed as 'Construct --> Send --> Receive --> Serialize --> Transition' in the opening diagram, then introduced as 'Construct / Send / Receive and serialize / Validate / Transition' (five items, combining Receive+Serialize and adding Validate) in the numbered list immediately below, then re-listed as 'Construct, Send, Receive, Validate, Transition' in the state-machine analogy section. Three different enumerations of the five phases in the same file. Also: 'A useful way to think about the universal workflow step is as a state machine' is throat-clearing. Both issues are fixable with a small edit, but the phase-name drift is a correctness issue, not just style." }

[runs.files."explanation/prompt-construction.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: structured-document vs chat transcript, 11 sections with cache-optimized ordering, prefix-caching rationale, 3 cache breakpoints and their placement, AST-derived error injection, why errors go last." }
detail_compliance = { score = 5, justification = "Discursive prose throughout. Clear statement of scope boundary ('does not cover how Forge decides which files … that process is Context Assembly')." }
guidance_adherence = { score = 5, justification = "Respects the dividing line: describes the destination (prompt structure), not the journey (discovery/ranking). Cross-references context-assembly for the journey. Explains *why* the ordering is what it is, grounded in cache mechanics." }
cross_linking = { score = 5, justification = "Links to workflow-step (prerequisite), reference/prompt-construction, context-assembly (downstream), golden-path tutorial (concrete example)." }
quadrant_purity = { score = 4, justification = "Each of the 11 sections has a labeled entry (**Sections 1–2:**, etc.) that edges toward reference tabulation, but every entry is followed by explanatory prose about what each section is for and why it belongs where it does. The bold-labeled format is borderline but functional." }
prose_quality = { score = 4, justification = "Parallelism is strong: 'if a relevant file is missing, the LLM hallucinates; if the token budget is wasted on irrelevant content, the signal-to-noise ratio drops; if the ordering is wrong, caching fails' lands. Most paragraphs earn their length. One observation from the tutorial (attempt 2 takes 4.9s vs 7.8s) is repeated here — appropriate for explanation but worth a cross-reference check." }

[runs.files."explanation/context-assembly.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: import graph, PageRank, budget packing, progressive disclosure, exploration loop, separate-document-completions rationale, tradeoffs section." }
detail_compliance = { score = 5, justification = "Walks through discovery → ranking → packing → disclosure → exploration → tradeoffs as the detail field requires. Does not re-describe section structure or cache ordering (that is prompt-construction's job)." }
guidance_adherence = { score = 5, justification = "Dividing-line paragraph at top restates the separation from prompt-construction explicitly. Tradeoffs section cites production evidence (Aider, Sourcegraph) for the structural-analysis-over-embeddings choice." }
cross_linking = { score = 5, justification = "Links to prompt-construction (prerequisite), workflow-step, reference, howto. Final paragraph has explicit 'for how the assembled content is structured' pointer to prompt-construction." }
quadrant_purity = { score = 4, justification = "The 'Token Budget Packing' subsection uses procedural sentences ('Items are packed in priority order. Within each priority tier …') that describe the algorithm rather than its motivation. The algorithm itself belongs in the reference; here, prose should motivate why knapsack-style priority packing over alternatives. The motivation is present but the stepwise description is noisier than it needs to be." }
prose_quality = { score = 4, justification = "Generally tight. The 'Tradeoffs and Alternatives' section is the prose high point — three bullet-leading paragraphs that each state a choice and defend it. Elsewhere, one paragraph explaining exploration (batch-compatibility rationale) runs long and could be split — the first half makes the point, the second half repeats it in different words." }

[runs.files."explanation/task-decomposition.md"]
coverage = { score = 5, justification = "All 9 covers items addressed: three modes, planner inputs+outputs, why planning gets premium resources, fan-out dispatch, gather+conflict resolution, nested fan-out, sanity checks, git strategy, final result assembly." }
detail_compliance = { score = 4, justification = "Lifecycle from submission to committed result is covered. The detail field calls for 'diagrams showing the planner output, step execution, and fan-out/gather flow' — no diagrams present. Multi-step and fan-out flows are described in prose only." }
guidance_adherence = { score = 5, justification = "Answers both 'how is a task decomposed' and 'how is a final answer assembled' directly. Connects planner-gets-reasoning-tier to the planning-is-the-hard-part principle." }
cross_linking = { score = 5, justification = "Links to workflow-step, reference, tutorial. Further Reading section at the end." }
quadrant_purity = { score = 3, justification = "Fan-Out Mechanics is structured as Dispatch / Execution / Gather / Conflict detection / Merge+validate subsections — this is how-it-works mechanics shaped like a runbook. 'What the planner produces' is a bullet list of PlanStep fields (identifier, target files, context files, sub-tasks, capability tier override) that belongs in the reference. Sanity Check verdicts listed as bullets (CONTINUE/REVISE/ABORT) duplicate the reference table. The page explains what happens; a stricter editor would cut the enumerations and point at the reference." }
prose_quality = { score = 4, justification = "Direct and specific where the prose is narrative. Where the structure turns into subheaded mechanics (fan-out mechanics, sanity check verdicts), prose reduces to one-line definitions that work fine but are low-content." }

[runs.files."explanation/output-processing.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: tool-use over free-form, LLMResponse schema split, D50 search/replace rationale, four-level fallback with motivation per level, sequential-application mechanics, ambiguity-as-error." }
detail_compliance = { score = 5, justification = "Design decisions and tradeoffs are the focus. The 'Walking Through the Chain' example shows a concrete trailing-whitespace case falling through levels 1→2, and an indentation case falling through 1→2→3 — exactly the worked examples the detail field asks for." }
guidance_adherence = { score = 5, justification = "Explains why search/replace, why four levels, and what goes wrong with slight whitespace differences. Grounded in specific failure modes (token waste, silent destruction)." }
cross_linking = { score = 5, justification = "Links to reference, context-assembly (for why target file contents must be in the prompt), and validation-and-retries (for what happens when an edit fails)." }
quadrant_purity = { score = 4, justification = "Each of the four fallback levels has a subsection. The subsections are motivation + one example, which is explanation-appropriate, but the subsection structure itself is near-reference. 'Sequential Application and Ambiguity as Error' reads more as specification than rationale." }
prose_quality = { score = 4, justification = "Good contrastive framing ('Early LLM orchestrators parsed code out of markdown fenced blocks. … This is fragile'). 'The problem is one of scale and scope' is a mild throat-clearer before the two concrete failure modes. Most paragraphs earn their length." }

[runs.files."explanation/validation-and-retries.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: deterministic-before-LLM, validation pipeline, transition mapping, blind-retry cost, AST error construction, cache-efficient error placement, per-mode retry semantics." }
detail_compliance = { score = 5, justification = "Connects validation-retry to the workflow-step pattern. Cites Aider, Claude Code, and SWE-bench for the error-feedback-improves-fix-rates claim. AST context extraction is explained conceptually with a concrete enriched-error code block." }
guidance_adherence = { score = 5, justification = "Prior-art citations are present. The 'Why Blind Retries Waste Budget' section names the specific benchmark behavior. Error-placement section grounds the cache-breakpoint choice in prefix stability mechanics." }
cross_linking = { score = 5, justification = "Links to reference, howto, output-processing." }
quadrant_purity = { score = 4, justification = "The 'Validation Pipeline' section lists three named checks (ruff lint, ruff format, test execution) with bold labels and one-paragraph descriptions each — this is the closest the file comes to reference shape. The pipeline listing is functional but belongs in the reference; here, the narrative could reduce to 'the pipeline runs lint, format, and optionally tests.'" }
prose_quality = { score = 4, justification = "'The design reflects a general principle: compute what you can compute, and reserve LLM calls for what only the LLM can do' is a crisp summation. 'This is not a theoretical concern' lands. Some paragraphs could be 20% shorter without losing content, but none are bloated." }

[runs.files."explanation/llm-dispatch.md"]
coverage = { score = 5, justification = "All 8 covers items addressed with explicit sections: tier abstraction rationale, four tiers, planner-gets-reasoning, batch submit-wait-resume, 50% cost reduction, poller-signal delivery, document-completion-enables-batch, cache ordering + cache_control headers." }
detail_compliance = { score = 5, justification = "Unifies model routing and batch processing under 'how Forge manages LLM calls.' Both topics derive from the batch-first principle." }
guidance_adherence = { score = 5, justification = "Each subsection motivates why rather than how. Why abstract tiers, why planning gets reasoning, why document completion, why cache ordering." }
cross_linking = { score = 5, justification = "Links to reference, howto, workflow-step." }
quadrant_purity = { score = 4, justification = "The 'Four tiers' enumeration lists REASONING/GENERATION/SUMMARIZATION/CLASSIFICATION with purpose paragraphs each — a labeled enumeration that duplicates the reference's tier table. Poller-signal-delivery subsection is mechanistic. Not a fatal drift but measurable." }
prose_quality = { score = 4, justification = "'Better planning inputs produce better plans, and better plans reduce total work' is tight. 'This is not incidental' as a sentence opener appears and is fine; 'Designing every operation as a document completion is what makes the 50% cost reduction available' closes the batch section with economy. A few paragraphs in the prompt-caching section restate the ordering principle already stated in prompt-construction; a cross-reference would be tighter." }

[runs.files."explanation/observability.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: two-layer strategy, what is stored, best-effort writes, OTel span hierarchy, execution-journal framing." }
detail_compliance = { score = 5, justification = "Design-decisions framing throughout. Why SQLite (no payload limit), why separate from Temporal (payload size), why best-effort (observability not blocking task execution)." }
guidance_adherence = { score = 5, justification = "Each design choice is explained as a response to a specific constraint. D42 named explicitly for the best-effort write policy." }
cross_linking = { score = 5, justification = "Links to reference and howto. Log file paths and env var names appear inline, which is fine for explanation." }
quadrant_purity = { score = 4, justification = "The 'What Is Stored and Why' bullet list of interactions-table fields (task_id / step_id / role / system_prompt / …) is reference-shaped; the reference already owns this table, so the enumeration here could be cut to a one-sentence pointer. Log-files section near the end is practical guidance rather than explanation." }
prose_quality = { score = 4, justification = "'The observability store can be disabled entirely for testing by setting FORGE_DB_PATH to an empty string' is concrete. 'Understanding how this data is captured, where it lives, and why it is organized the way it is helps when diagnosing problems and when reasoning about workflow behavior' — the intro sentence is on the long side for one idea; 'Forge produces a large amount of intermediate data during workflow execution' repeats the point." }

[runs.files."explanation/forge-run-extraction.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: playbook concept, independent-workflow rationale, extraction pipeline (three activities), tag inference (deterministic from metadata), injection at priority 5, self-learning feedback loop." }
detail_compliance = { score = 5, justification = "Builds on core primitives explicitly — extraction uses the same document-completion pattern as everywhere else. Scope to forge's own store enforced." }
guidance_adherence = { score = 5, justification = "Opens by distinguishing this pipeline from transcript-ingestion (the other learning pipeline) and points to learning-loops for the cross-cutting story. Does not describe pbook's schema." }
cross_linking = { score = 5, justification = "Links to reference, howto, learning-loops, transcript-ingestion, context-assembly." }
quadrant_purity = { score = 4, justification = "Extraction pipeline section uses **bolded-activity-name:** format for each of the three activities — a labeled enumeration that borders on reference. The surrounding prose does explain why each activity exists, keeping it in explanation territory, but the form is procedural-shaped." }
prose_quality = { score = 5, justification = "'The loop works for successes too, not just failures' is a crisp pivot. Tag-inference prose states the same-logic-in-both-places symmetry and why it matters, in a compact paragraph. Final paragraph explicitly bounds the claim to forge's store and redirects to learning-loops — no fluff, useful scoping sentence." }

[runs.files."explanation/transcript-ingestion.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: ingestion-lives-in-forge rationale, two-workflow split, SUMMARIZATION-tier+batch-mode rationale, cross-queue contract, task-queue-split rationale, already-ingested tracking, why this pipeline produces pbook entries." }
detail_compliance = { score = 5, justification = "Discursive prose that explains the division of responsibility as the detail field asks. Opens with 'why does forge read Claude Code transcripts?' and answers it in terms of LLM-infrastructure ownership. Does not enumerate workflow inputs or CLI flags (those are in reference)." }
guidance_adherence = { score = 5, justification = "Division-of-responsibility framing is the thesis of the document. Link forward to learning-loops appears twice, including in the final paragraph." }
cross_linking = { score = 5, justification = "Links to prerequisites (llm-dispatch, forge-run-extraction), reference, howto, learning-loops." }
quadrant_purity = { score = 4, justification = "A mermaid sequence diagram of the cross-queue handoff is present — appropriate for explanation (it visualizes the relationship rather than instructing). The bolded two-workflow labels (**TranscriptIngestionWorkflow** and **BatchIngestionWorkflow**) each introduce one paragraph; the format is borderline reference but each paragraph carries explanatory prose." }
prose_quality = { score = 5, justification = "'The answer is that the expensive step is the LLM analysis, and forge is the system with the batch API infrastructure' — thesis stated directly. 'The pipeline never commits garbage to pbook's store on the theory that an empty ingestion is always better than a wrong one' — opinionated, precise, earns its place. Long paragraphs have load-bearing content." }

[runs.files."explanation/learning-loops.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: problem statement, two-loops reality, schema comparison, separation rationale (genuine vs. artifactual), convergence path as concrete refactor, hot-path latency cost, what-this-means-today." }
detail_compliance = { score = 5, justification = "Thesis-driven long-form. Two mermaid diagrams (parallel loops today, proposed unified pipeline) and a field-by-field schema table — all three artifacts earn their place by making the contrast concrete, which is explicitly what the detail field calls for." }
guidance_adherence = { score = 5, justification = "Thesis ('bifurcation is an accident of history plus one real design tension') stated in the opening paragraph and sustained throughout. Distinguishes genuine-separation (input schema richness, hot-path latency) from artifactual-separation (schema drift, duplicate extraction). Every paragraph carries weight." }
cross_linking = { score = 5, justification = "Links to forge-run-extraction, transcript-ingestion (prerequisites), context-assembly, external pbook repo." }
quadrant_purity = { score = 4, justification = "The schema-comparison table is the one place in the explanation where a table is load-bearing — the contrast is the point, as the guidance says. Two mermaid diagrams are explanatory (showing relationships). The numbered convergence-step list is prescriptive-shaped but each step is framed as 'concretely, this code change' rather than as a recipe." }
prose_quality = { score = 5, justification = "Long-form but never padded. 'That is a real information-loss boundary, not a refactoring inconvenience' — opinionated, precise. 'Every forge task reads playbooks during context assembly. Today that read is a local SQL query. Under convergence, it becomes a cross-queue Temporal workflow call' — clean parallel. 'Do not assume that a lesson captured in one store is visible from the other. It isn't' — direct. The prose earns its length throughout." }

[runs.files."explanation/task-domains.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: what a domain is+controls, task-agnostic-via-domains mechanism, four pipeline touchpoints, shared LLMResponse schema, five existing domains." }
detail_compliance = { score = 5, justification = "Concise. Connects each domain field to the task-agnostic principle. Shows how different prompts produce different behavior through the same pipeline." }
guidance_adherence = { score = 5, justification = "Explicitly states the coupling constraint: 'domain configuration can only influence the pipeline through text (prompts) and ValidationConfig booleans.' Naming this constraint is exactly what the explanation should do." }
cross_linking = { score = 5, justification = "Links to context-assembly and validation-and-retries as prerequisites, reference, and howto/add-domain." }
quadrant_purity = { score = 4, justification = "The bolded field walkthrough (**The role prompt**, **The output requirements**, etc.) enumerates DomainConfig fields in a shape reference already owns. The final 'Existing domains' section lists five domains with one paragraph each — also reference-shaped, but the prose focus is on purpose rather than fields, which keeps it in explanation." }
prose_quality = { score = 5, justification = "'This boundary is what makes the architecture extensible' directly names the consequence. 'The consequence is also a constraint' — pairs the benefit with its cost. Tight throughout, no filler." }

[runs.files."explanation/ocr-pipeline.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: builds on core primitives, two execution paths, image extraction + URI scheme, batch signals mirror LLM batch, multi-chunk gather, SHA-256 duplicate detection." }
detail_compliance = { score = 5, justification = "Framed as 'OCR as an instance of Forge's patterns'. Cross-references workflow-step and llm-dispatch instead of re-explaining them. Concrete Temporal primitives named (ABANDON parent-close policy, signal names)." }
guidance_adherence = { score = 5, justification = "Relationship-to-core-pipeline section makes the 'separate workflow family, shared infrastructure' framing explicit. Links out to the batch_jobs reference for schema detail." }
cross_linking = { score = 5, justification = "Links to workflow-step, llm-dispatch (including the #batch-mode anchor), external Mistral docs, reference, howto." }
quadrant_purity = { score = 4, justification = "Section headers like 'Document Splitting and Chunking', 'Image Extraction and the ocr-image:// URI Scheme', and 'SHA-256 Duplicate Detection' describe mechanisms rather than motivating them. Each section carries explanatory prose, but the subheading vocabulary skews toward 'how' rather than 'why'." }
prose_quality = { score = 4, justification = "'Rather than introducing new patterns, it composes the primitives already described' — clean framing sentence. The 2 MB signal-size rationale paragraph is concrete ('retaining embedded base64 data for a document with many images would exceed it'). A few sections could trim an intro sentence that restates the section header." }

[runs.files."explanation/planner-eval.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: why planner-eval matters, two evaluation modes, deterministic check purpose, LLM-as-judge mechanics, eval corpus framing." }
detail_compliance = { score = 5, justification = "Connects directly to 'planning is the hard part' principle. Two-mode framing — deterministic for structural validity, judge for quality — is the central explanatory move." }
guidance_adherence = { score = 5, justification = "Explains why both modes are needed (deterministic gates on validity, judge measures quality). Both are needed and complementary, not alternatives." }
cross_linking = { score = 5, justification = "Links to task-decomposition, reference, howto." }
quadrant_purity = { score = 3, justification = "The 'What Deterministic Checks Verify' section is seven bolded bullets enumerating the check names and what each verifies — this is exactly the reference's content. 'How LLM-as-Judge Scoring Works' lists six judge criteria with one-sentence descriptions each. Both enumerations duplicate the reference. An explanation-appropriate version would name a few categories of check (path safety, structural invariants, coverage) and point to the reference for the list." }
prose_quality = { score = 4, justification = "'If plan quality bounds everything that runs downstream, then measuring plan quality is the highest-leverage form of testing available' — the thesis sentence does heavy lifting. The two-mode framing paragraph is tight. The bullet-list sections are low-content per line because each bullet is one sentence of description." }

# --- References ---

[runs.files."reference/system-overview.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: principles table (7 rows), module map (every top-level module and every subpackage), tech stack, env vars (7 entries), file system layout." }
detail_compliance = { score = 5, justification = "Tabular throughout. One-sentence intros per section. No narrative prose beyond section introductions." }
guidance_adherence = { score = 5, justification = "Module map mirrors the src/forge/ directory tree section by section (top-level, activities/, code_intel/, eval/, llm_providers/, ocr/, alembic/). One-line descriptions per module." }
cross_linking = { score = 5, justification = "See Also section at the bottom points to explanation, sibling reference (workflow-step), and the tutorial." }
quadrant_purity = { score = 5, justification = "Pure reference. No instructions, no rationale, no opinion. Tables only." }
prose_quality = { score = 5, justification = "Intros are one sentence each. No filler. Table cells are consistently brief. Cannot be meaningfully shortened." }

[runs.files."reference/workflow-step.md"]
coverage = { score = 5, justification = "All 5 covers items addressed plus extras: activities broken out by role (core pipeline, context variants, planning, exploration, sanity check, conflict resolution, git, batch, file write), workflow definitions with dispatch paths, TransitionSignal + SanityCheckVerdict + deferred signals, retry policies per activity, and data models (ForgeTaskInput, TaskDefinition, TaskResult, StepResult, SubTaskResult, LLMCallResult, LLMStats, AssembledContext, LLMResponse, ValidationResult). Enum tables for TaskDomain, CapabilityTier, MatchLevel. Configuration models (ValidationConfig, ContextConfig, ModelConfig, ThinkingConfig). Source location table at the end." }
detail_compliance = { score = 5, justification = "Tabular. One table per activity category and per data model. Each data model table has field/type/required/description columns consistently." }
guidance_adherence = { score = 4, justification = "Activities ordered roughly by execution (core pipeline first, then planning/exploration/sanity/conflict/git/batch). The ForgeTaskWorkflow description embeds prose about dispatch behavior rather than keeping to a pure table — acceptable but closer to specification. No per-data-model 'see explanation' back-link on individual model tables (there is one in the intro paragraph)." }
cross_linking = { score = 4, justification = "Intro paragraph links to explanation. No See Also footer. Within-doc sections do not cross-reference sibling references (no link to task-decomposition reference for Plan/PlanStep/SubTask, which are documented there not here)." }
quadrant_purity = { score = 4, justification = "The ForgeTaskWorkflow subsection uses prose bullets ('Creates a worktree per attempt. Runs the five-phase pattern once per attempt.') instead of a pure table. This describes behavior rather than specifying it — borderline procedural." }
prose_quality = { score = 4, justification = "Intro sentences are brief. Section headings are consistent. The ForgeTaskWorkflow narrative is the weakest prose stretch — three dispatch paths each described in two or three sentences of procedural prose. Could be a table." }

[runs.files."reference/prompt-construction.md"]
coverage = { score = 5, justification = "All 3 covers items addressed: 11-row section table with source/stability/cache-behavior columns, cache breakpoint summary (3 rows), error injection format with a sample error block and AST context example." }
detail_compliance = { score = 5, justification = "The 11-row section table is the centerpiece, as the detail field requires. Cache breakpoint table follows. Error injection format shown with two concrete examples (raw error output, annotated AST context)." }
guidance_adherence = { score = 5, justification = "Section table includes the section number, name, source module/config, stability class, and cache behavior — all five columns the guidance specifies." }
cross_linking = { score = 5, justification = "Links to explanation, context-assembly reference. A 'Related references' table at the bottom points to three related reference files with one-line descriptions." }
quadrant_purity = { score = 5, justification = "Pure reference. Tables only. The error-injection section shows format via example blocks, not procedural instructions." }
prose_quality = { score = 5, justification = "Intros are one or two sentences. The section ordering sentence ('Sections toward the top are the most stable …') earns its place by justifying the column layout." }

[runs.files."reference/context-assembly.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: priority ordering (6 rows), provider table (12 providers), token budget algorithm with inputs/steps/estimation/utilization, six data models (AssembledContext, ContextStats, ContextConfig, ContextProviderSpec, ContextRequest, ExplorationResponse, ContextResult), seven CLI flags." }
detail_compliance = { score = 5, justification = "Tabular reference. Brief JSON examples for ContextRequest and ExplorationResponse formats. Algorithm is presented as numbered steps rather than prose — appropriate for reference." }
guidance_adherence = { score = 5, justification = "Does not duplicate the system prompt section table (that lives in prompt-construction reference). Explicit 'for the system prompt section table … see prompt-construction' pointer at the top." }
cross_linking = { score = 5, justification = "Links to prompt-construction reference, explanation, howto." }
quadrant_purity = { score = 5, justification = "Pure reference. Code examples are declarative shapes, not instruction." }
prose_quality = { score = 5, justification = "Brief, consistent table cells. No filler in the algorithm description ('4 characters per token (character-based heuristic). Conservative for English and code. Avoids tokenizer library dependencies')." }

[runs.files."reference/task-decomposition.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: Plan/PlanStep/SubTask fields, planner prompt inputs and outputs, execution mode selection, CLI flags (execution, retry, task definition, thinking), git worktree lifecycle (path+branch naming, per-mode lifecycle, cleanup), conflict resolution. JSON task file format with annotated field table." }
detail_compliance = { score = 5, justification = "Tabular throughout. One table per data model. Per-mode worktree lifecycle uses numbered prose steps, which is reference-appropriate because the sequence is part of the specification." }
guidance_adherence = { score = 5, justification = "Plan format → execution modes → CLI flags → git lifecycle follows the natural specification order. JSON task file format fully annotated." }
cross_linking = { score = 5, justification = "See Also points to explanation and howto. Sanity check verdicts table cross-references the activity definition in the workflow-step reference." }
quadrant_purity = { score = 4, justification = "The 'Lifecycle by execution mode' section uses numbered steps for each mode. Each step describes a deterministic event ('Create worktree branched from base_branch') rather than instructing the reader — this is reference-style specification, but the numbered-step form edges toward procedural." }
prose_quality = { score = 5, justification = "Brief intros, consistent tables. Task ID regex pattern stated inline without narrative. Cleanup rules stated as two short bulleted lists." }

[runs.files."reference/output-processing.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: LLMResponse/FileOutput/FileEdit/EditOperation fields, four-level matching algorithm with per-level failure conditions, fuzzy-match parameters (0.60 similarity, 0.05 uniqueness gap), edit application order and error conditions, file write behavior." }
detail_compliance = { score = 5, justification = "Precise thresholds. Each level specifies exact pass/fail conditions ('Count is zero: no match found; proceed to level 2'). Log messages per level listed in a table." }
guidance_adherence = { score = 5, justification = "Algorithm descriptions include thresholds and edge-case behavior (ambiguity → fail, not guess)." }
cross_linking = { score = 5, justification = "Related section links to explanation, validation-and-retries reference, and configure-validation howto." }
quadrant_purity = { score = 5, justification = "Pure reference. Declarative. No opinions." }
prose_quality = { score = 5, justification = "Tight. The failure-condition bullets per level are three lines each, consistent. Nothing to cut." }

[runs.files."reference/validation-and-retries.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: ValidationResult and ValidationConfig field tables, three validation checks (ruff_lint, ruff_format, tests) with tool/pass/fail columns, TransitionSignal mapping rules, error section format with AST context inclusion rules, retry behavior per execution mode, CLI flags." }
detail_compliance = { score = 5, justification = "Tabular. Error section shown as a concrete template with the attempt-number header. AST context inclusion rules given as a four-row table covering the possible cases." }
guidance_adherence = { score = 5, justification = "Validation model fields, checks, transition mapping, and CLI all present with consistent table structure." }
cross_linking = { score = 5, justification = "Related section lists four sibling references (explanation, output-processing reference, configure-validation howto, workflow-step reference)." }
quadrant_purity = { score = 5, justification = "Pure reference. The template-shaped error section is a format specification, not an instruction." }
prose_quality = { score = 5, justification = "Brief intros, consistent tables. The 'Check name (F401) and the short rule description are appended to the comment' sentence is the most narrative the file gets; it is one sentence explaining the annotation format." }

[runs.files."reference/llm-dispatch.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: CapabilityTier enum, default model mapping, call-site-to-tier assignments, ModelConfig fields, per-step tier override with JSON example, batch_jobs table schema, batch job states (8 states) with the storing invariant, batch signal names, forge_batch_id search attribute, BatchPollerWorkflow properties, cache control placement, cache token fields in LLMStats, OTel span attributes, CLI flags for forge run and forge worker." }
detail_compliance = { score = 5, justification = "Tabular. Every data structure (enum, table, model, config) gets its own small table. One JSON code example for per-step override." }
guidance_adherence = { score = 5, justification = "Configuration and batch mechanics kept in scope. Batch state invariant ('The poller never writes succeeded directly …') explicitly called out in prose because it is a correctness property the reference must convey." }
cross_linking = { score = 5, justification = "Links to explanation and howto at the top." }
quadrant_purity = { score = 5, justification = "Pure reference. The one-paragraph invariant narrative is a factual statement, not instruction." }
prose_quality = { score = 5, justification = "Tables do the work. The invariant paragraph is three sentences; no filler." }

[runs.files."reference/observability.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: interactions+runs+batch_jobs schemas, Alembic migration management, forge status and forge run flags, log files with rotation policy, env vars (5 entries), API message logs (--log-messages flag and file format), OTel spans (hierarchy, names, attributes). LLMStats model table at the end." }
detail_compliance = { score = 5, justification = "Complete. Tables for schemas, CLI flags, env vars, OTel spans. Sample span tree shown as an ASCII diagram." }
guidance_adherence = { score = 5, justification = "Alembic file paths listed. Rotation policy stated (10 MB, 5 backups). Log format string given." }
cross_linking = { score = 5, justification = "Links to explanation and howto at the top." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Intros one or two sentences. The ASCII span tree is a diagram, not prose. Nothing to cut." }

[runs.files."reference/forge-run-extraction.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: playbooks table schema, tag inference rules (file extensions, description keywords, default), PlaybookEntry / ExtractionResult / ExtractionWorkflowInput / ExtractionWorkflowResult models, forge extract / playbooks list / playbooks show / playbooks add CLI, playbook injection (representation type, priority, retrieval, budget behavior), scope boundary statement." }
detail_compliance = { score = 5, justification = "Tabular. Tag-inference rules split into two tables (extension mapping, keyword mapping) plus the default. CLI commands each get a subsection with option tables." }
guidance_adherence = { score = 5, justification = "Explicit scope boundary in the opening paragraph: 'does not describe pbook's entries table or any aspect of the transcript ingestion pipeline.' Redirects to transcript-ingestion reference." }
cross_linking = { score = 5, justification = "Links to explanation, howto, transcript-ingestion reference, learning-loops, context-assembly reference." }
quadrant_purity = { score = 5, justification = "Pure reference. Scope boundary enforced." }
prose_quality = { score = 5, justification = "Brief intros. Tables consistent. Nothing redundant." }

[runs.files."reference/transcript-ingestion.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: TranscriptIngestionWorkflow (input JSON, output, signals, early-exit conditions), BatchIngestionWorkflow (input, output, fan-out with child workflow IDs), prepare_transcript activity, cross-queue calls into pbook (ExtractionWorkflow child + record_ingested_session activity with arguments), experience-dict schema, model routing and batch config, forge ingest CLI (flags, env vars, exit codes, workflow ID construction), module layout." }
detail_compliance = { score = 5, justification = "Tabular. One table per subject. Intro sentences one or two sentences. No narrative prose beyond intros." }
guidance_adherence = { score = 5, justification = "Structure mirrors the code path: workflow → activity → cross-queue calls → CLI → module layout. Scope boundary to the forge side only, with pbook-side redirection." }
cross_linking = { score = 5, justification = "Links to explanation, howto, forge-run-extraction reference, learning-loops." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "All intros short. Tables do the heavy lifting." }

[runs.files."reference/task-domains.md"]
coverage = { score = 5, justification = "All 4 covers items addressed: TaskDomain enum, DomainConfig fields, pipeline touchpoints (context assembly, exploration, planner, CLI), validation defaults per domain. Plus per-domain examples for role_prompt, exploration_task_noun, exploration_completion_noun, and a domain registry code snippet." }
detail_compliance = { score = 5, justification = "Tabular. Per-domain example tables for three fields. One validation-defaults comparison table across all domains." }
guidance_adherence = { score = 5, justification = "One row per field with the activity that reads it. Per-domain examples." }
cross_linking = { score = 5, justification = "Links to explanation and howto." }
quadrant_purity = { score = 5, justification = "Pure reference. The registry code snippet is specification, not instruction." }
prose_quality = { score = 5, justification = "Brief. The one-sentence summary after the validation table ('code_generation enables ruff … All other domains disable ruff …') earns its place by explaining the pattern in the table." }

[runs.files."reference/ocr-pipeline.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: 8 OCR workflows with input/output/purpose columns, signals tables for OcrStoreWorkflow and OcrGatherWorkflow, all major data models (OcrSyncInput, OcrSubmitInput+Result, OcrStoreInput+Result, OcrGatherInput, OcrExportInput+Result, OcrBatchRef, OcrMarkInput+Result, ChunkRef, OcrDuplicateCheckResult, OcrListJobsInput+Result, OcrJobEntry), two-layer status model with derivation rules, ocr_results + ocr_images tables, shared batch_jobs table (scoped), ocr-image:// URI format, CLI commands via forge start." }
detail_compliance = { score = 5, justification = "Tabular. Every workflow, model, and database table gets its own table. Status model explained in two-sentence prose because the two-layer design needs the framing." }
guidance_adherence = { score = 5, justification = "OCR-specific columns noted. Shared batch_jobs scope redirected to llm-dispatch reference." }
cross_linking = { score = 5, justification = "Links to howto, explanation, llm-dispatch reference (both as a general link and as a specific #batch-job-states anchor)." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Intros brief. Status-derivation rules stated as four short bullet points — minimal, complete." }

[runs.files."reference/planner-eval.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: corpus format with directory structure and EvalCase schema, 9 deterministic checks with pass conditions and known_repo_files dependency, LLM-as-judge criteria (6 criteria) and scoring configuration, data models (DeterministicCheckResult, DeterministicResult, CheckStatus, JudgeCriterion, JudgeScore, JudgeVerdict, EvalCase, PlanEvalResult, EvalRunRecord, EvalComparison), forge eval-planner CLI flags and exit codes. Result storage path specified." }
detail_compliance = { score = 5, justification = "Tabular throughout. One JSON example of an EvalCase. Judge configuration table." }
guidance_adherence = { score = 5, justification = "Check name, verification scope, and pass condition per check. Judge criteria with description per criterion." }
cross_linking = { score = 5, justification = "Links to howto and explanation." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Brief intros. Run-comparison threshold stated in one sentence at the end of the judge section." }

[runs.files."reference/nushell-module.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: module location and load instruction, five commands (ocr submit, ocr list, ocr export doc, ocr mark, ocr unmark) with parameters/signature/returns tables, prerequisites (temporal CLI, forge worker, nushell version), internal architecture showing the workflow-per-command mapping." }
detail_compliance = { score = 5, justification = "One table per command. Return record/table schemas given. Parameter tables include type, required, default, and description." }
guidance_adherence = { score = 5, justification = "Each command gets its own subsection with consistent structure. Return value shapes shown for both --sync and batch paths of ocr submit." }
cross_linking = { score = 5, justification = "Links to howto and ocr-pipeline reference." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Tight. Module-location block is a two-row two-column table with no wasted text." }

# --- How-to Guides ---

[runs.files."howto/submit-tasks.md"]
coverage = { score = 4, justification = "5 of 6 covers items addressed: single-step, planned, JSON task file, fan-out depth, sanity check interval. The sixth — 'how to inspect plan output before execution starts' — is not present; the 'Inspect results' section covers post-execution inspection only. A dry-run or plan-preview recipe is missing." }
detail_compliance = { score = 5, justification = "CLI-focused recipes with brief intros and commands. JSON task file shown as an annotated example with a per-field table." }
guidance_adherence = { score = 5, justification = "Each recipe introduces the task in one or two sentences, then shows the command. JSON task format is presented with field descriptions." }
cross_linking = { score = 5, justification = "Links to explanation and reference at the top." }
quadrant_purity = { score = 4, justification = "Intro paragraphs explain rather than instruct ('The LLM receives the target file contents, auto-discovered imports, and repository structure. It produces edits for existing files or full content for new files.') — this is background the reader does not need for the task. A stricter how-to would drop these descriptions." }
prose_quality = { score = 4, justification = "Direct command-focused prose where the reader needs it. Some paragraphs (the pair after the single-step command, the prose around fan-out depth) describe pipeline behavior in sentences that could be cut or moved to the explanation." }

[runs.files."howto/control-context-assembly.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: --context-file, --include-deps, --no-explore/--max-exploration-rounds, diagnose file inclusion, check token budget utilization. Each is a dedicated section with CLI examples." }
detail_compliance = { score = 5, justification = "Short, task-focused sections. Each has one or two CLI examples and expected output. The budget utilization section shows a stats block." }
guidance_adherence = { score = 5, justification = "Each section addresses one specific 'I want to' scenario. Direct command examples." }
cross_linking = { score = 5, justification = "Links to explanation and reference at the top." }
quadrant_purity = { score = 4, justification = "The 'diagnose why a file was or was not included' section has four bulleted explanations (not in import graph, below budget, excluded by progressive disclosure, not a Python file) — these explain mechanics. The bullets include actionable next steps, which keeps them task-focused, but the structure verges on teaching." }
prose_quality = { score = 5, justification = "Command-focused. No filler. Expected-output blocks are minimal." }

[runs.files."howto/configure-validation.md"]
coverage = { score = 5, justification = "All 4 covers items addressed: enable/disable checks, add test execution, configure retry limits, inspect validation results. Additional sections for auto-fix and task-file-based config." }
detail_compliance = { score = 5, justification = "Short task-focused sections with CLI examples." }
guidance_adherence = { score = 5, justification = "Every section is a direct 'how do I …' recipe. No teaching. Links to the explanation and reference for context." }
cross_linking = { score = 5, justification = "Related section with three sibling references." }
quadrant_purity = { score = 5, justification = "Pure how-to. Each recipe is command-focused." }
prose_quality = { score = 5, justification = "Brief. Recipes are one or two commands plus a one-sentence note." }

[runs.files."howto/configure-llm-dispatch.md"]
coverage = { score = 5, justification = "All 4 covers items addressed: run a task in sync mode, check batch job status, configure batch poll interval, override model tier (both whole-task and per-step)." }
detail_compliance = { score = 5, justification = "Short CLI-focused recipes. Per-step tier override includes a JSON task file example." }
guidance_adherence = { score = 5, justification = "Task-focused sections. SQL query included for batch_jobs table inspection." }
cross_linking = { score = 5, justification = "Links to explanation and reference at the top, and to reference at the end." }
quadrant_purity = { score = 4, justification = "The --sync recipe includes a 'Use --sync when:' three-bullet explanation of when to reach for sync mode. This is useful guidance but edges into teaching. Acceptable in how-to as a decision aid, but a stricter version would drop it and rely on the explanation." }
prose_quality = { score = 4, justification = "Command-focused. The prose inserts between examples are one or two sentences each. Nothing significant to cut." }

[runs.files."howto/debug-workflow.md"]
coverage = { score = 5, justification = "All 7 covers items addressed as numbered sections: inspect workflow history, find prompt for a specific step, diagnose step validation failure, check token budget utilization, read log files, enable API message logs, use verbose output for real-time debugging." }
detail_compliance = { score = 5, justification = "Each section starts with a 'Symptom:' line and walks through numbered diagnostic steps, exactly as the detail field specifies." }
guidance_adherence = { score = 5, justification = "Symptom-based structure throughout. jq pipelines included for scripting; temporal workflow CLI commands included." }
cross_linking = { score = 5, justification = "Links to reference and explanation at the top." }
quadrant_purity = { score = 5, justification = "Pure how-to. Each step is actionable. No teaching." }
prose_quality = { score = 5, justification = "Direct, command-focused. Output samples are minimal. Tight throughout." }

[runs.files."howto/manage-playbooks.md"]
coverage = { score = 5, justification = "All 4 covers items addressed: run extraction, list and inspect, manually add, verify playbooks appear in future contexts." }
detail_compliance = { score = 5, justification = "Short CLI-focused recipes. Scope statement in the intro: scoped to forge's own store, with redirect to ingest-transcripts for pbook." }
guidance_adherence = { score = 5, justification = "Task-focused sections. SQL command shown for direct-database correction." }
cross_linking = { score = 5, justification = "Links to ingest-transcripts, explanation, reference." }
quadrant_purity = { score = 5, justification = "Pure how-to." }
prose_quality = { score = 5, justification = "Brief. Scope sentence in the intro is one clause." }

[runs.files."howto/ingest-transcripts.md"]
coverage = { score = 5, justification = "All 6 covers items addressed as dedicated recipes: single session, --all discovery, project filter, dry-run, --force reprocess, debug stuck ingestion." }
detail_compliance = { score = 5, justification = "Short CLI-focused recipes with numbered steps. Sample output shown for most recipes." }
guidance_adherence = { score = 5, justification = "Title starts with 'How to'. Prerequisites block names the three runtime dependencies (forge worker, pbook worker, batch poller schedule)." }
cross_linking = { score = 5, justification = "Links to explanation and reference at the top. Cross-references forge status for poller state." }
quadrant_purity = { score = 4, justification = "Debug-stuck-ingestion section has four numbered steps where each contains a one-sentence explanation of what to look for. These are debugging decision aids — borderline teaching but task-focused. The 'Reprocessing creates new entries; it does not update existing ones' note in the --force recipe explains a failure mode rather than prescribing action." }
prose_quality = { score = 5, justification = "Tight. No filler. Sample-output blocks are minimal." }

[runs.files."howto/add-domain.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: add to TaskDomain enum, create config in domains.py, update CLI option, add tests, write prompts and validation defaults. Step-numbered structure." }
detail_compliance = { score = 5, justification = "Step-by-step recipe with code-level diffs (show-this-line-added-here)." }
guidance_adherence = { score = 5, justification = "Assumes competence. No teaching. Each step shows the exact code change." }
cross_linking = { score = 5, justification = "Links to explanation and reference at the top." }
quadrant_purity = { score = 4, justification = "The 'Checklist for the config' subsection lists 8 bulleted review items for the config. This is decision support for the task, but the format is closer to a checklist than to a set of actions the reader takes in sequence." }
prose_quality = { score = 5, justification = "Direct. Code blocks show only the relevant lines with ← pointers. No filler." }

[runs.files."howto/run-ocr.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: sync OCR, batch OCR submission, check batch status, list submissions, export results, multi-chunk handling. Plus soft-delete and hash backfill." }
detail_compliance = { score = 5, justification = "CLI-focused recipes. Each command shown with its expected JSON output." }
guidance_adherence = { score = 5, justification = "Every recipe is a forge start + JSON input invocation. Multi-chunk section correctly explains the workflow manages it transparently." }
cross_linking = { score = 5, justification = "Links to explanation, reference, Mistral batch docs." }
quadrant_purity = { score = 5, justification = "Pure how-to." }
prose_quality = { score = 5, justification = "Brief. Expected-output blocks trimmed." }

[runs.files."howto/run-evaluations.md"]
coverage = { score = 5, justification = "All 4 covers items addressed: run against corpus, create a new eval case, run with judge scoring, interpret results. Plus save-to-file, JSON output, evaluate-pre-generated-plans." }
detail_compliance = { score = 5, justification = "CLI-focused recipes with sample output." }
guidance_adherence = { score = 5, justification = "Task-focused sections. JSON eval case shown as a complete example." }
cross_linking = { score = 5, justification = "Links to explanation and reference (including a #planevalresult deep link)." }
quadrant_purity = { score = 4, justification = "The 'Interpret evaluation results' section has four bold-labeled definitions (Deterministic PASS, FAIL, SKIP, Judge scores) that explain what each outcome means. This is reader decision support, but the format is glossary-shaped — borderline reference." }
prose_quality = { score = 5, justification = "Command-focused. Sample outputs abbreviated." }

[runs.files."howto/use-nushell-module.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: load module, submit documents, list and filter, export, mark/unmark, compose with pipelines." }
detail_compliance = { score = 5, justification = "Nu commands with one-line descriptions. Pipeline examples show par-each, filter, sort-by composition." }
guidance_adherence = { score = 5, justification = "Every recipe is a nu command. No Python, no forge CLI equivalents." }
cross_linking = { score = 5, justification = "Links to reference and explanation at the top." }
quadrant_purity = { score = 5, justification = "Pure how-to." }
prose_quality = { score = 5, justification = "Very brief. Most recipes are a one-liner with a one-sentence intro." }

# =============================================================================
# Post-fix pass (2026-04-20)
# =============================================================================

[[runs]]
timestamp = "2026-04-20T12:55:00Z"
description = "Fresh qualitative pass after the fix-all-issues round (phase-name drift, dry-run recipe, plan-then-execute contrast, de-enumeration of task-decomposition and planner-eval, two mermaid diagrams). Re-read the edited files fresh; unchanged files scored from their confirmed-unchanged current state. No scores copied from prior runs."

[runs.project]
topic_decomposition = { score = 5, justification = "17 topics with a clean progression — foundation (system-overview, golden-path, workflow-step), pipeline internals, platform (observability), learning loops, parameterization (task-domains), extensions (ocr, eval, nushell). No redundancy. The Hugo-migration pass structured this cleanly; this pass's edits did not change topic scope." }
quadrant_balance = { score = 4, justification = "15 explanation / 15 reference / 11 howto / 1 tutorial. For project-docs, the how-to + reference weighting is correct. The single tutorial (golden-path) is deliberate but leaves extension topics (OCR, eval, ingestion) without a guided first-run on-ramp. Same gap as prior passes; no change." }
audience_alignment = { score = 5, justification = "Language assumes Python/Temporal/LLM familiarity as declared. External links to Temporal, Anthropic, Mistral, Hextra replace re-explaining foundations. Complexity scales with topic." }
completeness = { score = 5, justification = "All 42 files in diataxis.toml carry content. check-status-consistency passes. No stubs." }

# --- Tutorial ---

[runs.files."tutorials/golden-path.md"]
coverage = { score = 5, justification = "All 9 covers items addressed as labeled steps: submission, planner decomposition, context assembly, exploration, assembled prompt, structured output, edit application, validation failure+retry, commit, verbose inspection. Unchanged since prior pass." }
detail_compliance = { score = 4, justification = "Concrete add-a-stats-command example, observable output per step, ~20min read. Step 5 ('Examine the assembled prompt structure') remains a two-paragraph signpost rather than a step with visible output — the tutorial correctly avoids re-explaining prompt structure but breaks the cadence. Unchanged from prior pass." }
guidance_adherence = { score = 4, justification = "Most steps end with 'For more on X, see …' callouts that honor the do-not-explain-internals-inline guidance. 'What you built' and 'Where to go next' sections present. Step 9's cache-arithmetic aside ('4.9s vs 7.8s because 8,689 of 9,536 input tokens served from cache') is explanatory content that the linked prompt-construction doc should own." }
cross_linking = { score = 5, justification = "Callouts after every step. 'Where to go next' lists 7 destinations." }
quadrant_purity = { score = 4, justification = "Mostly a walkthrough. The 'Context around error' text block in Step 9 skirts reference shape (it shows section structure rather than narrating what the learner observes). Step 8 briefly explains why validation mapped to FAILURE_RETRYABLE." }
prose_quality = { score = 4, justification = "Direct prose with frequent observable output. Minor filler: 'Notice that we did not specify --target-file', 'After a few seconds', the sentence about step boundaries that restates the prior one. Trimmable but not disqualifying. Unchanged since prior pass." }

# --- Explanations ---

[runs.files."explanation/system-overview.md"]
coverage = { score = 5, justification = "All 6 covers items now addressed. The previously-missing 'Plan-then-execute model vs. future dynamic task evolution' item is now covered by a dedicated three-paragraph section naming the three deferred signals and the explicit trade-off." }
detail_compliance = { score = 5, justification = "High-level orientation piece, no code, no CLI commands, clear mental map. The detail field's 'Diagrams showing component relationships are encouraged' is now met — a mermaid flowchart shows CLI, Temporal, workflows, activities, code intelligence, Anthropic API, worktrees, and the observability store with solid/dashed lines for control vs. observability paths." }
guidance_adherence = { score = 5, justification = "Principles grounded in consequences. Plan-then-execute vs. dynamic-evolution contrast is now explicit (updated guidance requires it). Temporal docs linked. Golden-path and workflow-step cross-links present." }
cross_linking = { score = 5, justification = "Links to reference, workflow-step, golden-path tutorial, external Temporal docs and Anthropic Batch API docs." }
quadrant_purity = { score = 5, justification = "No tables, no numbered procedures, no field enumerations. Even the 'seven architecture principles' section uses prose under each principle to argue consequences rather than tabulating them. Diagram is explanatory (shows component relationships), not instructional." }
prose_quality = { score = 4, justification = "Generally direct. One mild redundancy survives: the 'Forge inverts this relationship' argument is set up across two consecutive paragraphs (lines 24 and 26) before the batch-API motivation lands at line 28 — the argument does build, but a tighter version would merge the chat-loop critique and the inversion into one paragraph. 'Nothing more.' closes paragraph 26 with emphasis that works. The new plan-then-execute section is three paragraphs that each make a distinct point; none are padded." }

[runs.files."explanation/workflow-step.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: five-phase pattern, task-agnostic orchestration, Temporal-vs-activity relationship, statelessness, transition vocabulary, orchestrator-in-control." }
detail_compliance = { score = 5, justification = "Conceptual deep-dive. State-machine analogy used. Explains why each decision matters." }
guidance_adherence = { score = 5, justification = "The updated guidance pins the canonical phase vocabulary (Construct, Send, Receive and Serialize, Validate, Transition) and requires it everywhere in the file. This pass verified every enumeration: opening diagram (line 19), numbered list (lines 29-33), state-machine analogy (line 70), testability paragraph (line 94). All four now use the exact same five phase names in the exact same order. The correctness bug from the prior pass is fixed. Chat-loop contrast, testability, and observability are all present as consequences. Prerequisite frontmatter description updated to match." }
cross_linking = { score = 5, justification = "Links to reference/workflow-step and golden-path tutorial appear in paragraph-appropriate positions (not only trailing)." }
quadrant_purity = { score = 4, justification = "The five-phase numbered list and the three-signal bullet list remain — each bullet carries explanatory prose (what the phase does, what the signal means) rather than being a reference-style field enumeration, but the shape is still enumerative. A stricter editor would push the numbered list into the reference and describe the phases in flowing prose here. Borderline but not contaminated." }
prose_quality = { score = 5, justification = "The phase-name drift that cost this file prose quality in the prior pass is fixed. The 'A useful way to think about …' throat-clearer is removed; the state-machine section now opens 'The universal workflow step is a state machine where …'. 'This is not an implementation shortcut. It is the central design decision' — still tight. 'This is the entire control loop. There is no other path through the system' — direct and load-bearing. Nothing to cut." }

[runs.files."explanation/prompt-construction.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: structured-document vs chat, 11 sections with cache-optimized ordering, prefix-caching rationale, 3 cache breakpoints, AST-derived error injection, why errors go last. Unchanged from prior pass." }
detail_compliance = { score = 5, justification = "Discursive prose. Explicit scope boundary ('does not cover how Forge decides which files …'). Unchanged." }
guidance_adherence = { score = 5, justification = "Describes the destination (prompt structure) not the journey (discovery). Cross-references context-assembly. Cache mechanics grounded in prefix stability. Unchanged." }
cross_linking = { score = 5, justification = "Links to workflow-step (prerequisite), context-assembly, reference, golden-path tutorial." }
quadrant_purity = { score = 4, justification = "Each of the 11 sections has a labeled entry (**Sections 1–2:**, etc.) — borderline reference shape — but each entry carries explanatory prose about what the section is for and why it belongs in its position. Functional but enumerative." }
prose_quality = { score = 4, justification = "Strong parallelism in the opening paragraph ('if a relevant file is missing … if the token budget is wasted … if the ordering is wrong …'). Most paragraphs earn their length. One observation repeats content from the golden-path tutorial (attempt-2-is-faster-because-cache) — acceptable cross-reference for an explanation but worth a future audit." }

[runs.files."explanation/context-assembly.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: import graph, PageRank, budget packing, progressive disclosure, exploration loop, separate-document-completion rationale, tradeoffs. Unchanged." }
detail_compliance = { score = 5, justification = "Walks through discovery → ranking → packing → disclosure → exploration → tradeoffs. Does not re-describe section structure." }
guidance_adherence = { score = 5, justification = "Dividing-line paragraph at top restates the separation. Tradeoffs section cites production evidence (Aider, Sourcegraph)." }
cross_linking = { score = 5, justification = "Links to prompt-construction (prerequisite), workflow-step, reference, howto, golden-path tutorial." }
quadrant_purity = { score = 4, justification = "'Token Budget Packing' subsection uses procedural sentences ('Items are packed in priority order. Within each priority tier …') that describe the algorithm. The algorithm belongs in the reference; here, prose should motivate the knapsack-style-over-alternatives choice." }
prose_quality = { score = 4, justification = "Tradeoffs section is the prose high point — three bullet-leading paragraphs that each state a choice and defend it. One long paragraph on the batch-compatibility rationale for separate exploration rounds could be split." }

[runs.files."explanation/task-decomposition.md"]
coverage = { score = 5, justification = "All 9 covers items addressed: three modes, planner inputs+outputs, why planning gets premium resources, fan-out dispatch, gather+conflict resolution, nested fan-out, sanity checks, git strategy, final result assembly." }
detail_compliance = { score = 5, justification = "Lifecycle from submission to committed result covered. The detail field's diagram requirement is now met — the new mermaid flowchart shows parent → per-sub-task child workflows → gather → conflict-resolution → merge → validate → commit, with the decision node for file conflicts." }
guidance_adherence = { score = 5, justification = "The updated guidance forbids PlanStep field bullet lists, SanityCheckVerdict value bullets, and Dispatch/Execution/Gather/Conflict/Merge-shaped Fan-Out-Mechanics subheaders. All three are gone: 'What the planner produces' is now one prose paragraph that describes what each step carries (files, context, fan-out, tier override) and links to reference for field-level detail; the sanity-check section is one paragraph describing the three-outcome verdict shape in prose and linking to reference for verdict names; fan-out mechanics is a diagram plus four prose paragraphs that motivate each stage (why children don't commit, why conflict resolution is the universal workflow step, why validation runs on the merge). The result is explanation that explains rather than specifies." }
cross_linking = { score = 5, justification = "Links to workflow-step, reference, tutorial. Further Reading section at the end." }
quadrant_purity = { score = 5, justification = "The enumerations that pulled this file down to 3 in the prior pass are removed. Remaining subsections (### Single-step, ### Planned multi-step, ### Fan-out / gather under 'Three Execution Modes'; ### Worktree-per-task isolation, ### Human-gated merges, ### Disposable worktrees under 'Git Strategy') are explanatory — each partitions a conceptual boundary and carries prose that motivates the boundary rather than specifies its contents. Clean explanation throughout." }
prose_quality = { score = 5, justification = "The new fan-out mechanics prose flows. 'Crucially, children do not commit' — direct. 'Validation is deliberately run on the merge, not on the individual children, because correctness is a property of what lands in the tree' — concrete rationale, not restatement. 'Sanity-checking is another application of the universal pattern, not a new control structure' earns the paragraph it closes." }

[runs.files."explanation/output-processing.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: tool-use over free-form, LLMResponse schema split, D50 search/replace rationale, four-level fallback with motivation per level, sequential application, ambiguity-as-error. Unchanged." }
detail_compliance = { score = 5, justification = "Design decisions focus. 'Walking Through the Chain' example shows trailing-whitespace and indentation cases. Unchanged." }
guidance_adherence = { score = 5, justification = "Explains why search/replace, why four levels. Grounded in specific failure modes. Unchanged." }
cross_linking = { score = 5, justification = "Links to reference, context-assembly, validation-and-retries." }
quadrant_purity = { score = 4, justification = "Each of the four fallback levels has a subsection with motivation + one example. The subsections are explanation-appropriate content wrapped in a reference-adjacent structure." }
prose_quality = { score = 4, justification = "Good contrastive framing ('Early LLM orchestrators parsed code out of markdown fenced blocks. This is fragile'). 'The problem is one of scale and scope' is a mild throat-clearer before the two concrete failure modes." }

[runs.files."explanation/validation-and-retries.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: deterministic-before-LLM, validation pipeline, transition mapping, blind-retry cost, AST error construction, cache-efficient error placement, per-mode retry semantics. Unchanged." }
detail_compliance = { score = 5, justification = "Connects validation-retry to the workflow-step pattern. Cites Aider, Claude Code, SWE-bench. AST context shown with a concrete enriched-error block." }
guidance_adherence = { score = 5, justification = "Prior-art citations present. Error-placement section grounds cache-breakpoint choice in prefix stability mechanics." }
cross_linking = { score = 5, justification = "Links to reference, howto, output-processing." }
quadrant_purity = { score = 4, justification = "The 'Validation Pipeline' section bolded-labels three checks (ruff lint, ruff format, test execution) with one-paragraph descriptions each — closest the file comes to reference shape. Belongs in the reference; the narrative could reduce to 'the pipeline runs lint, format, and optionally tests.'" }
prose_quality = { score = 4, justification = "'Compute what you can compute, and reserve LLM calls for what only the LLM can do' summarizes the principle crisply. 'This is not a theoretical concern' lands. Some paragraphs could be 20% shorter without losing content." }

[runs.files."explanation/llm-dispatch.md"]
coverage = { score = 5, justification = "All 8 covers items addressed: tier abstraction rationale, four tiers, planner-gets-reasoning, batch submit-wait-resume, 50% cost reduction, poller-signal delivery, document-completion-enables-batch, cache ordering + cache_control headers. Unchanged." }
detail_compliance = { score = 5, justification = "Unifies model routing and batch processing under 'how Forge manages LLM calls.' Both derive from the batch-first principle." }
guidance_adherence = { score = 5, justification = "Each subsection motivates why, not how." }
cross_linking = { score = 5, justification = "Links to reference, howto, workflow-step." }
quadrant_purity = { score = 4, justification = "The 'Four tiers' enumeration lists REASONING/GENERATION/SUMMARIZATION/CLASSIFICATION with purpose paragraphs each — duplicates the reference's tier table. Poller-signal-delivery subsection reads mechanistically." }
prose_quality = { score = 4, justification = "'Better planning inputs produce better plans, and better plans reduce total work' lands. 'Designing every operation as a document completion is what makes the 50% cost reduction available' closes the batch section with economy. A few paragraphs in prompt-caching restate content from prompt-construction — cross-reference would be tighter." }

[runs.files."explanation/observability.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: two-layer strategy, what is stored, best-effort writes, OTel span hierarchy, execution-journal framing. Unchanged." }
detail_compliance = { score = 5, justification = "Design-decisions framing. Why SQLite, why separate from Temporal, why best-effort." }
guidance_adherence = { score = 5, justification = "Each design choice is explained as a response to a constraint. D42 named explicitly for best-effort write policy." }
cross_linking = { score = 5, justification = "Links to reference and howto." }
quadrant_purity = { score = 4, justification = "The 'What Is Stored and Why' bullet list of interactions-table fields is reference-shaped; the reference already owns this table, so the enumeration here could be cut to a one-sentence pointer. Log-files section near the end is practical guidance rather than explanation." }
prose_quality = { score = 4, justification = "'The observability store can be disabled entirely by setting FORGE_DB_PATH to an empty string' is concrete. Intro sentence is on the long side for one idea." }

[runs.files."explanation/forge-run-extraction.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: playbook concept, independent-workflow rationale, extraction pipeline, tag inference, injection at priority 5, self-learning loop. Unchanged." }
detail_compliance = { score = 5, justification = "Builds on core primitives explicitly — extraction uses the same document-completion pattern. Scope to forge's own store enforced." }
guidance_adherence = { score = 5, justification = "Distinguishes this pipeline from transcript-ingestion and points to learning-loops for cross-cutting story. Does not describe pbook's schema." }
cross_linking = { score = 5, justification = "Links to reference, howto, learning-loops, transcript-ingestion, context-assembly." }
quadrant_purity = { score = 4, justification = "Extraction pipeline section uses **bolded-activity-name:** format for three activities — labeled enumeration bordering on reference. Surrounding prose keeps it explanatory." }
prose_quality = { score = 5, justification = "'The loop works for successes too, not just failures' — crisp pivot. Tag-inference paragraph states the same-logic-in-both-places symmetry compactly. Final paragraph bounds the claim and redirects to learning-loops." }

[runs.files."explanation/transcript-ingestion.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: ingestion-lives-in-forge rationale, two-workflow split, SUMMARIZATION-tier+batch-mode rationale, cross-queue contract, task-queue-split rationale, already-ingested tracking, why pbook entries. Unchanged." }
detail_compliance = { score = 5, justification = "Discursive prose. Opens with 'why does forge read Claude Code transcripts?' and answers it in terms of LLM-infrastructure ownership. Does not enumerate workflow inputs or CLI flags." }
guidance_adherence = { score = 5, justification = "Division-of-responsibility framing is the thesis. Link forward to learning-loops appears twice." }
cross_linking = { score = 5, justification = "Links to prerequisites, reference, howto, learning-loops." }
quadrant_purity = { score = 4, justification = "A mermaid sequence diagram of the cross-queue handoff — appropriate (visualizes relationship). Two **bolded workflow names** each introduce one paragraph — borderline reference format." }
prose_quality = { score = 5, justification = "'The answer is that the expensive step is the LLM analysis, and forge is the system with the batch API infrastructure' — thesis stated directly. 'The pipeline never commits garbage to pbook's store on the theory that an empty ingestion is always better than a wrong one' — opinionated, precise." }

[runs.files."explanation/learning-loops.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: problem statement, two-loops reality, schema comparison, separation rationale (genuine vs. artifactual), convergence path, hot-path latency cost, what-this-means-today. Unchanged." }
detail_compliance = { score = 5, justification = "Thesis-driven long-form. Two mermaid diagrams (parallel loops today, proposed unified pipeline) and a field-by-field schema table. All three earn their place." }
guidance_adherence = { score = 5, justification = "Thesis stated in the opening paragraph and sustained. Distinguishes genuine-separation from artifactual-separation." }
cross_linking = { score = 5, justification = "Links to forge-run-extraction, transcript-ingestion, context-assembly, external pbook repo." }
quadrant_purity = { score = 4, justification = "Schema-comparison table is load-bearing — the contrast is the point, per guidance. Two mermaid diagrams explanatory. The numbered convergence-step list is prescriptive-shaped but each step is framed as 'concretely, this code change' rather than as a recipe." }
prose_quality = { score = 5, justification = "'That is a real information-loss boundary, not a refactoring inconvenience' — opinionated, precise. 'Do not assume that a lesson captured in one store is visible from the other. It isn't' — direct. Long paragraphs are load-bearing." }

[runs.files."explanation/task-domains.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: what a domain is+controls, task-agnostic-via-domains, four touchpoints, shared LLMResponse schema, five existing domains. Unchanged." }
detail_compliance = { score = 5, justification = "Concise. Connects each field to the task-agnostic principle." }
guidance_adherence = { score = 5, justification = "Names the coupling constraint: 'domain configuration can only influence the pipeline through text and ValidationConfig booleans.'" }
cross_linking = { score = 5, justification = "Links to prerequisites, reference, howto/add-domain." }
quadrant_purity = { score = 4, justification = "Bolded field walkthrough enumerates DomainConfig fields in a shape reference already owns. 'Existing domains' section lists five domains with one paragraph each — reference-shaped." }
prose_quality = { score = 5, justification = "'This boundary is what makes the architecture extensible' names the consequence directly. 'The consequence is also a constraint' pairs benefit with cost. Tight throughout." }

[runs.files."explanation/ocr-pipeline.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: builds on core primitives, two execution paths, image extraction + URI scheme, batch signals mirror LLM batch, multi-chunk gather, SHA-256 duplicate detection. Unchanged." }
detail_compliance = { score = 5, justification = "Framed as 'OCR as an instance of Forge's patterns'. Cross-references workflow-step and llm-dispatch." }
guidance_adherence = { score = 5, justification = "Relationship-to-core-pipeline section explicit." }
cross_linking = { score = 5, justification = "Links to workflow-step, llm-dispatch (with anchor), external Mistral docs, reference, howto." }
quadrant_purity = { score = 4, justification = "Section headers describe mechanisms rather than motivating them ('Document Splitting and Chunking', 'Image Extraction and the ocr-image:// URI Scheme'). Prose underneath is explanatory but vocabulary skews how-oriented." }
prose_quality = { score = 4, justification = "'Rather than introducing new patterns, it composes the primitives already described' — clean framing. A few sections could trim an intro sentence that restates the header." }

[runs.files."explanation/planner-eval.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: why planner-eval matters, two evaluation modes, deterministic check purpose, LLM-as-judge mechanics, eval corpus framing." }
detail_compliance = { score = 5, justification = "Connects directly to 'planning is the hard part' principle. Two-mode framing is the central explanatory move." }
guidance_adherence = { score = 5, justification = "The updated guidance forbids enumerating the deterministic checks or the judge criteria. This pass verified both are gone: 'What Deterministic Checks Verify' is now one prose paragraph naming categories (path-safety, structural invariants, coverage, ordering, file-plausibility) plus one sentence noting the known_repo_files dependency, with a pointer to the reference. 'How LLM-as-Judge Scoring Works' is one paragraph describing the dimensions in prose ('whether the decomposition is complete, whether step sizes are appropriate, whether the ordering lets each step build on its predecessors, whether context choices are plausible, whether fan-out is used where it helps, whether the plan's own explanation is coherent') plus one paragraph on defaults and overrides. The authoritative enumerations live in the reference, as the guidance requires." }
cross_linking = { score = 5, justification = "Links to task-decomposition, reference, howto." }
quadrant_purity = { score = 5, justification = "The seven-check and six-criterion bullet lists that pulled this file down to 3 in the prior pass are gone. The file is now pure explanation: two-mode framing, categorical descriptions, and pointers to reference for the enumeration. No instruction, no field tables, no specification." }
prose_quality = { score = 5, justification = "'If plan quality bounds everything that runs downstream, then measuring plan quality is the highest-leverage form of testing available' — thesis sentence does heavy lifting. 'These are not alternatives — they address different questions' — direct. The replacement deterministic-checks paragraph ('Deterministic checks are pure functions over a Plan and its TaskDefinition. They fall into a few categories …') compacts the prior seven bullets into one load-bearing sentence-run. Nothing wasted." }

# --- References ---

[runs.files."reference/system-overview.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: principles table (7 rows), module map (every top-level module and every subpackage), tech stack, env vars, file system layout. Unchanged." }
detail_compliance = { score = 5, justification = "Tabular throughout. One-sentence intros." }
guidance_adherence = { score = 5, justification = "Module map mirrors src/forge/ directory tree section by section. One-line descriptions." }
cross_linking = { score = 5, justification = "See Also footer points to explanation, sibling reference, tutorial." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Intros are one sentence each. No filler. Cannot be meaningfully shortened." }

[runs.files."reference/workflow-step.md"]
coverage = { score = 5, justification = "All 5 covers items addressed plus extras. Activities broken out by role; workflows with dispatch paths; TransitionSignal + SanityCheckVerdict + deferred signals; retry policies per activity; data models and configuration models." }
detail_compliance = { score = 5, justification = "Tabular. One table per activity category and per data model. Frontmatter description now updated to match canonical phase vocabulary." }
guidance_adherence = { score = 4, justification = "Activities roughly in execution order. No per-model 'see explanation' back-link on individual model tables (there is one in the intro paragraph). The 'Activities are organized by their role in the five-phase pattern: Construct, Send, Receive/Serialize, Validate, and Transition' sentence at the top of the Activities section uses slash-separated 'Receive/Serialize' rather than the canonical 'Receive and Serialize' — minor drift from the explanation's vocabulary, worth aligning." }
cross_linking = { score = 4, justification = "Intro paragraph links to explanation. No See Also footer. Within-doc sections do not cross-reference sibling references for shared models (e.g., Plan/PlanStep/SubTask are documented in task-decomposition reference, not here, without a cross-pointer)." }
quadrant_purity = { score = 4, justification = "The ForgeTaskWorkflow subsection uses prose bullets ('Creates a worktree per attempt. Runs the five-phase pattern once per attempt.') instead of a pure table — describes behavior rather than specifying it." }
prose_quality = { score = 4, justification = "Intros brief. The ForgeTaskWorkflow narrative is the weakest prose stretch — three dispatch paths each described in two or three sentences of procedural prose. Could be a table." }

[runs.files."reference/prompt-construction.md"]
coverage = { score = 5, justification = "All 3 covers items addressed: 11-row section table with source/stability/cache-behavior columns, cache breakpoint summary, error injection format with a sample error block and AST context example. Unchanged." }
detail_compliance = { score = 5, justification = "11-row section table is the centerpiece." }
guidance_adherence = { score = 5, justification = "All five columns the guidance specifies present." }
cross_linking = { score = 5, justification = "Links to explanation, context-assembly reference. 'Related references' table at the bottom." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Intros are one or two sentences." }

[runs.files."reference/context-assembly.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: priority ordering (6 rows), provider table (12 providers), token budget algorithm, six data models, seven CLI flags. Unchanged." }
detail_compliance = { score = 5, justification = "Tabular. Brief JSON examples for request/response formats. Algorithm as numbered steps." }
guidance_adherence = { score = 5, justification = "Does not duplicate the system prompt section table." }
cross_linking = { score = 5, justification = "Links to prompt-construction reference, explanation, howto." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Brief, consistent table cells." }

[runs.files."reference/task-decomposition.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: Plan/PlanStep/SubTask fields, planner prompt inputs and outputs, execution mode selection, CLI flags, git worktree lifecycle, conflict resolution. JSON task file format annotated." }
detail_compliance = { score = 5, justification = "Tabular throughout. One table per data model." }
guidance_adherence = { score = 5, justification = "Plan format → execution modes → CLI flags → git lifecycle follows natural spec order." }
cross_linking = { score = 5, justification = "See Also points to explanation and howto. Sanity check verdicts cross-reference workflow-step reference." }
quadrant_purity = { score = 4, justification = "Lifecycle by execution mode uses numbered steps for each mode. Each step describes a deterministic event — reference-style specification, but numbered-step form edges toward procedural." }
prose_quality = { score = 5, justification = "Brief intros, consistent tables. Cleanup rules stated as two short bulleted lists." }

[runs.files."reference/output-processing.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: data models, four-level matching with per-level failure conditions, fuzzy-match parameters, edit application order, file write behavior. Unchanged." }
detail_compliance = { score = 5, justification = "Precise thresholds. Each level specifies exact pass/fail conditions." }
guidance_adherence = { score = 5, justification = "Algorithm descriptions include thresholds and edge-case behavior." }
cross_linking = { score = 5, justification = "Related section with three sibling references." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Failure-condition bullets per level are three lines each, consistent." }

[runs.files."reference/validation-and-retries.md"]
coverage = { score = 5, justification = "All 5 covers items addressed. Error section template shown with attempt-number header." }
detail_compliance = { score = 5, justification = "Tabular. AST context inclusion rules as a four-row table." }
guidance_adherence = { score = 5, justification = "All four consistent sections." }
cross_linking = { score = 5, justification = "Related section lists four sibling references." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Brief intros, consistent tables." }

[runs.files."reference/llm-dispatch.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: CapabilityTier enum, default model mapping, call-site-to-tier assignments, ModelConfig, per-step override, batch_jobs schema, batch states with the storing invariant, signal names, search attribute, BatchPollerWorkflow, cache control, cache token fields, OTel attributes, CLI flags." }
detail_compliance = { score = 5, justification = "Tabular. One JSON example for per-step override." }
guidance_adherence = { score = 5, justification = "Batch state invariant explicitly called out in prose because it is a correctness property." }
cross_linking = { score = 5, justification = "Links to explanation and howto." }
quadrant_purity = { score = 5, justification = "Pure reference. Invariant paragraph is factual, not instructional." }
prose_quality = { score = 5, justification = "Tables do the work. Invariant paragraph three sentences." }

[runs.files."reference/observability.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: schemas, Alembic, CLI flags, log files, env vars, API message logs, OTel spans. LLMStats model table at the end." }
detail_compliance = { score = 5, justification = "Complete. ASCII span tree diagram." }
guidance_adherence = { score = 5, justification = "Alembic file paths, rotation policy, log format all stated." }
cross_linking = { score = 5, justification = "Links to explanation and howto." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Intros one or two sentences." }

[runs.files."reference/forge-run-extraction.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: playbooks schema, tag inference, data models, CLI commands, playbook injection, scope boundary. Unchanged." }
detail_compliance = { score = 5, justification = "Tabular. Two tables for tag inference, subsections for CLI commands." }
guidance_adherence = { score = 5, justification = "Explicit scope boundary. Redirects to transcript-ingestion reference." }
cross_linking = { score = 5, justification = "Links to explanation, howto, transcript-ingestion reference, learning-loops, context-assembly reference." }
quadrant_purity = { score = 5, justification = "Pure reference. Scope boundary enforced." }
prose_quality = { score = 5, justification = "Brief intros, consistent tables." }

[runs.files."reference/transcript-ingestion.md"]
coverage = { score = 5, justification = "All 7 covers items addressed: two workflows with inputs/outputs/signals/early-exit, activity, cross-queue calls, experience schema, model routing, CLI, module layout. Unchanged." }
detail_compliance = { score = 5, justification = "Tabular. One table per subject." }
guidance_adherence = { score = 5, justification = "Structure mirrors code path. Scope boundary explicit." }
cross_linking = { score = 5, justification = "Links to explanation, howto, forge-run-extraction reference, learning-loops." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "All intros short." }

[runs.files."reference/task-domains.md"]
coverage = { score = 5, justification = "All 4 covers items addressed plus per-domain examples and domain registry snippet. Unchanged." }
detail_compliance = { score = 5, justification = "Tabular. Per-domain example tables for three fields. Validation-defaults comparison across all domains." }
guidance_adherence = { score = 5, justification = "One row per field with the reading activity." }
cross_linking = { score = 5, justification = "Links to explanation and howto." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Brief summary after validation table explains the pattern." }

[runs.files."reference/ocr-pipeline.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: 8 OCR workflows, signals, all data models, two-layer status model with derivation rules, ocr_results + ocr_images tables, shared batch_jobs table, URI format, CLI. Unchanged." }
detail_compliance = { score = 5, justification = "Tabular. Status model explained in two-sentence prose." }
guidance_adherence = { score = 5, justification = "OCR-specific columns noted. Shared batch_jobs scope redirected." }
cross_linking = { score = 5, justification = "Links to howto, explanation, llm-dispatch reference." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Status-derivation rules stated as four short bullet points." }

[runs.files."reference/planner-eval.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: corpus format, 9 deterministic checks with pass conditions, judge criteria (6 criteria) and scoring configuration, data models, CLI flags and exit codes. Result storage path specified. Unchanged." }
detail_compliance = { score = 5, justification = "Tabular throughout. One JSON example of EvalCase." }
guidance_adherence = { score = 5, justification = "Check name, verification scope, and pass condition per check. Judge criteria with description per criterion." }
cross_linking = { score = 5, justification = "Links to howto and explanation." }
quadrant_purity = { score = 5, justification = "Pure reference. Now the exclusive home of the deterministic-check list and judge-criteria enumeration after the explanation was trimmed — the authoritative specification lives here." }
prose_quality = { score = 5, justification = "Brief intros. Run-comparison threshold stated in one sentence." }

[runs.files."reference/nushell-module.md"]
coverage = { score = 5, justification = "All 6 covers items addressed: module location, five commands, prerequisites, internal architecture. Unchanged." }
detail_compliance = { score = 5, justification = "One table per command. Return schemas given." }
guidance_adherence = { score = 5, justification = "Each command has consistent structure." }
cross_linking = { score = 5, justification = "Links to howto and ocr-pipeline reference." }
quadrant_purity = { score = 5, justification = "Pure reference." }
prose_quality = { score = 5, justification = "Module-location block is a two-row two-column table with no wasted text." }

# --- How-to Guides ---

[runs.files."howto/submit-tasks.md"]
coverage = { score = 5, justification = "All 6 covers items addressed. The previously-missing 'how to inspect plan output before execution starts' item is now covered by a dedicated section: observe planner output via `-v`, cancel via `temporal workflow cancel` if the plan is wrong, or run the planner in isolation via `forge eval-planner`. The section gives a complete pre-execution inspection workflow." }
detail_compliance = { score = 5, justification = "CLI-focused recipes with brief intros and commands. JSON task file shown as annotated example with per-field table." }
guidance_adherence = { score = 5, justification = "Each recipe introduces the task, then shows the command. JSON task format presented with field descriptions." }
cross_linking = { score = 5, justification = "Links to explanation and reference at the top, and within-file to run-evaluations for the eval-planner route." }
quadrant_purity = { score = 4, justification = "Intro paragraphs explain pipeline behavior ('The LLM receives the target file contents, auto-discovered imports, and repository structure. It produces edits for existing files or full content for new files.') rather than simply introducing the task. A stricter how-to would drop these." }
prose_quality = { score = 4, justification = "Direct command-focused where the reader needs it. Some paragraphs describe pipeline behavior in sentences that could be cut or moved to the explanation." }

[runs.files."howto/control-context-assembly.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: --context-file, --include-deps, --no-explore/--max-exploration-rounds, diagnose file inclusion, check token budget utilization. Unchanged." }
detail_compliance = { score = 5, justification = "Short, task-focused sections. Budget utilization section shows a stats block." }
guidance_adherence = { score = 5, justification = "Each section addresses one specific 'I want to' scenario." }
cross_linking = { score = 5, justification = "Links to explanation and reference at the top." }
quadrant_purity = { score = 4, justification = "The 'diagnose why a file was or was not included' section has four bulleted explanations that verge on teaching, though each includes actionable next steps." }
prose_quality = { score = 5, justification = "Command-focused. No filler." }

[runs.files."howto/configure-validation.md"]
coverage = { score = 5, justification = "All 4 covers items addressed plus auto-fix and task-file config. Unchanged." }
detail_compliance = { score = 5, justification = "Short task-focused sections with CLI examples." }
guidance_adherence = { score = 5, justification = "No teaching. Links to explanation and reference." }
cross_linking = { score = 5, justification = "Related section with three sibling references." }
quadrant_purity = { score = 5, justification = "Pure how-to." }
prose_quality = { score = 5, justification = "Brief. Recipes are one or two commands plus a one-sentence note." }

[runs.files."howto/configure-llm-dispatch.md"]
coverage = { score = 5, justification = "All 4 covers items addressed: sync mode, batch job status, batch poll interval, model tier override (whole-task and per-step). Unchanged." }
detail_compliance = { score = 5, justification = "Short CLI-focused recipes. Per-step override with JSON task file." }
guidance_adherence = { score = 5, justification = "SQL query included for batch_jobs inspection." }
cross_linking = { score = 5, justification = "Links to explanation and reference." }
quadrant_purity = { score = 4, justification = "The --sync recipe includes a 'Use --sync when:' three-bullet decision aid. Useful but edges into teaching." }
prose_quality = { score = 4, justification = "Command-focused. Prose inserts are one or two sentences." }

[runs.files."howto/debug-workflow.md"]
coverage = { score = 5, justification = "All 7 covers items addressed as numbered sections with 'Symptom:' leads." }
detail_compliance = { score = 5, justification = "Each section starts with a symptom and walks through numbered steps, exactly as the detail field specifies." }
guidance_adherence = { score = 5, justification = "Symptom-based structure throughout. jq pipelines and temporal CLI commands included." }
cross_linking = { score = 5, justification = "Links to reference and explanation." }
quadrant_purity = { score = 5, justification = "Pure how-to. Every step actionable." }
prose_quality = { score = 5, justification = "Direct, command-focused." }

[runs.files."howto/manage-playbooks.md"]
coverage = { score = 5, justification = "All 4 covers items addressed. Unchanged." }
detail_compliance = { score = 5, justification = "Short CLI-focused recipes, scoped to forge's store." }
guidance_adherence = { score = 5, justification = "Task-focused sections. SQL command for direct-database correction." }
cross_linking = { score = 5, justification = "Links to ingest-transcripts, explanation, reference." }
quadrant_purity = { score = 5, justification = "Pure how-to." }
prose_quality = { score = 5, justification = "Brief. Scope sentence is one clause." }

[runs.files."howto/ingest-transcripts.md"]
coverage = { score = 5, justification = "All 6 covers items addressed as dedicated recipes." }
detail_compliance = { score = 5, justification = "Short CLI-focused recipes with numbered steps. Sample output for most recipes." }
guidance_adherence = { score = 5, justification = "Prerequisites block names three runtime dependencies." }
cross_linking = { score = 5, justification = "Links to explanation and reference. Cross-references forge status for poller state." }
quadrant_purity = { score = 4, justification = "Debug-stuck-ingestion section has four numbered steps with one-sentence explanations of what to look for. Debugging decision aids — borderline teaching." }
prose_quality = { score = 5, justification = "Tight. No filler." }

[runs.files."howto/add-domain.md"]
coverage = { score = 5, justification = "All 5 covers items addressed: four files to modify plus prompt-writing, validation-defaults, and testing." }
detail_compliance = { score = 5, justification = "Step-by-step recipe with code-level diffs." }
guidance_adherence = { score = 5, justification = "Assumes competence. Each step shows exact code change." }
cross_linking = { score = 5, justification = "Links to explanation and reference." }
quadrant_purity = { score = 4, justification = "The 'Checklist for the config' subsection is a glossary-shaped review list rather than sequential action steps." }
prose_quality = { score = 5, justification = "Direct. Code blocks show only relevant lines." }

[runs.files."howto/run-ocr.md"]
coverage = { score = 5, justification = "All 6 covers items addressed plus soft-delete and hash backfill. Unchanged." }
detail_compliance = { score = 5, justification = "CLI-focused recipes. Each command shown with expected JSON output." }
guidance_adherence = { score = 5, justification = "Every recipe is a forge start + JSON input invocation." }
cross_linking = { score = 5, justification = "Links to explanation, reference, Mistral batch docs." }
quadrant_purity = { score = 5, justification = "Pure how-to." }
prose_quality = { score = 5, justification = "Brief. Expected-output blocks trimmed." }

[runs.files."howto/run-evaluations.md"]
coverage = { score = 5, justification = "All 4 covers items addressed plus save-to-file, JSON output, pre-generated-plans evaluation. Unchanged." }
detail_compliance = { score = 5, justification = "CLI-focused recipes with sample output." }
guidance_adherence = { score = 5, justification = "Task-focused. JSON eval case shown as a complete example." }
cross_linking = { score = 5, justification = "Links to explanation and reference." }
quadrant_purity = { score = 4, justification = "The 'Interpret evaluation results' section has four bold-labeled definitions (Deterministic PASS, FAIL, SKIP, Judge scores). Reader decision support, but glossary-shaped." }
prose_quality = { score = 5, justification = "Command-focused." }

[runs.files."howto/use-nushell-module.md"]
coverage = { score = 5, justification = "All 6 covers items addressed. Unchanged." }
detail_compliance = { score = 5, justification = "Nu commands with one-line descriptions. Pipeline examples show par-each, filter, sort-by composition." }
guidance_adherence = { score = 5, justification = "Every recipe is a nu command." }
cross_linking = { score = 5, justification = "Links to reference and explanation." }
quadrant_purity = { score = 5, justification = "Pure how-to." }
prose_quality = { score = 5, justification = "Very brief. Most recipes are a one-liner." }
