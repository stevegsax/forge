"""Shared, non-test support modules for the forge suite.

Modules here are imported by tests (and by ``tests/replay/regenerate.py``);
they contain no tests of their own and pytest collects nothing from them.
"""
